# DIOGENES OVERNIGHT — anneal-memory, 2026-08-11

**Source selector:** `episodic.py read --since 24h --tags anneal-memory  (filtered to Agent: diogenes at 2026-08-11*)`
**Stated count: 6 episode(s).** Severity: GOOD HALF 1 · LOW 2 · MEDIUM 2 · SELF 1
**STILL OPEN: NOT REPORTED BY DIOGENES THIS WINDOW.** No slot was emitted, so no count is written here. Do not infer one — an invented debt number is the defect `spore-498` exists to stop.

> ⚠ **UNTRIAGED AND VERBATIM.** Generated by `scripts/route_diogenes.py` from the episodic store without judgement. Nothing here has been checked against disk except where a line says so explicitly. Diogenes' own `[prescription: ...]` tags are his assessment, not a ruling.

---

```
[laptop/flow]   ID: diogenes-20260811-043310-669b2894e28e
  Time: 2026-08-11T04:33:10  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: MEDIUM: anneal_memory/graduation.py:160,162 — the fix corrected FOUR sites across TWO parallel walkers, and only the _NAMED_PATTERN_RE half is pinned by a test. Mutation-checked in /tmp/am_dio_20260811 at fef6af0: reverting `!+`->`!!|!` at line 131 turns exactly 1 test red, at 133 turns 6 red — and reverting EITHER line 160 or 162 leaves the full suite 1654 passed, 0 failed. The behaviour that revert silently loses is real and measured on both consumers of that regex: (a) graduation.py:886 — validate_graduations on `- !!! big_one | 3x (2026-08-11) [evidence: a1b2c3d4 "..."]` plus a second `!!` pattern returns graduated_names ["big_one","small_one"] at HEAD and ["small_one"] with the second walker reverted, so the `!!!` pattern forms no Hebbian co-graduation link (continuity.py:2229 needs >=2 names); (b) graduation.py:697 — the same `!!!` line against a pattern_history whose prior explanation shares 4 meaningful words returns cross_session_collisions [("big_one",["broke","clearance","collar","walk"])] / demoted 1 at HEAD, and [] / validated 1 with the walker reverted: the cross-session SYCOPHANCY GATE silently validates the highest salience tier where it should demote. The commit message names the recorded `parallel_walker_drift` pattern as its reason for fixing both regexes and then leaves the second one guarded by nothing, so the exact drift it cites can recur and the suite will stay green. Fix: a test on _NAMED_PATTERN_WITH_EVIDENCE_RE directly (`!!!` with evidence, dash AND column-zero) plus the two behavioural assertions above — I wrote it as tests/test_dio_proposed.py in the copy and mutation-verified it: 4 passed at HEAD, 1 failed on reverting 160, 1 failed on reverting 162, 4 failed on reverting both. [quoted: `    r"|(?=(?:!+|\?|✓|\*)[ \t])"` (:160) and `    r"(?:(?:!+|\?|✓|\*)[ \t]+)?"` (:162)] [prescription: run]
  Tags: anneal-memory, code_quality, code_review
```

---

```
[laptop/flow]   ID: diogenes-20260811-043323-a174de226c05
  Time: 2026-08-11T04:33:23  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: MEDIUM: anneal_memory/continuity.py:930-932 — the READER now accepts any run of `!`; the WRITER-side prompt the library hands the agent still teaches `!!` as the top tier, and the test that exists to stop exactly this divergence cannot see it. _marker_reference() renders the pattern-line contract into the wrap package (called at continuity.py:779), and its marker list is closed at four entries — verified by rendering it: `_marker_reference("2026-08-11")` emits "- `!` urgent / load-bearing" and "- `!!` highest urgency" and nothing above. So an agent composing from the generated instructions is told `!!` is the maximum salience while graduation.py:131/133 now parse `!!!` and longer (and flows own live neocortex already writes `!!!` lines today). This is the write-side/read-side asymmetry already in my Developing Knowledge (write_side_guard_incomplete_relative_to_read_side_guard) — the fix moved the reader and left the instruction. Sharper half: tests/test_graduation.py:1084 TestCanonicalTemplateFormatEndToEnd states its whole purpose as pinning "the format the template now teaches against the regex the immune system uses, preventing silent re-divergence" — but its fixture `_canonical_continuity` (:1094) is a hand-copied f-string literal that never calls _marker_reference, so it is coupled to nothing. RUN-VERIFIED in the copy: deleting the `!!` teaching line from the template outright leaves that class 3 passed; changing the template to teach a bogus `@@` marker the parser rejects ALSO leaves it 3 passed. The guard cannot move when the defect appears. Fix: add the `!!!`/`!+` tier to the template list, and derive the parity fixture from the rendered _marker_reference() output instead of hand-copying it. Not executed. Secondary, PRE-EXISTING not from this diff: the template omits `*` entirely though the regex accepts it. [quoted: `- ``!!`` highest urgency → ``- !! pattern_name | 3x ({today}) [evidence: ...]``` (continuity.py:932); `    immune system uses, preventing silent re-divergence."""` (test_graduation.py:1092)] [prescription: reasoned]
  Tags: anneal-memory, code_review, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260811-043334-13c735c0eba1
  Time: 2026-08-11T04:33:34  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: LOW: anneal_memory/graduation.py:73 — the grammar comment that documents the very regex this commit changed still enumerates the marker set as a closed four, 58 lines above the changed hunk and inside the block a maintainer reads before touching it. The comment says the explicit pattern signal is ONE of a dash bullet, a FlowScript marker `!` `!!` `?` `✓` `*`, or leading indentation; :131 and :133 now accept `!+`. The example block at :104-108 likewise shows `- !! pattern_name | 3x` and no `!!!` line, so a reader checking "is a triple valid here?" gets a confident answer to the wrong question. This matters more than ordinary doc-rot because a closed enumeration of the bang tiers is the artifact that produced the defect in the first place — CHANGELOG.md:603 records v0.3.3 tightening the alternation to match "only the canonical marker set", and the canonical set was read off prose exactly like this line. Fix: state the rule as "a RUN of one or more `!`" in both the enumeration and the examples. Not executed. [quoted: `# FlowScript marker (``!`` ``!!`` ``?`` ``✓`` ``*``), OR leading INDENTATION`] [prescription: reasoned]
  Tags: anneal-memory, code_review, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260811-043344-8008b22ab515
  Time: 2026-08-11T04:33:44  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: LOW: CHANGELOG.md:5 — a public-parser behaviour change landed in a SemVer PyPI-published library (pyproject.toml:7 version = "0.9.6") with an empty `## [Unreleased]` section, leaving the newest word in the changelog on this exact subject stating the opposite of HEAD. CHANGELOG.md:603 frames matching `!!!` as the defect that v0.3.3 fixed; nothing supersedes it. The historical entry is NOT false — it accurately records what v0.3.3 did — so this is an absence, not an error, which is why it is LOW and not higher; and the change is unreleased, so the release step can still catch it. But the convention is unambiguous: git log over the last 15 commits touching CHANGELOG.md shows every behaviour-changing commit (AM-CONSOLIDATE-EFFERENT, AM-SPORE-CAS, AM-RECALL-IDF, AM-CONTLOCK...) carrying its own entry, and these two do not. An adopter grepping the changelog for `!!!` today learns it is rejected by design. Fix: an `## [Unreleased]` entry naming the widened alternation, plus a superseded-by pointer on the :603 line. Not executed. [quoted: `## [Unreleased]` (:5); `- **``_NAMED_PATTERN_RE`` FlowScript marker alternation tightened** (Codex L3 v0.3.2 review). v0.3.2s ``[!?✓*]+`` was wider than the prose said — it matched ``!!!``, ``!*?``, ``??``, etc.` (:603)] [prescription: reasoned]
  Tags: anneal-memory, code_review, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260811-043410-0232d4625034
  Time: 2026-08-11T04:34:10  Agent: diogenes  Type: observation
  Source: laptop_overnight_review
  Content: GOOD HALF, RE-DERIVED NOT CARRIED — every claim in fef6af0 commit message verified against disk, and the seat honored the criterion. Ran in a copy at /tmp/am_dio_20260811, real repo untouched (porcelain empty at fef6af0). (1) At 954a860 the suite is 5 failed / 1649 passed and all 5 reds are the criterion file — the defect genuinely reproduces. At fef6af0 it is 1654 passed / 0 failed, so the +5 delta is exactly the criterion tests, as claimed. (2) `git diff 954a860..fef6af0 -- tests/test_pattern_salience_prefix.py` is EMPTY — the implementer did not edit its own acceptance test, which was the loud constraint on the handoff. (3) The two sites that ARE pinned are mutation-verified: reverting graduation.py:131 turns exactly test_marker_at_column_zero_also_handles_triple red, reverting :133 turns 6 red including the omission-detector pair. (4) The `!!!` round-trip is clean end to end, not just at the parser: a `!!!` line that fails grounding demotes to `- !!! ghost_one | 2x (2026-08-11) (ungrounded)` — prefix preserved — and re-parses to {"ghost_one": 2}; a `!!!` line caught by the cross-session gate demotes to `(cross-session-overlap)` correctly. (5) mypy clean, 18 source files. (6) The widening adds no new false-positive class: `!!` at column 0 already matched pre-fix, so the only newly-accepted shape is a run of 3+, and `- !!! | 3x` (prefix with no identifier) still returns {}. THE SHAPE OF TONIGHTS RESIDUE, and it is a fact about the offload loop rather than about this seat: the code change is correct and complete at all four sites, and ALL FOUR findings are in companion artifacts the criterion never mentioned — the untested second walker, the generated wrap prompt, the grammar comment, the changelog. The criterion scoped itself to `anneal_memory/graduation.py` and got exactly that. Residue lives where the criterion stopped looking, which is the primitive stated the other way round.
  Tags: anneal-memory, code_review, criterion, offload_seat
```

---

```
[laptop/flow]   ID: diogenes-20260811-043445-0ffae86ae93d
  Time: 2026-08-11T04:34:45  Agent: diogenes  Type: observation
  Source: laptop_overnight_review
  Content: SELF-CHECK, Aug 11 (anneal-memory): FOUR FOR FOUR ON COMPANION ARTIFACTS, WHICH IS EITHER A REAL SHAPE OR MY OWN CHEAPEST MOVE. The code change tonight is small (four regex sites) and I could not fault it — mutation-verified correct, round-trip clean, mypy clean, no new false-positive class. Every finding I filed is therefore about something ADJACENT to the code: a coverage gap, a prompt, a comment, a changelog. That is exactly the distribution a reviewer produces when the diff is clean AND exactly the distribution a reviewer produces when it is padding, and the two are indistinguishable from the finding list alone. What separates them here: two of the four carry a measured behavioural consequence I ran (graduated_names losing a name, the sycophancy gate flipping demote->validate), and the third and fourth I explicitly graded LOW rather than dressing them up. The one I would attack if I were Chip is #4 — a missing changelog entry on an UNRELEASED change is arguably not a finding at all, and I filed it partly because the repo convention is 15/15. I kept it LOW and said why in the body; if it recurs as a filed finding on a future unreleased window, that is inflation and should be dropped. MECHANICAL CARRY THAT PAID TONIGHT: my Aug-7 rule (when the window touches the function an open finding lives in, run BOTH sides of the diff) generalised — I ran the suite at 954a860 as well as HEAD, which is what let me confirm the +5 delta claim instead of accepting it. And the Aug-6 rule held: every coordinate in all four findings was pasted from `sed -n Np FILE` at HEAD, not retyped from a read buffer. FIFTH CONSECUTIVE NIGHT THE RISK LIVED AT A SEAM: 08-04/05 diagnosis-vs-prescription, 08-06 claim-vs-citation, 08-07 open-vs-widened, tonight fixed-vs-pinned. ⚠ Last full constellation drift sweep: 2026-05-24, now ~11.5 weeks. The cognitive half of the dual lens still has not run in a quarter of a year, I have noted it three nights running, and noting it is not running it — diagnosis_without_action_applies_to_surveillance_too, pointed at me.
  Tags: anneal-memory, grounding_floor, self_check
```
