# DIOGENES OVERNIGHT — anneal-memory, 2026-08-15

**Source selector:** `episodic.py read --since 24h --tags anneal-memory  (filtered to Agent: diogenes at 2026-08-15*)`
**Stated count: 9 episode(s).** Severity: COVERAGE 1 · HIGH 2 · LOW 2 · MEDIUM 3 · STILL OPEN 1
**STILL OPEN: 8**

> ⚠ **UNTRIAGED AND VERBATIM.** Generated by `scripts/route_diogenes.py` from the episodic store without judgement. Nothing here has been checked against disk except where a line says so explicitly. Diogenes' own `[prescription: ...]` tags are his assessment, not a ruling.

---

```
[laptop/flow]   ID: diogenes-20260815-045047-bc9e4195d4c7
  Time: 2026-08-15T04:50:47  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: HIGH: anneal_memory/crystal.py:938-941 — the CrystalDecision consumer contract still specifies the ceiling that 0f57107 removed, and it has a LIVE READER that implements it, so the OUT path the release claims to have opened is still shut for exactly the population the release exists to rescue. The docstring tells every consumer that crystallize requires level in {2,3} and that a router MUST guard on it; flow/scripts/crystal_decision_apply.py:125 does precisely that, so a cold 4x/6x/10x pattern routed to crystallize is pushed onto surfaced_back with the message "crystallize needs level 2/3 + explanation" instead of crystallizing. That script is wired into the anansi and daemon overnight OUT paths (scripts/anansi_overnight_synthesis_argus.py:173, scripts/daemon_overnight_exploration_argus.py:181), so for those two entities AM-LEVELCAP part 2 is inert. crystal.py was edited in this window at :124-154, :521-527 and :832-840; this contract sits 100 lines below the last hunk and was not touched. — The fix: update the contract to level >= MIN_PROVEN_LEVEL (or level is None), and fix the reader in flow. Both are needed; the docstring alone leaves anansi and daemon capped. [quoted: `So a consumer routing ``crystallize`` MUST guard` / `` ``level in (2, 3)`` `` / flow: `if level not in (2, 3) or not explanation:`] [prescription: reasoned]
  Tags: anneal-memory, code_review, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260815-045100-891a7dae383f
  Time: 2026-08-15T04:51:00  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: HIGH: anneal_memory/graduation.py:57-61,78 — the silent-drop defect AM-LEVELCAP was written to close SURVIVES INTACT on the bare path for the identical 4x+ population, and the comment that documents the asymmetry as "inert" is refutable by running the function. _BARE_GRADUATION_RE stays [23], so a today-dated evidence-free line at 4x+ matches no branch at all. RUN-VERIFIED against HEAD in a clone (/tmp/am_dio_20260815/repo), validate_graduations with citations_seen=True: at 2x/3x with no history the line demotes and bare_demoted=1; at 4x/6x/10x bare_demoted=0, carried_forward=0, demoted=0, skipped_non_today=0 and the line comes back BYTE-UNCHANGED. With warm at-peak history the 2x/3x line is HELD and marked (carried-forward); the 4x/6x/10x line gets no CarriedForward record at all. Same for the AM-PROVENANCE mature-carry: a 3x line with [provenance:] returns CarriedForward(provenance=True), a 6x line returns []. This is the exact property the diffs own new test asserts for the cited path (tests/test_graduation.py test_a_high_level_line_is_not_silently_dropped, "a 4x line must be accounted for by exactly one outcome") and it is FALSE for the bare path. The comment reasoning is also wrong on its own terms: the mass-demotion blast radius it fears is what _bare_carryforward_decision (AM-PRESERVE-BARE-PATH, v0.5.0) already prevents, six lines below the regex, and the run above shows it doing so at 2x/3x. The live flow neocortex carries four today-dated bare lines at 4x/5x/9x/10x right now, and continuity.py:970-990 explicitly instructs the composer to re-stamp mature patterns to today. — The fix: widen _BARE_GRADUATION_RE to the same ([2-9]|\d{2,}) and let the carryforward hold do the job it was built for. [quoted: `` ``_BARE_GRADUATION_RE`` below stays ``[23]`` `` / `out in the normal case, so the asymmetry is inert`] [prescription: run]
  Tags: anneal-memory, code_quality, code_review
```

---

```
[laptop/flow]   ID: diogenes-20260815-045118-2b65cc26d767
  Time: 2026-08-15T04:51:18  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: MEDIUM: anneal_memory/continuity.py:923 — the library's OWN generated wrap instruction still teaches the ceiling the library just removed, under a heading that claims to describe what the immune system reads. The block at :910 opens "### Pattern Line Format (CRITICAL — this is what the immune system reads)" and :923 states the marker range as N is 1, 2, or 3; :956 stops the ladder at "Validates existing 2x -> 3x" with no rung above. The immune system now reads 2 and up (_GRADUATION_RE is ([2-9]|\d{2,})). This is the AUTHORING surface: prepare_wrap emits this text and the composing agent writes the neocortex from it, so a fixed library shipped with an unfixed prompt re-imposes the cap on every consumer that follows the instructions. The release's own argument is that the methodology belongs to the caller and the library may not encode it — that argument applies to the generated prompt as much as to the regex, and continuity.py is not touched anywhere in this window. SCOPE LIMIT: whether to RECOMMEND graduating past 3x is a methodology call I am not making; what is wrong on disk is a format spec, presented as a description of the parser, that is narrower than the parser. — The fix: state the range truthfully at :923. [quoted: `### Pattern Line Format (CRITICAL — this is what the immune system reads)` and `- Graduation marker `| Nx ({today})` where N is 1, 2, or 3`] [prescription: reasoned]
  Tags: anneal-memory, code_review, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260815-045137-9c9adcc51ed9
  Time: 2026-08-15T04:51:37  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: MEDIUM: anneal_memory/crystal.py:737 — six prose sites across the two files this window edited still state the level range as 2-or-3, all of them now false against the code beside them. Verbatim at HEAD: crystal.py:737 "monotonic upsert), but still must be 2 or 3." on CrystalStore.update, whose very next branch calls self._validate_level (crystal.py:747), the function this diff widened to >= MIN_PROVEN_LEVEL. graduation.py:274 "    level: int  # Level at which the pattern graduated (2 or 3)"; graduation.py:421 "    today_level: int  # Level the agent attempted to graduate to (2 or 3)"; graduation.py:514 "    prior_level: int  # Level at which it appeared in the previous wrap (2 or 3)"; graduation.py:1834 "        level: The current level (2 or 3) to demote from." — _demote_line now legitimately receives 10 and correctly writes 9x (observed in the live neocortex, which records "demoted from 10x this wrap"). graduation.py:530 "Scans the ## Patterns section for 2x/3x lines with [evidence: <id> \"explanation\"]." is validate_graduations' own summary line and is the one a reader hits first. This is the comment-states-a-NUMBER class: each is cheap to fix and each will otherwise be read as the contract by the next person to touch these functions. Counted as ONE defect, six sites. — The fix: mechanical, restate as ">= 2". [quoted: `monotonic upsert), but still must be 2 or 3.` and `    level: int  # Level at which the pattern graduated (2 or 3)`] [prescription: reasoned]
  Tags: anneal-memory, code_review, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260815-045137-b9eb44bf27df
  Time: 2026-08-15T04:51:37  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: MEDIUM: CHANGELOG.md:31 — the release notes claim nothing pinned the ceiling, and the same diff DELETES the assertion that pinned it. The line reads "Both fixes are pinned by mutation-verified tests. The suite was green **before and after** the level-cap fix (nothing pinned the ceiling), which is itself why the new tests assert the property". "The level-cap fix" is defined in the same entry as BOTH halves. MEASURED, not reasoned: at fef6af0 (pre-fix) the suite is 1654 green; patching ONLY the graduation regex onto that suite leaves it 1654 green, so the claim holds for the graduation half; patching the crystal half onto it turns tests/test_crystal.py::TestCrystallize::test_validation_raises_valueerror[bad3] RED — 1 failed, 1653 passed. That is the `{"name": "p", "level": 4, "explanation": "x"}` bad-arg case the diff removes, and the new test's own docstring says so: "``{\"level\": 4}`` used to sit in the bad-args list above, asserting a ceiling". So the CHANGELOG and the test file disagree about the same fact in one commit. Impact is on the record, not on behaviour: a maintainer reading "nothing pinned the ceiling" will not go looking for a deleted assertion. — The fix: scope the sentence to the graduation half, which is where it is true. [quoted: `The suite was green **before and after** the level-cap fix (nothing pinned the ceiling)`] [prescription: run]
  Tags: anneal-memory, code_review, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260815-045152-1db4d65e9728
  Time: 2026-08-15T04:51:52  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: LOW: anneal_memory/crystal.py:139-140 — a sentence is placed in quotation marks and attributed to the shipped methodology, and that string exists nowhere in the repository. The new comment reads: which is exactly what the shipped methodology warns against: "not a one-way 3x ratchet — without an OUT path it bloats until an always-loaded list stops working". A `grep -rF` for `without an OUT path it bloats until an always-loaded list stops working` across every .py and .md in the tree returns ZERO hits outside crystal.py:140 itself. The two real texts it is a paraphrase of are this same file's module docstring at crystal.py:5 ("graduates IN at 3x and the section had no OUT path — a one-way ratchet, so the") and continuity.py:1147 ("one-way 3x ratchet that monotonically bloats until an always-loaded list stops"), and neither is "the shipped methodology" — both are the library's own prose. The cost is greppability: the quotation marks promise a locatable source and a reader who goes looking finds nothing. — The fix: quote crystal.py:5 verbatim, or drop the quotation marks. [quoted: `"not a one-way 3x ratchet — without an OUT` / `path it bloats until an always-loaded list stops working".`] [prescription: run]
  Tags: anneal-memory, code_review, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260815-045152-758b88fa6d45
  Time: 2026-08-15T04:51:52  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: LOW: anneal_memory/crystal.py:5 — the module docstring and the comment this diff EDITED, 120 lines apart in one file, now state different tier boundaries. The docstring at :3-5 says the working set is "developing (1x/2x)" and that "a pattern graduates IN at 3x"; the comment rewritten in this window at :126-127 says Proven-tier wisdom crystallizes and "a pattern graduates IN to the working set at 2x; crystallization is the stage PAST that", with MIN_PROVEN_LEVEL = 2 on the line below. Under the docstring's model 2x is developing and MIN_PROVEN_LEVEL is off by one; under the comment's model it is correct. The author's hand was on this exact sentence (the diff changes it from "at 2x/3x ... PAST 3x" to "at 2x ... PAST that") and the file's opening paragraph — the first thing any reader of this module sees — was left on the old model. — The fix: restate :3-5 on the 2x boundary. [quoted: `graduates IN at 3x and the section had no OUT path` and `# at 2x; crystallization is the stage PAST that`] [prescription: reasoned]
  Tags: anneal-memory, code_review, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260815-045213-62d8de41709a
  Time: 2026-08-15T04:52:13  Agent: diogenes  Type: observation
  Source: laptop_overnight_review
  Content: STILL OPEN (8), total live defect count at HEAD a34eaa2 for anneal-memory, all eight verified against disk tonight. SEVEN ARE NEW, all filed this window: crystal.py:938-941 consumer contract still specifies level in {2,3} and flow/scripts/crystal_decision_apply.py:125 implements it, so the OUT path is still shut for anansi and daemon (HIGH); graduation.py:78 _BARE_GRADUATION_RE keeps [23] so the silent drop survives for bare 4x+ lines, run-verified (HIGH); continuity.py:923 generated wrap instruction still says N is 1, 2, or 3 (MEDIUM); six prose sites stating 2-or-3 in the two files this window edited, crystal.py:737 + graduation.py:274/421/514/530/1834 (MEDIUM); CHANGELOG.md:31 claims nothing pinned the ceiling and the same diff deletes the assertion that did, measured red (MEDIUM); crystal.py:139-140 quotes a sentence that exists nowhere in the tree (LOW); crystal.py:5 module docstring puts the IN boundary at 3x against the comment this diff edited putting it at 2x (LOW). ONE IS CARRIED from the 2026-05-22 review and re-verified live tonight: assert-for-narrowing at exactly three sites (server.py:201, continuity.py:1572, continuity.py:2267), which now also contradicts the house rule written at graduation.py:661-664 (narrow for mypy WITHOUT an assert, -O-safe). ONE CARRIED FINDING CLOSED, verified against the diff that closed it: my 2026-05-22 bare-demotion literal-replace LOW is fixed at graduation.py:1088-1093, and the fix comment credits it by name. NOT IN THE COUNT: the README Jain-citation precision note carried in my own continuity is unchanged since 0c79d9d (2026-04-22) but its content claim was NOT re-checked tonight (no network), so I am not counting an unverified item in a number labelled live.
  Tags: anneal-memory, code_review, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260815-045238-b43ffb2ad20d
  Time: 2026-08-15T04:52:38  Agent: diogenes  Type: observation
  Source: laptop_overnight_review
  Content: COVERAGE: RE-DERIVED BY RUNNING — cloned HEAD a34eaa2 to /tmp/am_dio_20260815/repo, built a venv, editable install, and ran the FULL suite: 1676 passed, 3 subtests, 0 failed. mypy on anneal_memory: Success, no issues in 18 source files. MUTATION-CHECKED BOTH HALVES OF THE FIX, each reversion turning exactly its own new tests red: reverting _GRADUATION_RE to ([23]) and _validate_level to `not in VALID_LEVELS` gives 15 failed / 263 passed on test_graduation.py + test_crystal.py, and the failures are precisely TestLevelCapAM_LEVELCAP and TestCrystallize::test_there_is_NO_UPPER_BOUND_on_level / test_monotonic_raise_holds_above_3 — the new tests are real, not decorative. Ran the PRE-fix tree at fef6af0 (1654 green) and patched each half onto it separately to test the CHANGELOG's coverage claim. Drove the CLI end to end against a scratch db: `crystal crystallize --level 10` succeeds and prints "Crystallized: deep_pattern (10x, timeless/just-in-time)"; `--level 1` and `--level abc` produce the intended argparse errors. Exercised validate_graduations directly at levels 2/3/4/6/10 across four shapes (bare no-history, bare warm-at-peak, provenance-carry, evidence-bearing) and recorded the accounting for each. Read the whole of the diff plus the un-diffed consumers that substantiate it: graduation.py's regex block and main validation loop, crystal.py module docstring / update() / _validate_level / CrystalDecision / _extract_pattern_meta, cli.py's _proven_level and both --level sites, continuity.py's prepare_wrap crystallization-candidate filter, _build_pattern_format_instructions and the graduate_out warning, and flow/scripts/crystal_decision_apply.py with its two overnight callers. Grepped the whole package for every numeric level gate and confirmed no OTHER code-level cap survives (extract_proven_patterns, detect_pattern_omissions, crystallization_candidates and graduate_out are all open-ended >= comparisons). Ran the live flow neocortex through both the old and new regex: 0 evidence-bearing lines visible to ([23]), 3 visible to ([2-9]|\d{2,}) at 5x/7x/8x, and 4 today-dated bare lines at 4x/5x/9x/10x. Pre-flight `grep -F` on all 14 quoted strings and `grep -n`/`sed -n` on every coordinate before filing; the fabricated-quote claim was confirmed by a repo-wide grep returning 0 hits. Repo UNTOUCHED — porcelain empty at a34eaa2, HEAD == origin/main, every mutation ran in the /tmp clone. NOT CHECKED: the CHANGELOG's headline measurement (validated 1 -> 7, direct_co_citations 0 -> 4, session pairs 0 -> 6, 6 associations on the live store) is NOT reproducible from here — it was taken against the pre-wrap neocortex text of 2026-08-14 morning, which has since been replaced by the evening consolidate; I verified the DIRECTION on the current file and nothing more, so those five numbers stand unaudited. I did NOT open the real anneal store or run association_stats, so the claim that 6 associations formed is unverified. I did not re-check the README Jain citation (no network) or any other external citation in README/docs. I did not read tests/ outside test_crystal.py and test_graduation.py, and I did not audit the 1676-green suite for what it fails to cover beyond the level axis. I did not run the MCP server or exercise server.py at all. And I did not review the flow-side scripts as code — I opened crystal_decision_apply.py only far enough to substantiate that the stale anneal contract has a live reader. [prescription: none]
  Tags: anneal-memory, code_review, semantic
```
