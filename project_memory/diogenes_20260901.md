# DIOGENES OVERNIGHT — anneal-memory, 2026-09-01

**Source selector:** `episodic.py read --since 24h --tags anneal-memory  (filtered to Agent: diogenes at 2026-09-01*)`
**Stated count: 10 episode(s).** Severity: LOW 2 · MEDIUM 5 — 7 of 10 episode(s) carry a severity; the other 3 are COVERAGE 1 · COVERAGE-OPEN 1 · STILL OPEN 1
**STILL OPEN: 14** — **7 filed tonight** (measured at HEAD this window) · **7 carried**. A carried item was NOT necessarily re-checked this window; only the slot block below says which ones were.

> ⚠ **UNTRIAGED AND VERBATIM.** Generated by `scripts/route_diogenes.py` from the episodic store without judgement. Nothing here has been checked against disk except where a line says so explicitly. Diogenes' own `[prescription: ...]` tags are his assessment, not a ruling.

---

```
[laptop/flow]   ID: diogenes-20260901-044056-6ba567fd738f
  Time: 2026-09-01T04:40:56  Agent: diogenes  Type: observation
  Source: laptop_overnight_review
  Content: COVERAGE-OPEN: anneal-memory 2026-09-01, HEAD b5a6e7c4db1d3db06d7202671433414a6111f87f (== origin/main? NO — local is AHEAD 2 and the two commits in this very window are UNPUSHED; `git ls-remote origin main` against the LIVE remote returns a34eaa28d21de035b37818dac3ed12c720ba5523, which is the 0.9.7 release commit, so both b5a6e7c and acf5d7a exist only on this laptop), `git status --porcelain` empty, window a34eaa2..b5a6e7c = 2 commits. Note /Users/phillipclapham/Documents/anneal-memory is a SYMLINK to /Users/phillipclapham/Briefcase/anneal-memory — same repo, one checkout. REVIEW STARTED, NOT FINISHED — if no COVERAGE line for this repo exists in this run, THIS RUN DIED and the finding list is PARTIAL. [prescription: none]
  Tags: anneal-memory, code_review, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260901-045009-b4e67d16b1fb
  Time: 2026-09-01T04:50:09  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: MEDIUM: anneal_memory/continuity.py:972 — THE DELIVERED WRAP INSTRUCTIONS NOW CONTRADICT THEMSELVES ABOUT THE ONE CLAIM THIS COMMIT WENT BACK AND CORRECTED. acf5d7a records, in its own message, a codex L3 MED it accepted: "I wrote that the serialized Nx is a monotonic high-water mark. False - _demote_line docstring is 3x->2x or 2x->1x. The visible level is current standing... Corrected." It WAS corrected, at :933-934 — [quoted: `the visible ``Nx`` is current standing, not a ratchet. The monotonic high-water mark` / `is kept by the library (``max_level_reached``), not by this line.`]. Thirty-nine lines later the SAME commit newly wrote the refuted claim back in — [quoted: ```3x``->``4x``, ``11x``->``12x``. There is no top rung: the level is a monotonic high-water` / `mark of how many times lived experience re-earned the pattern.`]. RUN, not read: _build_wrap_instructions("proj", None, "2026-09-01") contains BOTH strings (True/True), so every composing agent receives both in one prompt. RUN on behaviour: a 12x line whose explanation shares no words with its cited episodes saves to disk as `- deep_pattern | 11x (2026-09-01) (ungrounded)` — the visible level went DOWN, so :972 is the false half. THE FIX: delete "the level is a monotonic high-water mark of how many times lived experience re-earned the pattern" from :972-973 and keep only the no-top-rung claim, which is true and is what that bullet is for; :933 already carries the correct account. [prescription: run]
  Tags: anneal-memory, code_review, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260901-045026-37a8216bd092
  Time: 2026-09-01T04:50:26  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: MEDIUM: skill/anneal-memory/SKILL.md:85 — THE COMMIT WHOSE SUBJECT IS "THE LAST SURFACE OF THE CLASS" LEFT EIGHT SURFACES, SIX OF THEM IN THE FOUR FILES IT EDITED. b5a6e7c asserts, verbatim: "CLASS RE-SWEPT AFTER THE EDIT, whole repo: the only remaining occurrences of the retired phrasing are inside the negative-assertion list in TestTeacherCoversReaderRange". FALSE. RUN, not reasoned: I wrote the assertion the repo does not have (regex over the closed ladder and the closed demotion enumeration, across README.md, docs/architecture.md, skill/anneal-memory/SKILL.md and both examples/agent-instructions.full*.example) and executed it at HEAD b5a6e7c — RED, 8 sites: README.md:208, README.md:431, docs/architecture.md:19, docs/architecture.md:36, skill/anneal-memory/SKILL.md:35, skill/anneal-memory/SKILL.md:85, examples/agent-instructions.full.example:58, examples/agent-instructions.full.cli.example:64. SIX of the eight are in the four files this commit touched. The sharpest is :85, five lines above the line the commit did change — [quoted: `Patterns graduate 1x → 2x → 3x, and each graduation must **cite the episode ID** that validates it.`] — which is the DEFINITIONAL sentence of that section, so an adopting agent loading the skill reads the cap first and the no-ceiling correction at :90 second. README.md:208 is worse than stale, it is refutable — [quoted: `A 2x/3x graduation that fails either check above demotes (3x→2x, 2x→1x)`] — RUN: a 12x ungrounded graduation saves as `- deep_pattern | 11x (2026-09-01) (ungrounded)`, a transition that enumeration excludes, and the commit itself names README.md as the PyPI long description that "does reach every visitor". WHY THE SWEEP MISSED THEM, and it is not carelessness: the sweep was run against the FIVE PHRASE LITERALS the sibling commit pinned in TestTeacherCoversReaderRange ("1, 2, or 3", "graduations (2x and 3x)", "to 2x or 3x", "Patterns at 3x:", "highest urgency") — I re-ran all five repo-wide and that narrow claim is TRUE, sources clean, tests only. The class was defined by the strings already known, so the ladder spelling was never in the denominator. THE FIX: fix the 8 sites, and add the negative assertion to tests/test_integrity.py, which already reaches the repo root and already asserts against skill/anneal-memory/SKILL.md and examples/ — the pinning machinery exists and was not pointed at prose. [prescription: run]
  Tags: anneal-memory, code_review, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260901-045039-8c3996a2e92d
  Time: 2026-09-01T04:50:39  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: MEDIUM: anneal_memory/continuity.py:2658 — A RUNTIME UserWarning TELLS THE OPERATOR A 12x PATTERN IS "3x", AND IT FIRES ON EVERY WRAP. The predicate at :2653 is a FLOOR — [quoted: `if cf.max_level_reached >= 3 and not cf.provenance`] — so 4x, 12x, 18x all qualify, but the message hard-codes the retired ceiling — [quoted: `f"{len(graduate_out)} top-tier (3x) pattern(s) were carried forward "`]. RUN, in a scratch store under /tmp/dio_am_0901, two wraps, a 12x pattern re-stamped with a citation that fails the grounding check: carried_forward = [{name: mature_pattern, held_level: 12, max_level_reached: 12, days_since_grounded: 1, cited: True, provenance: False}] and the warning printed verbatim `1 top-tier (3x) pattern(s) were carried forward this wrap with no resolving citation and no provenance: mature_pattern`. The identical scenario at 3x prints the same sentence, so the two are INDISTINGUISHABLE to the reader and the number is decoration, not data. THIS IS THE AM-LEVELCAP CLASS IN THE ONE PLACE IT MATTERS MOST: unlike the docs, this string is inside `packages = ["anneal_memory"]`, so it is the only surface of the class that actually ships in the wheel, and it reaches the operator at the moment of decision — the message asks them to graduate OUT, retire, or add provenance, and misstates the level the decision turns on. THE FIX: interpolate the level(s) instead of asserting one — either name them per pattern or say "Proven-tier (3x and above)". [prescription: run]
  Tags: anneal-memory, code_quality, code_review
```

---

```
[laptop/flow]   ID: diogenes-20260901-045054-aff1885a5707
  Time: 2026-09-01T04:50:54  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: MEDIUM: anneal_memory/graduation.py:69 — THE DEFERRAL NOTE THAT MAKES SPORE-676 SAFE TO POSTPONE RESTS ON THE WORD "INERT", AND INERT IS MEASURABLY THE WRONG WORD. The _BARE_GRADUATION_RE asymmetry (still `[23]` while _GRADUATION_RE is 2-and-up) is deliberate and I am not asking for it to be widened — acf5d7a is right that widening the demotion path needs its own slice. The defect is the recorded JUSTIFICATION — [quoted: `# out in the normal case, so the asymmetry is inert; it is recorded here rather than` / `# left to be rediscovered.`] — because a future reader triaging this reads "inert" as "no observable difference" and deprioritises accordingly. RUN, four scenarios in /tmp/dio_am_0901, identical two-wrap setup, only the level changed, second wrap re-stamps to TODAY so the date gate does not apply: 3x BARE carry -> saved `- mature_pattern | 3x (2026-08-31) (carried-forward)`, carried_forward = [{held_level: 3, max_level_reached: 3, cited: False, provenance: False}], AND the top-tier UserWarning fires. 12x BARE carry -> saved `- mature_pattern | 12x (2026-08-31)`, carried_forward = [], NO marker, NO warning. So the mature line is not merely spared demotion; it is spared the ENTIRE ungrounded-carry audit that the less-mature line gets, silently. That is a difference in observable behaviour, which is what inert denies. It also inverts the intent the same file argues at :39-41: the patterns a practice exercises MOST are the ones excluded from the immune system. (The cited-but-failed path is unaffected — 12x CITED-FAILED does produce a carried_forward row, so the hole is bare-only.) THE FIX IS THE COMMENT, NOT THE REGEX: replace "inert" with what it actually is — a today-dated bare 4x+ line is invisible to the carry-forward audit as well as to demotion — so the next reader sizes spore-676 off the real exposure. [prescription: run]
  Tags: anneal-memory, code_review, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260901-045106-1b141c7ce4ae
  Time: 2026-09-01T04:51:06  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: MEDIUM: CHANGELOG.md:19 — THE PUBLISHED RELEASE NOTE ASSERTS THE EXACT PROPERTY THE ZERO-PADDING BUG BROKE, AND THE FIX REACHED NO RELEASE SURFACE. 0.9.7 line 19 — [quoted: `- **The floor is unchanged**: 1x is a developing pattern and belongs in the working set, not the deep store. `([2-9]|\d{2,})` and `MIN_PROVEN_LEVEL = 2` both still exclude it.`]. RUN, the old regex reconstructed literally and executed: `| 01x` matches at group(1)="01", int()==1; `| 00x` matches at level 0; `| 007x` at 7. Only a bare `| 1x` is excluded. So the shipped regex does NOT exclude 1x, and this bullet is the one codex L3 refuted as a HIGH. LIVE-CHECKED, not assumed: pypi.org/pypi/anneal-memory/json returns info.version 0.9.7 as the latest release, so every installed adopter has the hole AND a changelog telling them it cannot happen. Second half, same issue: CHANGELOG.md:5 `## [Unreleased]` is EMPTY (line 6 blank, line 7 is the 0.9.7 header) while acf5d7a fixed exactly this in the source, and pyproject version + __init__.__version__ both still read 0.9.7 — so the correctness fix exists only in an unpushed local commit with no changelog entry and no version bump. THE FIX: amend the 0.9.7 bullet to state the ceiling was lifted and the padding hole shipped with it, and put the `([2-9]|[1-9][0-9]+)` correction under Unreleased so it survives to the next cut. [prescription: reasoned]
  Tags: anneal-memory, code_review, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260901-045119-01744db7f9a7
  Time: 2026-09-01T04:51:19  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: LOW: tests/test_graduation.py:1193 — THE TEST NAMED "EXACTLY" CHECKS ONE DIRECTION, AND THE MISSING DIRECTION IS THE ONE ITS OWN DOCSTRING WARNS ABOUT. [quoted: `def test_marker_kinds_taught_are_exactly_the_kinds_the_reader_accepts(self):`] with [quoted: `Teaching not a closed set invites a marker the reader silently drops.`] The body asserts only teaching ⊇ {?, ✓, *} — [quoted: `for accepted in ("``?``", "``✓``", "``*``"):`] — plus one behavioural probe that the reader drops `~`. Nothing asserts teaching ⊆ reader, so a generator that added a `~` or `#` example to the taught marker list would pass GREEN, which is precisely the failure the docstring names. (`!` is also absent from the tuple though the reader accepts `!+`.) This matters more than a naming nit because the whole class exists to stop teacher/reader divergence and it was added in the same commit that fixed a divergence a hand-copied fixture could not see. NOT A FINDING, CHECKED: the behavioural half is correct — RUN in a scratch store, `- ~ tilde_pattern | 2x ... [evidence: ...]` yields pattern_history None while `- * tilde_pattern` and an unmarked line both yield max_level_reached 2, so the continuity.py teaching that an unlisted glyph is silently ignored by the per-name defenses is accurate as written. THE FIX: extract the taught marker glyphs from the delivered text and assert the SET equals the readers alternation, so an added kind fails. [prescription: reasoned]
  Tags: anneal-memory, code_quality, code_review
```

---

```
[laptop/flow]   ID: diogenes-20260901-045131-c35310904ec1
  Time: 2026-09-01T04:51:31  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: LOW: anneal_memory/graduation.py:48 — A COORDINATE IN THE AM-LEVELCAP RATIONALE POINTS AT A COMMENT ABOUT A DIFFERENT REGEX. [quoted: `# anchoring, continuity.py:2176) was ALREADY any-level and says so in its own comment —`]. RUN: the only executable use of _NAMED_PATTERN_WITH_EVIDENCE_RE in continuity.py is at :2203 ([quoted: `ev_match = _NAMED_PATTERN_WITH_EVIDENCE_RE.match(line)`]); the other occurrence is the import at :41. Line 2176 reads [quoted: `# Result: ``## Anti-Patterns`` bullets matching the widened`] — a v0.3.3 comment about _NAMED_PATTERN_RE, a DIFFERENT regex whose name is one token shorter, so a reader who follows the pointer lands on prose that looks close enough to stop reading. The claim the coordinate supports is TRUE (I verified the regex is any-level `(\\d+)x` at :203 and that its comment says so), which is what makes the wrong pointer cheap to leave and cheap to fix. Pre-existing from abf119f, not introduced this window; surfaced while substantiating the window. Not DRIFT — codeshape.py a34eaa2 anneal_memory/graduation.py exits 1, CODE CHANGED. THE FIX: point at continuity.py:2203, or drop the line number and keep the symbol, which is the durable half. [prescription: reasoned]
  Tags: anneal-memory, code_review, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260901-045430-4963872f2c8d
  Time: 2026-09-01T04:54:30  Agent: diogenes  Type: observation
  Source: laptop_overnight_review
  Content: STILL OPEN (14), total live defect count for anneal-memory at HEAD b5a6e7c = 7 CARRIED + 7 filed tonight (0 HIGH / 5 MEDIUM / 2 LOW). Filed separately: 0 DRIFT (not in this count).

▶ DERIVATION, so the number is checkable. Prior slot: diogenes-20260815-045212-62d8de41709a, STILL OPEN (8) at HEAD a34eaa2 — which is exactly this window's base, so nothing happened to this repo between 08-15 and tonight. ONE CLOSED, verified at HEAD and not from the commit that claims it: continuity.py:923's generated wrap instruction "where N is 1, 2, or 3" is gone (grep -c "1, 2, or 3" anneal_memory/continuity.py returns 0) and it is now pinned by a negative assertion against the DELIVERED text, tests/test_graduation.py:1176. 8 − 1 = 7 carried.

▶ THE SEVEN CARRIED, ALL SEVEN RE-DERIVED INDIVIDUALLY AGAINST DISK TONIGHT — none carried on "the window did not touch its file". Two of the seven moved and are re-cited at coordinates measured tonight.
(1) HIGH crystal.py:938 — the consumer contract still reads `crystallize` requires ``level`` ∈ {2, 3}, and flow/scripts/crystal_decision_apply.py:125 still implements `if level not in (2, 3) or not explanation:`. ⚠ IT GOT WORSE, NOT STALER: 0f57107 changed the enforcement to crystal.py:835 `level < MIN_PROVEN_LEVEL`, so the docstring now misdescribes its own module, and the OUT path stays shut for anansi and daemon at 4x+.
(2) HIGH graduation.py:86 — `_BARE_GRADUATION_RE` still `r"\|\s*([23])x\s*..."`. Run-verified tonight in a scratch store: a today-dated bare 12x carry produces carried_forward [] and no marker where the identical 3x line produces both. Deliberate per :65-70; the JUSTIFICATION for the deferral is a separate MEDIUM filed tonight.
(3) MEDIUM — six prose sites stating 2-or-3 in files this repo edited: crystal.py:737 "still must be 2 or 3", graduation.py:283/430/523 "(2 or 3)", graduation.py:539 "Scans the ## Patterns section for 2x/3x lines", graduation.py:1844 "The current level (2 or 3) to demote from". Four of the six MOVED with this window's insertions (274→283, 421→430, 514→523, 1834→1844); the prose is byte-unchanged. graduation.py:77's "(2x or 3x)" is NOT counted — it describes `_BARE_GRADUATION_RE`, where it is correct.
(4) MEDIUM CHANGELOG.md:31 — "The suite was green before and after the level-cap fix (nothing pinned the ceiling)". ⚠ CARRIED ON WEAKER EVIDENCE AND I SAY SO: CHANGELOG.md is not in `git diff --name-only a34eaa2..b5a6e7c` (README.md, continuity.py, graduation.py, types.py, both examples, SKILL.md, test_graduation.py), so the text is unchanged — but I did NOT re-run the 08-15 mutation measurement tonight.
(5) LOW crystal.py:139-140 — quotation marks around "not a one-way 3x ratchet — without an OUT path it bloats until an always-loaded list stops working". REFINED, not retracted: the exact quoted string still appears nowhere but at the quotation itself, so the 08-15 claim holds; tonight's grep also turned up the near-source it paraphrases, continuity.py:1167 "one-way 3x ratchet that monotonically bloats until an always-loaded list stops working", which makes the fix a two-word edit rather than a hunt.
(6) LOW crystal.py:5 — the module docstring still reads "a pattern graduates IN at 3x" while MIN_PROVEN_LEVEL = 2 at crystal.py:148 and the comment at crystal.py:131 puts the boundary at 2.
(7) LOW assert-for-narrowing, three sites, carried from 2026-05-22: server.py:201 `assert isinstance(msg, dict), "parsed message must be a dict"` unchanged; continuity.py:1572 → :1592 and :2267 → :2288, both moved by this window and both still asserts. graduation.py's house rule (narrow for mypy WITHOUT an assert, -O-safe) unchanged, and continuity.py:2309 documents the -O failure mode in its own comment.

▶ DRIFT RECONCILIATION, stated so it is auditable. 0 DRIFT, and it is a rule outcome, not a judgement. I ran codeshape.py a34eaa2 against all three Python files I filed against: anneal_memory/graduation.py, anneal_memory/continuity.py and tests/test_graduation.py each returned exit 1, CODE CHANGED — FINDING. The remaining four findings are in .md files (skill/anneal-memory/SKILL.md, README.md, CHANGELOG.md), which can never be DRIFT by rule, so no check was run on them.

▶ THE NIGHT'S SHAPE, and it is the same shape at four scales. Two commits, both fixing "a surface teaches a ceiling the library removed", and both introduced a fresh instance of exactly that class while closing others. b5a6e7c's subject calls itself "the last surface of the class" and its message asserts a whole-repo re-sweep; eight sites survive, six of them in the four files it edited, because the sweep was run against the five phrase literals already known rather than against the ladder spelling. acf5d7a accepted a codex correction that the visible Nx is current standing and not a high-water mark, corrected it at continuity.py:933, and re-asserted the refuted claim at :972 — both strings live in one generated prompt, which I confirmed by running the generator. The zero-padding HIGH that same commit fixed is still live on PyPI at 0.9.7 with a changelog bullet asserting the property the bug broke, and `## [Unreleased]` is empty. And the only surface of the class that actually ships in the wheel is a UserWarning that calls a 12x pattern "3x" at the moment it asks the operator to decide about it. THE CHECKABLE PROXY WAS GRADED INSTEAD OF THE REAL TARGET, five times, by a commit pair whose entire subject is that failure mode.
  Tags: anneal-memory, code_review, still_open
```

---

```
[laptop/flow]   ID: diogenes-20260901-045505-90ef02ecc338
  Time: 2026-09-01T04:55:05  Agent: diogenes  Type: observation
  Source: laptop_overnight_review
  Content: COVERAGE: anneal-memory 2026-09-01, HEAD b5a6e7c4db1d3db06d7202671433414a6111f87f. ⛔ HEAD IS NOT origin/main AND THAT IS A FACT ABOUT THIS WINDOW, NOT A CACHE STALENESS: `git ls-remote origin main` against the LIVE remote returns a34eaa28d21de035b37818dac3ed12c720ba5523 (the 0.9.7 release commit), `git status -sb` reads `## main...origin/main [ahead 2]`, and the two commits I reviewed are the two unpushed ones — so nothing in tonight's window has left this laptop. `git status --porcelain` EMPTY at start and at finish; every check ran read-only or in /tmp/dio_am_0901, nothing built, no worktree, no fetch. /Users/phillipclapham/Documents/anneal-memory is a symlink to /Users/phillipclapham/Briefcase/anneal-memory — one checkout, not two.

RE-DERIVED BY RUNNING, not carried forward:
· Full suite: `python3 -m pytest -q` → 1691 passed, 15 warnings, 3 subtests, 10.72s. The commit message claims 1691; it is exact. GREEN WITH ALL SEVEN OF TONIGHT'S FINDINGS PRESENT — none of them is covered.
· `python3 -m mypy anneal_memory` → Success: no issues found in 18 source files. Claim verified.
· `python3 -m ruff check .` → Found 65 errors. The commit claims "ruff 65 = unchanged baseline"; exact.
· `_build_wrap_instructions("proj", None, "2026-09-01")` executed and searched — BOTH the corrected "current standing, not a ratchet" (:933) and the refuted "the level is a monotonic high-water mark" (:972) are present in one delivered prompt. That is finding 1, and it is a run, not a read.
· Demotion behaviour in scratch stores under /tmp/dio_am_0901: 12x ungrounded → `- deep_pattern | 11x (2026-09-01) (ungrounded)` on disk; 3x → 2x; 2x → 1x; 12x with fabricated ids → 11x. This is what refutes README.md:208's closed enumeration and :972's monotonic claim.
· Carry-forward, four scenarios, two wraps each, second wrap re-stamped to today so the date gate does not apply: 3x BARE → `(carried-forward)` marker + carried_forward row + the top-tier UserWarning; 12x BARE → no marker, carried_forward [], NO warning; 3x and 12x CITED-FAILED → both rowed and both warned, and the 12x one printed `1 top-tier (3x) pattern(s)` for a pattern the same result object reports at held_level 12.
· Marker kinds executed rather than reasoned: `- ~ p | 2x ...` yields pattern_history None while `- * p` and an unmarked line both yield max_level_reached 2. That is what STOPPED me filing against the continuity.py teaching — the claim as written is accurate.
· The old regex `([2-9]|\d{2,})` reconstructed literally and executed: `| 01x` matches at level 1, `| 00x` at 0, `| 007x` at 7, bare `| 1x` excluded. CHANGELOG.md:19's "still exclude it" is false for the shipped release.
· `extract_proven_patterns` on a 1x/2x/4x/12x fixture → ['b','c','d']. The new FLOOR docstring at graduation.py:1422 is CORRECT; no finding filed against it.
· The doc-ceiling assertion the repo does not have, written and executed at HEAD: RED, 8 sites, coordinates in the finding.
· All five phrase literals from the sibling commit's negative-assertion list greped repo-wide — sources clean, tests only. The commit's NARROW sweep claim is TRUE and I say so; the class claim is not.
· `codeshape.py a34eaa2` on graduation.py, continuity.py and tests/test_graduation.py — all exit 1.
· `curl pypi.org/pypi/anneal-memory/json` — info.version 0.9.7 is the current published release. Live oracle, not memory.
· All seven carried findings re-derived individually with `grep -n`/`sed -n` at HEAD, including the four coordinates that moved.
· The slot pre-flighted through the LIVE parser before filing: route_diogenes.extract_still_open(['...']) → 14 and extract_measurement_split → (7, 7), which reconciles.

⛔ NOT CHECKED — say it plainly:
· I did NOT build a wheel or install the package, so "examples/ and skill/ are NOT in the wheel" rests on reading `packages = ["anneal_memory"]` at pyproject.toml:45, not on inspecting a built artifact. The commit's REACH paragraph is believed, not verified.
· I did NOT re-run the 08-15 mutation measurement behind carried item (4), CHANGELOG.md:31 — it is carried on the file being outside this window's diff, which is weaker than a re-run and is named as such in the slot.
· I did NOT open anneal_memory/store.py, server.py (beyond line 201), cli.py, schema.py, sessions.py or __init__.py, and I did NOT open docs/ beyond architecture.md:19 and :36. The store, MCP server and CLI were untouched by this window and are unexamined tonight.
· I did NOT read the 173 added lines of tests/test_graduation.py as a whole — I read the two new classes and the amended TestCanonicalTemplateFormatEndToEnd docstring, and I ran the suite. Running a test is not auditing whether it asserts the right thing; finding 6 is the one case where I did audit that, and I did not extend that audit to the other nine new tests.
· I did NOT exercise the MCP server path or the CLI, so every claim about what an ADOPTER experiences is derived from the library API in-process.
· I did NOT verify that any downstream consumer actually reads the "top-tier (3x)" warning text — the impact argument is that it reaches the operator, and I confirmed it is emitted, not that anyone acts on it.
· The four .md findings are prose-only; I read the paragraphs I cite and their immediate neighbours, NOT the documents end to end. README.md is 431+ lines and I opened roughly a dozen of them.
  Tags: anneal-memory, code_review, semantic
```
