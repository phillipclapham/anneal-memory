# DIOGENES OVERNIGHT — anneal-memory, 2026-09-03

**Source selector:** `episodic.py read --since 24h --tags anneal-memory  (filtered to Agent: diogenes at 2026-09-03*)`
**Stated count: 10 episode(s).** Severity: LOW 1 · MEDIUM 5 — 6 of 10 episode(s) carry a severity; the other 4 are COVERAGE 1 · COVERAGE-OPEN 1 · SELF 1 · STILL OPEN 1
**STILL OPEN: 20** — **6 filed tonight** (measured at HEAD this window) · **14 carried**. A carried item was NOT necessarily re-checked this window; only the slot block below says which ones were.

> ⚠ **UNTRIAGED AND VERBATIM.** Generated by `scripts/route_diogenes.py` from the episodic store without judgement. Nothing here has been checked against disk except where a line says so explicitly. Diogenes' own `[prescription: ...]` tags are his assessment, not a ruling.

---

```
[laptop/flow]   ID: diogenes-20260903-040002-b84cb7bd4577
  Time: 2026-09-03T04:00:02  Agent: diogenes  Type: observation
  Source: laptop_overnight_review
  Content: COVERAGE-OPEN: anneal-memory 2026-09-03, HEAD 3a763f2d9a690d0de90e1526502cca54b1960b9b (== origin/main? YES — verified against the LIVE remote, `git ls-remote origin HEAD` answered 3a763f2d9a690d0de90e1526502cca54b1960b9b; last night HEAD was 2 AHEAD of origin and those two have now shipped). `git status --porcelain` EMPTY at start. Window b5a6e7c..HEAD = 4 commits (a067a7d, c0c587f, 4b2045a, 3a763f2), which closes EXACTLY on the HEAD my 2026-09-01 review opened at (b5a6e7c4db1d3db06d7202671433414a6111f87f) — no gap, and the dispatch's 24h count of 4 matches the un-reviewed window for once. Prior slot to reconcile against: diogenes-20260901-045430-4963872f2c8d, STILL OPEN (14) at b5a6e7c = 7 carried + 7 filed. `~/Documents/anneal-memory` is a SYMLINK to `~/Briefcase/anneal-memory` — checked first; one checkout, not two. REVIEW STARTED, NOT FINISHED — if no COVERAGE line for anneal-memory exists in this run, THIS RUN DIED and the finding list is PARTIAL. [prescription: none]
  Tags: anneal-memory, code_review, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260903-040907-a6948e3a15fa
  Time: 2026-09-03T04:09:07  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: MEDIUM: anneal_memory/store.py:1952 — THE READ IS NOT IN THE CLEARING TRANSACTION, SO THE TOCTOU IS NARROWED AND NOT REMOVED, AND THE SIBLING METHOD FORTY LINES UP SAYS SO CORRECTLY. The 0.9.8 headline fix asserts [quoted: `Reading and clearing in one transaction removes the window instead of narrowing it. Found by codex + glm at L3.`] and the receipt class repeats it at :479 [quoted: `Read inside the clearing transaction, so it cannot disagree with what was cleared`], as does server.py:590, cli.py:1185 and CHANGELOG.md:36. It is false. `wrap_cancelled` reads via three `_get_metadata` SELECTs at store.py:1973-1975 and only then executes the first INSERT at :1977; the store runs the sqlite3 default `isolation_level=""`, where BEGIN fires before DML and not before SELECT — which THIS REPO ALREADY DOCUMENTS, in `_batch` at store.py:3959 [quoted: `BEGIN`` only fires before DML, not before SELECT`]. RUN, twice, not reasoned. (a) Probe inside the real method: at the first read `A._conn.in_transaction` is False, and still False immediately before the first INSERT. (b) The race driven to completion in /tmp/dio_am_0903/race.py — session A enters wrap_cancelled holding tok-OLD, a peer B on its own connection cancels and starts tok-NEW-LIVE-PEER between As read and As clear, and A returns `WrapCancelReceipt(token=tok-OLD, ..., partial_state=False)` while the store is left empty: the live peers wrap was destroyed and the operator was told a corpse was cleared. That is verbatim the outcome the docstring says is gone. NOTHING EXCLUDES THE COMPETING WRITER: `continuity_lock` guards the continuity FILE only and its own docstring says it does NOT span the wrap lifecycle, and none of the three `wrap_cancelled()` call sites (server.py:606, cli.py:1192, continuity.py:1388) takes it. ⚖ WHY MEDIUM AND NOT HIGH, STATED SO IT CAN BE ARGUED: the window really did shrink from an MCP round trip to three metadata SELECTs, so the fix delivered most of its value; what is wrong is the categorical claim, and a categorical claim is what stops the last statement being written. ⭐ THE CONTRAST IS THE FINDING: `wrap_started` at store.py:1855-1867 performs the IDENTICAL read-then-write, cross-references this method BY NAME at :1870, and states the limit exactly — [quoted: `there is no BEGIN IMMEDIATE and the SELECT takes no write lock, so a hypothetical competing connection WITHOUT the external flock could still race the check`]. The honest paragraph already exists in this file; the newer method dropped it. THE FIX: open the block with `BEGIN IMMEDIATE` so the read takes the write lock, then make the claim true — or delete the word removes and say narrowed, which is what shipped. [prescription: none]
  Tags: anneal-memory, code_quality, code_review
```

---

```
[laptop/flow]   ID: diogenes-20260903-040928-04ff666918c4
  Time: 2026-09-03T04:09:28  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: MEDIUM: pyproject.toml:7 — THE v0.9.8 TAG POINTS AT HEAD AND HEAD IS NOT THE 0.9.8 ON PyPI; THREE COMMITS OF FIXES, INCLUDING TWO THIS RELEASE WROTE ABOUT ITSELF, NEVER SHIPPED. Proven from the ARTIFACT, not from the tag or the commit messages. PyPI upload_time for both 0.9.8 files is 2026-09-02T17:28:12Z = 13:28 EDT. Commit times: a067a7d 13:20:38, c0c587f 13:33:22, 4b2045a 13:41:45, 3a763f2 14:09:36. The build is a067a7d; the three later commits are post-upload, and `git tag --points-at HEAD` answers v0.9.8. Downloaded both files and read them: the published sdist carries skill/anneal-memory/SKILL.md:123 [quoted: `| Recover a stuck wrap | ``anneal-memory wrap-status`` · ``wrap-cancel`` | — (CLI only) |`] — the exact false row 4b2045a exists to kill, and that commits own message says [quoted: `That was true until 0.9.8 and false the moment it shipped.`] It is STILL false in what shipped. The published cli.py:1185 still carries the TOCTOU pre-read [quoted: `# Capture the token BEFORE cancellation so we can report what`] that 3a763f2 removed, so the CLI parity fix is not in the release either. The published integrity.py carries the SHOUTING wrap_cancel description [quoted: `WHAT THIS DESTROYS, AND WHY YOU SHOULD CHECK FIRST: the wrap may `] that 3a763f2 reworded. ⛔ AND THE INTEGRITY MECHANISM IS WHERE IT BITES. tool-integrity.json exists for host-side description verification (README:451). Both trees are internally self-consistent — repo verify_integrity(tool-integrity.json) is (True, []), published-vs-published is (True, []) — but the published manifest hash for wrap_cancel is 220261f9af00e0eb... against the repo 0.9.8 hash e6e2b31933928669..., and running the repo verifier across the two returns, verbatim, (False, ["Tool wrap_cancel description hash mismatch (possible tampering)"]). An operator who takes the reference hash from the tagged 0.9.8 and checks an installed 0.9.8 gets a TAMPERING alarm on a clean install. `## [Unreleased]` at CHANGELOG.md:5 is EMPTY, so none of the three post-upload commits is disclosed anywhere. ⚙ CLOSED SEPARATELY BY THIS CHECK: my 09-01 COVERAGE line listed as NOT CHECKED that skill/ and examples/ are absent from the wheel because I had only read pyproject. Confirmed from the built wheel tonight: 25 files, `anneal_memory/` only, no skill/ — so the stale SKILL.md ships in the SDIST and the GitHub tree, not in the wheel. THE FIX: this is a record/release question, not a code one — either re-cut 0.9.8.post1 from HEAD, or move the tag off HEAD and list the three commits under [Unreleased]. [prescription: none]
  Tags: anneal-memory, code_review, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260903-040945-948a61ed5ba6
  Time: 2026-09-03T04:09:45  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: MEDIUM: tests/test_server.py:194 — THE TEST CALLED "THE RACE MADE CONCRETE" CONTAINS NO CONCURRENCY, AND THE PROPERTY ITS FAILURE MESSAGE DECLARES IMPOSSIBLE IS THE ONE I REPRODUCED TONIGHT. `test_report_cannot_name_a_wrap_it_did_not_clear` is documented [quoted: `The race made concrete. A peer replaces the wrap between the moment the caller decided to cancel and the clear itself`] and asserts [quoted: `assert stale_token not in text, (`] with the message [quoted: `"reported a token from a wrap that had already completed — the "`]. But the body does the peer replacement SEQUENTIALLY, before the handler is ever entered: `store.wrap_cancelled()` then `store.wrap_started(token="b" * 32, ...)` then `_handle_tools_call`. Nothing interleaves, so what it actually proves is the weaker property that the handler reports CURRENT state — real, and enough to kill a pre-read mutant, but it is the checkable proxy and not the target. The target is what my /tmp/dio_am_0903/race.py does: inject the peers wrap_cancelled + wrap_started BETWEEN the callers read and the callers clear, which is the only place the window exists. Under that interleaving the receipt returns tok-OLD after the peer has installed tok-NEW-LIVE-PEER — precisely `stale_token in text`, the state this assertion message calls the operator being told a corpse was cleared. So the guard passes at HEAD while the defect it names is live at HEAD. ⚠ IT IS NOT A USELESS TEST AND I AM NOT ASKING FOR IT TO BE DELETED — the pre-read mutant dies on it. What is wrong is the NAME and the DOCSTRING, which are the two things a future reader consults to decide whether this class is covered, and both say it is. Same shape as the sibling class this release exists to fix: a description that disagrees with correct code, fired at the test layer. THE FIX: rename to what it checks (reports the current wrap, not an earlier one) and either add a genuinely interleaved test — patching `_get_metadata` to fire the peer write between read and clear, which is a dozen lines and I have a working one — or state in the docstring that the residual window is untested. [prescription: none]
  Tags: anneal-memory, code_quality, code_review
```

---

```
[laptop/flow]   ID: diogenes-20260903-041000-a47b173b2f28
  Time: 2026-09-03T04:10:00  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: MEDIUM: anneal_memory/store.py:2057 — THE RECEIPT AND THE AUDIT TRAIL COMPUTE THE SAME PREDICATE TWO WAYS AND DISAGREE ON EXACTLY THE RECOVERY CASE THE AUDIT MARKER EXISTS FOR. Two definitions of "is this partial state", eleven lines apart in one method. The audit at :2032 uses the RAW string [quoted: `                    cancelled_ids_raw`] inside `healthy = bool(cancelled_started_at and cancelled_token and cancelled_ids_raw)`; the receipt at :2057 uses the PARSED value [quoted: `            cancelled_started_at and cancelled_token and episode_ids is not None`]. They agree everywhere except when `wrap_episode_ids` holds unparseable JSON — where the raw string is truthy and the parse yields None. RUN-VERIFIED in /tmp/dio_am_0903/audit_div2.py on a real audited store: set wrap_episode_ids to `{not json`, call wrap_cancelled(), and the receipt reports `partial_state = True` while the audit event records, verbatim, {"wrap_started_at": ..., "wrap_token": "tok-CORRUPT"} with NO partial_state key. So over MCP the operator is told [quoted: `Wrap state was CORRUPT (partial wrap-in-progress metadata) `] and the tamper-evident record calls the same event a routine abandonment. The comment directly above the audit branch is what makes this a defect rather than a nit — :2028 [quoted: `                # Healthy cancels have all three keys set. Anything`] and the stated purpose [quoted: `                # reviewing the audit trail know this was recovery,`]. On the one case that IS recovery, the marker is absent. The audit half is unchanged 0.9.7 code; the receipt half is new, so the divergence was CREATED by this release, in the method it rewrote. ⚠ HONEST LIMIT ON THE IMPACT: an auditor can still infer corruption from the absence of `wrap_episode_ids`/`wrap_episode_count` in the payload, since a healthy cancel always logs them — so this is a missing explicit marker plus a shape that also matches valid-JSON-of-the-wrong-type, not an unreconstructable record. THE FIX: hoist the parse above the audit block and compute the predicate once, feeding both the payload and the receipt from it. [prescription: none]
  Tags: anneal-memory, code_quality, code_review
```

---

```
[laptop/flow]   ID: diogenes-20260903-041017-760ff124abd6
  Time: 2026-09-03T04:10:17  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: MEDIUM: tests/test_integrity.py:452 — THE README GOT THE GENERALISING GUARD AND SKILL.md — THE FILE THAT ACTUALLY ROTTED — GOT A GUARD HARDCODED TO ONE TOOL NAME. 4b2045a claims two assertions close the class. Mutation-tested both, and the pair does kill the original mutant, but not the way the commit describes. (a) The NEGATIVE guard `test_skill_does_not_claim_an_mcp_tool_is_CLI_ONLY` scans "CLI only" rows for tool names and its docstring claims [quoted: `        what generalises past the one row that happened to be noticed.`]. It cannot: SKILL.mds MCP column is where underscore tool names live, so a row whose MCP cell reads "CLI only" by construction does not name the tool. MUTATION RUN — reverted SKILL.md to the b5a6e7c version in /tmp/dio_am_0903/mut and ran the class: 1 failed, 4 passed, and the one that failed is NOT this test. It passes on the exact defect it was written for. (b) The kill comes entirely from the POSITIVE guard at :452 [quoted: `        assert "``wrap_cancel``" in text, (`] — a literal, single, hardcoded tool name. There is no SKILL.md counterpart to `test_readme_documents_every_tool`, which loops over TOOLS. MUTATION RUN — fresh archive of HEAD in /tmp/dio_am_0903/mut2, replaced `delete_episode` with `REMOVED_BY_DIOGENES` in SKILL.md, ran the FULL suite: 1722 passed. A non-spore MCP tool can vanish from the agent-facing depth doc and nothing fires. ⛔ THE ASYMMETRY IS THE POINT: README.md, which did NOT rot, now has an every-tool assertion; SKILL.md, which DID rot and is what the agent loads, has one name in a string literal. The next tool added reproduces Alex De Groodts condition — a tool the reader never learns exists — with a green suite. THE FIX: mirror `test_readme_documents_every_tool` over SKILL.md with the same spore_* exemption. Measured at HEAD before prescribing: exactly three tools are unnamed in SKILL.md and all three are spore_get / spore_touch / spore_update, so that exemption makes the loop green today. [prescription: none]
  Tags: anneal-memory, code_quality, code_review
```

---

```
[laptop/flow]   ID: diogenes-20260903-041029-129c63babdd3
  Time: 2026-09-03T04:10:29  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: LOW: anneal_memory/store.py:483 — THE RECEIPT DOCSTRING CONTRADICTS ITSELF INSIDE ONE SENTENCE ABOUT WHEN episode_ids IS None, AND THE WRONG HALF IS THE ONE A CALLER BRANCHES ON. [quoted: `    is ``None`` when there were none or the stored JSON was unreadable —`] followed immediately by [quoted: `    distinct from ``[]``, which means a wrap really did freeze an empty list.`] The two clauses cannot both be true: "there were none" and "a wrap really did freeze an empty list" are the same state. MEASURED, not reasoned — /tmp/dio_am_0903/empty.py calls `wrap_started(token=..., episode_ids=[])` and the metadata stores the string `[]`, so `wrap_cancelled()` returns `WrapCancelReceipt(token=tttt..., started_at=..., episode_ids=[], partial_state=False)`. episode_ids is `[]`, never None, for a wrap with no episodes. On an idle store the same script returns `episode_ids=None` with token None. So the true rule is: None means no wrap-episode-ids metadata at all OR unreadable JSON; `[]` means an empty frozen list. A caller who reads the first clause and writes `if receipt.episode_ids is None: # no episodes` treats the CORRUPT case as the benign one — which is the case `partial_state` was added to make loud. LOW because both consumers in-tree get it right by accident (server.py:604 and cli.py:1207 both do `len(...) if ... is not None else 0` and never branch on the meaning), so this is a hazard for the next reader rather than a live bug. THE FIX: delete "there were none or" — the sentence is correct without it. [prescription: none]
  Tags: anneal-memory, code_review, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260903-041052-f6a4fee1a43a
  Time: 2026-09-03T04:10:52  Agent: diogenes  Type: observation
  Source: laptop_overnight_review
  Content: SELF-CORRECTION — diogenes-20260903-041029-129c63babdd3 (tonights LOW on store.py:483) carries TWO WRONG SUPPORTING COORDINATES. I wrote [quoted: `server.py:604 and cli.py:1207`]. Derived at HEAD immediately after filing: `grep -n "len(receipt.episode_ids) if receipt.episode_ids is not None else 0"` answers anneal_memory/server.py:624 and anneal_memory/cli.py:1210. CORRECTED CLAIM: both in-tree consumers of `WrapCancelReceipt.episode_ids` — server.py:624 and cli.py:1210 — compute a count with an `is not None` fallback and never branch on the None-vs-[] MEANING, which is why the docstring defect is LOW and not MEDIUM. The diagnosis, the severity and the prescription are unchanged; only the two pointers were wrong. ⛔ MECHANISM, AND IT IS THE ONE I ALREADY HAVE A RULE FOR. My 08-30 close is that a `file:line` appears ONLY as a findings SUBJECT coordinate and every other reference is a QUOTE with no number. Every SUBJECT coordinate filed tonight was greped immediately before writing and every one is exact (store.py:1952, pyproject.toml:7, test_server.py:194, store.py:2057, test_integrity.py:452, store.py:483). These two were SUPPORTING references, and I typed them out of the diff I had read forty minutes earlier instead of quoting. Derived-vs-typed, eleventh night, and the failure is on the un-gated half again — same as the wisp slip on 09-02, same as the levain slot slip this morning. ▶ THE RULE IS NOT THE GAP AND RE-REMEMBERING IT HAS NOW FAILED THREE TIMES. Close, mechanical and applied to the rest of this run: any `file:line` I am about to write that is NOT the subject coordinate gets greped in the same command that writes the episode, or the number comes out and a quote goes in. [prescription: none]
  Tags: anneal-memory, code_review, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260903-041153-60518ff1ecc5
  Time: 2026-09-03T04:11:53  Agent: diogenes  Type: observation
  Source: laptop_overnight_review
  Content: STILL OPEN (20), TOTAL AT HEAD 3a763f2 — anneal-memory, 2026-09-03. 6 filed tonight (0 HIGH / 5 MEDIUM / 1 LOW), 14 carried. Filed separately: 0 DRIFT (not in this count) — and that is a RULE OUTCOME, not a judgement: I ran codeshape.py b5a6e7c against every Python file I filed against (anneal_memory/store.py, anneal_memory/server.py, anneal_memory/cli.py, tests/test_server.py, tests/test_integrity.py) and all five returned exit 1, CODE CHANGED = FINDING. The remaining subject is pyproject.toml, which is not Python and not markdown; its finding is about the release artifact, not a comment. ONE SELF-CORRECTION (diogenes-20260903-041052-f6a4fee1a43a — two wrong SUPPORTING coordinates in tonights LOW; no diagnosis changed).

ARITHMETIC: prior slot diogenes-20260901-045430-4963872f2c8d, STILL OPEN (14) at HEAD b5a6e7c, which is exactly this windows base. MINUS 0 closed, PLUS 6 new = 20.

CLOSED (0), AND IT IS DERIVED. The window is 4 commits touching 18 files; graduation.py and crystal.py — where 9 of the 14 live — are not among them, and every one of the 14 was re-cited at a coordinate I greped tonight. FIVE COORDINATES MOVED and are re-stated here: CHANGELOG.md:19 -> :78 and :31 -> :90 (the 0.9.8 section added 59 lines at the top), continuity.py:2653 -> :2654 and :2658 -> :2659 and :1592 -> :1593 and :2288 -> :2289 (one inserted docstring line), server.py:201 -> :202 (the handler-map entry).

THE 14 CARRIED, EACH RE-DERIVED INDIVIDUALLY AT HEAD TONIGHT, RE-CITED FROM THE ORIGINATING EPISODE AND NOT FROM THE PREVIOUS SLOTS RE-TYPED LIST (the close I wrote this morning after the levain README.md:182 slip):
(1) HIGH crystal.py:938 — the consumer contract still reads crystallize requires level in {2, 3}; flow/scripts/crystal_decision_apply.py:125 still implements `if level not in (2, 3) or not explanation:`.
(2) HIGH graduation.py:86 — _BARE_GRADUATION_RE still `r"\|\s*([23])x\s*..."`, the compile opening at :85.
(3) MEDIUM — the six 2-or-3 prose sites, all unmoved: crystal.py:737, graduation.py:283, :430, :523, :539, :1844.
(4) MEDIUM CHANGELOG.md:90 (was :31) — the suite was green before and after the level-cap fix. Still NOT re-run; carried on the file text being unchanged, which is weaker, and this is its SECOND carry on that basis, so under my own 09-02 close it may not be carried this way again.
(5) LOW crystal.py:139 — the quotation with no source.
(6) LOW crystal.py:5 — graduates IN at 3x against MIN_PROVEN_LEVEL = 2.
(7) LOW assert-for-narrowing, three sites: server.py:202, continuity.py:1593, continuity.py:2289.
(8) MEDIUM continuity.py:972 — the level is a monotonic high-water mark, refuted, still in the same generated prompt as the correction at :933.
(9) MEDIUM skill/anneal-memory/SKILL.md:85 — the terminating 1x -> 2x -> 3x ladder.
(10) MEDIUM continuity.py:2659 — the UserWarning calling an Nx pattern top-tier (3x), predicate at :2654.
(11) MEDIUM graduation.py:69 — the deferral resting on the word inert.
(12) MEDIUM CHANGELOG.md:78 (was :19) — the published note asserting the property the zero-padding bug broke.
(13) LOW tests/test_graduation.py:1193 — the EXACTLY test checking one direction.
(14) LOW graduation.py:48 — the coordinate pointing at a different regex.

⚠ A NEAR-MISS WORTH RECORDING RATHER THAN BURYING: my first grep for item (7) was `assert isinstance` and returned ZERO in continuity.py, which would have closed two sites. The asserts are `assert package is not None` and `assert cont_tmp is not None` — the finding was always about assert-for-narrowing, not about isinstance. Widened the grep and both are there. Same shape as the levain README grep I nearly mis-closed on 09-01: a too-narrow probe returning empty reads as remediation.

⚖ NOTE ON WHAT 20 MEASURES: nine of the fourteen carried are the AM-LEVELCAP prose tail, deferred and cheap. The six filed tonight are all in the 0.9.8 release surface and one of them (the tag-vs-PyPI divergence) is live for anyone installing today. [prescription: none]
  Tags: anneal-memory, code_review, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260903-041232-e888869cf163
  Time: 2026-09-03T04:12:32  Agent: diogenes  Type: observation
  Source: laptop_overnight_review
  Content: COVERAGE: anneal-memory 2026-09-03, HEAD 3a763f2d9a690d0de90e1526502cca54b1960b9b == origin/main verified against the LIVE remote (`git ls-remote origin HEAD` answered 3a763f2d..., not a cached ref). Window b5a6e7c..HEAD = 4 commits, 851 insertions / 45 deletions across 18 files, closing exactly on my 09-01 HEAD with no gap. `git status --porcelain` EMPTY at start and EMPTY at finish — the repo was not touched: everything read-only, or against `git archive HEAD` copies and scratch stores under /tmp/dio_am_0903. ~/Documents/anneal-memory is a symlink to ~/Briefcase/anneal-memory; one checkout, not two. Slot pre-flighted through the LIVE parser -> 20, split (6, 14), reconciles.

RE-DERIVED BY RUNNING, not carried forward from 09-01:
· Full suite `python3 -m pytest -q` -> 1722 passed, 15 warnings, 3 subtests, 11.74s. DENOMINATOR CHECKED: 1691 on 09-01, so this window added 31 tests. GREEN WITH ALL SIX OF TONIGHTS FINDINGS PRESENT — none is covered.
· `python3 -m mypy anneal_memory` -> Success: no issues found in 18 source files.
· `python3 -m ruff check .` -> Found 64 errors (65 on 09-01).
· THE RACE, DRIVEN TO COMPLETION, not reasoned about. /tmp/dio_am_0903/race.py: two Store objects on separate connections; peer B cancels and starts tok-NEW-LIVE-PEER between As third `_get_metadata` read and As first INSERT; A returns token=tok-OLD, partial_state=False, and the store is left empty. Plus the independent probe: `A._conn.in_transaction` is False at the first read AND immediately before the first INSERT, so the reads are demonstrably outside the clearing transaction with no injection at all.
· THE AUDIT/RECEIPT DIVERGENCE, executed on a real audited store (/tmp/dio_am_0903/audit_div2.py): wrap_episode_ids set to `{not json`, receipt partial_state=True, audit event payload {"wrap_started_at": ..., "wrap_token": "tok-CORRUPT"} with no partial_state key. Read the actual s.audit.jsonl, not a mock.
· THE EMPTY-LIST SEMANTICS (/tmp/dio_am_0903/empty.py): `wrap_started(episode_ids=[])` stores the string `[]`, receipt returns episode_ids=[]; an idle store returns episode_ids=None. That is what refutes the docstring at store.py:483.
· TWO MUTATIONS RUN AGAINST FRESH `git archive HEAD` TREES. (a) SKILL.md reverted to the b5a6e7c version -> TestDocumentedToolCount 1 failed / 4 passed, and the failure is the hardcoded positive guard, NOT the "CLI only" scan the commit leads with. (b) `delete_episode` erased from SKILL.md -> FULL SUITE 1722 passed. Both prescriptions in that finding are measured, not guessed.
· THE PUBLISHED ARTIFACT, downloaded from PyPI and opened. Both 0.9.8 files fetched; sdist expanded; read its SKILL.md:123, cli.py:1185 and integrity.py wrap_cancel description; listed the wheel (25 files, anneal_memory/ only, no skill/ and no examples/ — which CLOSES an explicit NOT-CHECKED from my 09-01 coverage line, now proven from a built artifact rather than from reading pyproject). PyPI upload_time_iso_8601 read from the live JSON API: 2026-09-02T17:28:12Z.
· `verify_integrity` run three ways: repo manifest vs repo descriptions (True, []), published manifest vs published descriptions (True, []), published manifest vs repo descriptions (False, ["Tool wrap_cancel description hash mismatch (possible tampering)"]).
· len(TOOLS) = 17 and the 7+2+8 decomposition checked by name; both tool-integrity.json copies hold 17 keys.
· `codeshape.py b5a6e7c` on all five Python subjects — all exit 1.
· All 14 carried findings re-derived individually with grep -n / sed -n at HEAD, including the six coordinates that moved.

⛔ NOT CHECKED — say it plainly:
· I did NOT install the wheel into a venv and did NOT start the MCP server or drive a real client. Every claim about what an MCP caller experiences is derived from `_handle_tools_call` in-process. No `anneal-memory` CLI subcommand was invoked as a subprocess either; the CLI findings come from reading cmd_wrap_cancel and from the tests.
· I did NOT verify the CHANGELOGs "22 mutants, 22 killed". I ran two mutations of my own choosing; theirs are unaudited.
· I did NOT re-run the 08-15 mutation measurement behind carried item (4), CHANGELOG.md:90 — SECOND consecutive carry on file-text-unchanged, which my own 09-02 close says is one carry too many. Named here so it binds next night.
· I did NOT read the 278 added lines of tests/test_server.py as a whole — I read TestToolWrapCancel, TestWrapCancelReceiptIsRaceFree and the head of TestWrapCancelPartialStateMessage, and audited ONE test in depth (test_report_cannot_name_a_wrap_it_did_not_clear). Running a suite is not auditing whether it asserts the right thing, and I did not extend that audit to the other ~20 new tests.
· I did NOT open anneal_memory/graduation.py, crystal.py, spores.py, retrieval.py, schema.py, types.py or migration.py beyond the greps that re-derived the carried coordinates — untouched by this window and unexamined tonight.
· I did NOT open docs/ beyond the single changed line of library-quickstart.md, and I read the CHANGELOG only to line 90.
· The race I reproduced required ME to schedule the peer write with a patch on `_get_metadata`. The WINDOW is proven independently by in_transaction=False; the probability of a real client hitting it in three metadata SELECTs is small and I did not attempt to measure it under natural scheduling. [prescription: none]
  Tags: anneal-memory, code_review, semantic
```
