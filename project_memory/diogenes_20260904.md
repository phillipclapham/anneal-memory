# DIOGENES OVERNIGHT — anneal-memory, 2026-09-04

**Source selector:** `episodic.py read --since 1d --tags anneal-memory  (filtered to Agent: diogenes at 2026-09-04*)`
**Stated count: 12 episode(s).** Severity: HIGH 1 · LOW 2 · MEDIUM 5 — 8 of 12 episode(s) carry a severity; the other 4 are COVERAGE 1 · COVERAGE-OPEN 1 · SELF 1 · STILL OPEN 1
**STILL OPEN: 12** — **8 filed tonight** (measured at HEAD this window) · **4 carried**. A carried item was NOT necessarily re-checked this window; only the slot block below says which ones were.

> ⚠ **UNTRIAGED AND VERBATIM.** Generated by `scripts/route_diogenes.py` from the episodic store without judgement. Nothing here has been checked against disk except where a line says so explicitly. Diogenes' own `[prescription: ...]` tags are his assessment, not a ruling.

---

```
[laptop/flow]   ID: diogenes-20260904-042041-99d42a420567
  Time: 2026-09-04T04:20:41  Agent: diogenes  Type: observation
  Source: laptop_overnight_review
  Content: COVERAGE-OPEN: anneal-memory 2026-09-04, HEAD 34728acdf49104ef443399caf401bc42de4ec4ad (== origin/main? YES — `git ls-remote origin HEAD` answered 34728acd against the LIVE remote, not a cached ref), `git status --porcelain` EMPTY, branch main, window 3a763f2..HEAD = 10 commits (closes EXACTLY on my 2026-09-03 review HEAD 3a763f2, so no un-reviewed gap). NOTE: ~/Documents/anneal-memory is a SYMLINK — `pwd -P` resolves to /Users/phillipclapham/Briefcase/anneal-memory; same repo, not two targets. Carried baseline to verify: 20 open at 3a763f2 (6 filed 09-03 + 14 carried). REVIEW STARTED, NOT FINISHED — if no COVERAGE line for anneal-memory exists in this run, THIS RUN DIED and the finding list is PARTIAL. [prescription: none]
  Tags: anneal-memory, code_review, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260904-043355-0ef5a13bfca1
  Time: 2026-09-04T04:33:55  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: HIGH: anneal_memory/store.py:2088 — THE AUDIT GUARD ADDED TONIGHT AS A codex L3 HIGH LANDED ON ONE OF FIVE SITES, AND THE ONE IT MISSED IS THE ONLY ONE THAT WEDGES THE STORE. `wrap_cancelled` gained a try/except around its post-commit audit write this window, with a comment stating the rule: "THE CANCELLATION IS ALREADY COMMITTED ABOVE. Letting an audit I/O failure (disk full, permissions, a failed rotation) propagate would raise out of this method with the metadata ALREADY cleared and no receipt returned". `wrap_started` — modified in this same window, forty lines above — still calls the audit bare, AFTER its own commit and OUTSIDE `_db_boundary`: [quoted: `            self._audit.log(`] followed by [quoted: `                "wrap_started",`]. `_audit_log` does not swallow either, so `wrap_completed`, `prune` and `continuity_saved` carry the same shape; filed ONCE, at the worst site.

RUN-VERIFIED end to end in /tmp/dio_am on a scratch store, patching only the audit sink to raise OSError(28, "No space left on device") — the exact class the fixed sibling's comment names:
  before: wrap_started_at = ''
  RAISED: OSError [Errno 28] No space left on device
  after:  wrap_started_at = '2026-09-04T08:21:54.756198Z'   wrap_token = 'tok-A'
  SECOND wrap_started -> WrapInProgressError

THREE THINGS ARE WRONG AT ONCE AND THE THIRD IS THE SEVERE ONE. (a) The wrap IS started and the caller is told it failed. (b) A raw `OSError` escapes a Store method — not a StoreError — breaking the caller-consistency primitive the comment eighteen lines above defends by name ("EVERY store failure surfaces through StoreError"; "An optimisation outside the error boundary is a hole in the boundary"). (c) The store is left WEDGED: the next `prepare_wrap` raises WrapInProgressError, and the caller cannot use the new proven cancel because it never received the token — so recovery requires an UNPROVEN `wrap_cancel`, which this same window built the CAS to discourage. That is Alex De Groodt's three-day lockout, reachable through the release that closed it, and the commit body names that lockout four times.

The policy is not new and the file states it: `_batch` swallows deferred audit-flush exceptions after a successful commit, "because propagating one would trick the caller's outer except clause into cleaning up tmp files that represent committed state — a data-loss path." Verified on disk: that flush is wrapped in try/except Exception: pass.

The fix: wrap the `wrap_started` audit call in the same try/except-warn-swallow pattern `wrap_cancelled` now uses, including the inner catch around `warnings.warn` for the `-W error` case. NOT EXECUTED. [prescription: reasoned]
  Tags: anneal-memory, code_quality, code_review
```

---

```
[laptop/flow]   ID: diogenes-20260904-043410-deeae08b4c60
  Time: 2026-09-04T04:34:10  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: MEDIUM: tests/test_integrity.py:712 — THE GUARD WRITTEN TONIGHT TO PIN SKILL.md TAKES max() OVER THE UNION OF ALL LADDER LINES, SO ONE CORRECTED LINE CERTIFIES EVERY UNCORRECTED ONE — AND THERE IS AN UNCORRECTED ONE. The assertion is [quoted: `        assert max(levels) > 3, (`], where `levels` is built as [quoted: `        levels = {`] / [quoted: `            int(m)`] / [quoted: `            for line in ladder_lines`] / [quoted: `            for m in re.findall(r"(\d+)x", line)`] — a set comprehension flattened across EVERY matched line, not per line.

RUN-VERIFIED against the real file. The collector matches two lines:
  line 35: levels=[1, 2, 3]      "Graduated patterns (1x→2x→3x) live in continuity's `## Patterns`, always loaded..."
  line 85: levels=[1, 2, 3, 4, 12]  "Patterns graduate 1x → 2x → 3x → 4x → … with **no top rung**..."
  UNION = [1, 2, 3, 4, 12], max = 12, assert max>3 -> True. PASSES.

So the FIRST ladder sentence an agent reads in the doc an AGENT LOADS still teaches the exact ceiling AM-LEVELCAP removed on 2026-08-14, fifty lines above the correction, and the test added the same day to stop precisely that cannot see it. The docstring's own mutation check — "restore `1x → 2x → 3x` as the taught ladder and this fails" — passes because it mutates the one line already fixed; it cannot distinguish "one line fixed" from "the file fixed", which is the property the test is named for.

The commit's own message calls this class out: "an EXACTLY test checked one direction... a naive scan fails on correct text, the same over-matching trap as the SKILL.md 'CLI only' guard." Same commit, new instance, in the guard rather than the guarded.

The fix: assert per ladder line rather than over the union, then correct the line it flags. RAN THE PREDICATE HALF against the real file — a per-line max flags exactly line 35 and nothing else, so it neither misses nor over-matches. Did NOT edit the test or SKILL.md. [prescription: reasoned]
  Tags: anneal-memory, code_quality, code_review
```

---

```
[laptop/flow]   ID: diogenes-20260904-043424-67bcb251cc29
  Time: 2026-09-04T04:34:24  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: MEDIUM: skill/anneal-memory/SKILL.md:85 — THE LADDER FIX IMPORTED A CLAIM THE LIBRARY'S OWN GENERATED INSTRUCTIONS EXPLICITLY REFUTE, INTO THE ONE DOC AN AGENT LOADS. The rewritten sentence reads [quoted: `the level is a monotonic high-water mark of how many times lived experience re-earned the pattern, so a mature pattern is re-stamped `12x`, never flattened back to `3x`.`]. The wrap instructions this library GENERATES say the opposite in as many words: "⚠ The level you WRITE is not immutable: an ungrounded re-stamp can be demoted, so the visible `Nx` is current standing, not a ratchet. The monotonic high-water mark is kept by the library (`max_level_reached`), not by this line."

RUN-VERIFIED that the library is right and SKILL.md is wrong. Scratch store, a today-dated 4x line with a fabricated citation id, through the real `validated_save_continuity`:
  result demoted: 1
  AFTER: - widgets_and_gears | 4x -> `- widgets_and_gears | 3x (2026-09-04) (ungrounded)`
The written level went DOWN. It is not monotonic; `max_level_reached` is.

THE SHAPE: this is the carried finding I have had open on this repo since 2026-09-01 — the same refuted framing standing next to its own correction inside one generated prompt — PROPAGATED into a second file by the commit that fixed that file's other defect. The correction the source carries did not travel with the claim, so SKILL.md now teaches, with no caveat, the thing continuity.py stops to warn about. The half that WAS fixed (no top rung) is right and worth keeping; only the "monotonic high-water mark" gloss is false of the written level.

Note this is why it matters more here than in a comment: an agent that believes the written Nx is a ratchet will re-stamp a demoted line back up, which is the sycophancy path the immune system exists to catch.

The fix: say what continuity.py says — the level climbs with no ceiling, the visible Nx is current standing, and the monotonic high-water mark is `max_level_reached`, held by the library. NOT EXECUTED. [prescription: reasoned]
  Tags: anneal-memory, code_review, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260904-043440-396444e9d69c
  Time: 2026-09-04T04:34:40  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: MEDIUM: scripts/hooks/pre-push:23 — THE HOOK'S "HERE IS WHY" BLOCK MATCHES NOTHING ON macOS, SO EVERY BLOCKED PUSH GETS A HARDCODED DIAGNOSIS THAT IS WRONG FOR HALF THE GATE. The extraction is [quoted: `    sed -n '/FAILED\|AssertionError\|Error/p' /tmp/anneal_prepush.$$ | head -8 >&2`]. BSD sed has no `\|` alternation in a basic regex — it matches a LITERAL pipe.

PROVEN, not inferred, on this machine:
  input:  "FAILED foo" / "E   AssertionError: bar" / "plain line"
  the hook's form            -> [matched 0 lines]
  a line containing the literal string `FAILED|AssertionError|Error` -> MATCHES (proving it is a literal, not alternation)
  `sed -nE '/FAILED|AssertionError|Error/p'` -> [matched 2 lines]

Then reproduced on the REAL path: a scratch clone stamped at 0.9.9 on a post-tag HEAD. The hook correctly blocked (exit 1) — and printed a blank gap where the reason goes, while the underlying pytest output contains both `E       AssertionError: HEAD is stamped '0.9.9'...` and `FAILED tests/test_integrity.py::TestReleaseStampIsNotAPublishedVersion::...`. The evidence was there; the extractor could not see it.

WHY THAT IS MORE THAN COSMETIC: the hook gates TWO unrelated classes — [quoted: `        -k "ReleaseStampIsNotAPublishedVersion or SourceCompilesCleanlyOnEveryTargetPython" \`] — and the message under the dead extraction is unconditional: "HEAD is stamped at an already-released version. Bump to the next .devN (pyproject.toml, anneal_memory/__init__.py, server.json x2)". An invalid escape sequence blocking the push is therefore reported as a version-stamp problem with four remediation steps that do nothing, and the real cause was suppressed by the one line meant to surface it. The hook's own header says it "Deliberately FAILS CLOSED... a guard that degrades to a pass when it cannot run is the exact shape of the defect it guards against" — it fails closed correctly and then misnames why.

The fix: `sed -nE '/FAILED|AssertionError|Error/p'`. EXECUTED against the same input: 2 lines, both the ones wanted. [prescription: run]
  Tags: anneal-memory, code_quality, code_review
```

---

```
[laptop/flow]   ID: diogenes-20260904-043456-7f13a034525d
  Time: 2026-09-04T04:34:56  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: MEDIUM: scripts/hooks/pre-push:17 — THE GATE READS THE WORKING TREE AND THE PUSH PUBLISHES COMMITS, SO AN UNCOMMITTED FIX LETS THE BAD COMMIT THROUGH THE ONE INSTRUMENT IN THE WINDOW. The gate is [quoted: `if ! "$PY" -m pytest tests/test_integrity.py -q \`], and the test it runs reads the stamp by importing the package — `from anneal_memory import __version__` — i.e. off disk. The hook reads NOTHING from stdin, where git supplies `<local ref> <local sha> <remote ref> <remote sha>`, so it never learns which commits are being pushed. Its own failure text then asserts a fact about a different object: "HEAD is stamped at an already-released version."

RUN-VERIFIED on a scratch clone at /tmp/dio_am/clone with the hook really installed via `bash scripts/install_hooks.sh` (core.hooksPath=scripts/hooks confirmed):
  1. BASELINE — commit a 0.9.9 stamp on a post-tag HEAD, working tree matching:
       HOOK EXIT=1, blocked with the bump instructions. The guard works for the case it was built for.
  2. THE HOLE — leave that commit as HEAD, edit the four stamp sites in the WORKING TREE ONLY to 0.9.10.dev0, commit nothing:
       working tree: __version__ = "0.9.10.dev0"
       HEAD (what a push publishes): __version__ = "0.9.9"
       HOOK EXIT=0
The bad commit goes public and the gate reports health, because it looked at a file nobody is pushing.

THIS IS THE REACHABLE ORDER, NOT AN EXOTIC ONE: you notice the stamp is wrong, you fix the four files, and you push before committing — which is the same sequence that produced the incident (`a90c2cb`, "I committed the exact defect spore-710 exists to prevent"). It also covers `git push origin <branch>` while checked out somewhere else. And by the hook's own reckoning it is the ONLY thing in this window: "CI fires after the push, which is after the damage. This closes that window."

The commit body says "Verified on the real path rather than simulated" — and it verified case 1. Case 2 is the one the verification could not see, the same shape as the L3 note this repo wrote into cli.py the same day: "the first test could not see it: it held the lock BEFORE opening, so the post-open race was never exercised."

The fix: read the refs from stdin and check the stamp in each pushed commit — e.g. `git show "$local_sha":anneal_memory/__init__.py` — rather than importing the working tree. NOT EXECUTED. [prescription: reasoned]
  Tags: anneal-memory, architecture, code_review
```

---

```
[laptop/flow]   ID: diogenes-20260904-043527-17bf12be404b
  Time: 2026-09-04T04:35:27  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: MEDIUM: README.md:208 — THE ELEVEN-SURFACE LEVELCAP SWEEP NEVER TOUCHED README.md, SO THE PUBLIC SURFACE STILL TEACHES THE CEILING AT THREE SITES. `git show --stat def07cf` lists CHANGELOG.md, continuity.py, crystal.py, graduation.py, SKILL.md, test_continuity.py, test_graduation.py, test_integrity.py. README.md is not among them, and it is what an adopter reads on GitHub and PyPI.

The three live sites, quoted from disk:
- "A 2x/3x graduation that fails either check above demotes (3x→2x, 2x→1x)" — both halves wrong: the scope excludes 4x+, and the enumeration has no 4x→3x rung.
- "│ decision     │── wrap ───→│ ## Patterns (1x→3x)  │" (the architecture diagram)
- "Patterns can only be promoted (1x -> 2x -> 3x) during compression"

RUN-VERIFIED that a 4x+ line really does validate and demote, so these are false rather than merely dated. Scratch store, today-dated `- widgets_and_gears | 4x (2026-09-04) [evidence: <fabricated id> ...]` through the real `validated_save_continuity`: demoted 1, and the line came back as `- widgets_and_gears | 3x (2026-09-04) (ungrounded)` — a transition the README's own parenthetical says does not exist.

WHY THIS IS THE SAME DEFECT AND NOT A LEFTOVER. This repo's own commit message names the class as CAUSATIVE, not cosmetic: "at least one consumer implements exactly that, so the cap the release removed was RE-DERIVED FROM THE DOCUMENTATION by every reader who trusted it", and "A WIDENING EMITS NO ERROR SIGNAL ANYWHERE DOWNSTREAM... the only symptom is absence." The consumer that was caught doing it was in-house and has since been fixed; README is the surface reached by consumers who are not.

⚠ The asymmetry is now inverted from the one the same commit corrected. On 2026-09-03 the complaint was that README got the generalising tool guard while SKILL.md got a hardcoded one; SKILL.md now has both a tool guard and a ladder guard, and README has neither ladder guard nor a ladder fix.

The fix: correct all three sentences and extend the ladder assertion over README.md as well as SKILL.md, so the two docs cannot diverge again. NOT EXECUTED. [prescription: reasoned]
  Tags: anneal-memory, code_review, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260904-043527-d4876879d6d5
  Time: 2026-09-04T04:35:27  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: LOW: anneal_memory/continuity.py:2693 — THE REWORDED WARNING NOW NAMES A TIER WIDER THAN ITS OWN GATE, AND CONTRADICTS THE COMMENT EIGHT LINES ABOVE IT. The operator-facing string is [quoted: `            f"{len(graduate_out)} Proven-tier pattern(s) were carried forward "`], while the predicate that fills it is [quoted: `            if cf.max_level_reached >= 3 and not cf.provenance`].

This repo established in THIS window that "Proven-tier" means 2x-and-up with no ceiling — types.py now reads "existing Proven (2x+) pattern names", graduation.py "Patterns at Proven-tier (2x+)", crystal.py "level >= MIN_PROVEN_LEVEL" where MIN_PROVEN_LEVEL = 2. The gate is >= 3. So the sentence enumerates a set strictly narrower than the category it names, and the comment directly above says so in the opposite direction: "Lower-tier carries (2x) are held silently — they are the normal domain-blind-erosion-fix case, not a graduate-out decision", under a heading that still reads "assisted ... surface for TOP-tier patterns held this wrap."

⭐ THIS IS A GENUINE IMPROVEMENT WITH A NEW EDGE, NOT A REGRESSION — I want that on the record. The prior text said "top-tier (3x) pattern(s)", which called a 12x pattern 3x on every wrap; that is my carried 2026-09-01 MEDIUM and it is CLOSED. What replaced it is imprecise rather than false: a 3x carry IS Proven-tier, so no statement here is untrue. The cost is that a reader acting on the message ("review these") is looking at a list that silently excludes part of the tier it names, and the exclusion is deliberate and unstated.

The blanket 2x/3x -> Proven-tier rename was right at every other site I checked; this is the one site where the threshold genuinely is 3 and not MIN_PROVEN_LEVEL, so the sweep's substitution does not hold.

The fix: name the actual gate — "pattern(s) at 3x or above" — or state the exclusion in the sentence. NOT EXECUTED. [prescription: reasoned]
  Tags: anneal-memory, code_review, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260904-043543-b4c40d67eb7b
  Time: 2026-09-04T04:35:43  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: LOW: anneal_memory/cli.py:275 — THE CONTENTION DISCRIMINATOR WAS COPIED INTO TWO MODULES IN ONE COMMIT, INSIDE A CHANGE SET WHOSE STATED PURPOSE IS CLOSING land-somewhere-not-everywhere. `_is_write_lock_contention` is defined at cli.py:275 and again in server.py, both new this window. Verified IDENTICAL, not eyeballed: parsing both with `ast`, stripping docstrings and hashing the dumped body gives `bb2f6004b0da510c` for each.

Both copies carry the same three-element result-code tuple — [quoted: `            "SQLITE_BUSY",`], [quoted: `            "SQLITE_BUSY_SNAPSHOT",`], [quoted: `            "SQLITE_BUSY_TIMEOUT",`] — and the same Python-3.10 text fallback [quoted: `    return "database is locked" in text or "database is busy" in text`]. Every one of those is a thing that changes: a fourth SQLITE_BUSY_* code, the 3.10 floor being dropped, or a different sqlite build's message wording. Two copies means the CLI and the MCP transport can start disagreeing about whether the same exception is contention, silently and in opposite directions — the CLI printing a traceback where the server prints the recovery guidance, or vice versa.

⚠ THE FILE ITSELF ARGUES AGAINST THIS, TWICE, IN THE SAME COMMIT. cli.py's `_print_contention` docstring: "One function so the open-time path and the command-time path cannot drift into saying different things about the same condition." And the boundary comment: "It sits HERE, not at the `wrap_cancelled()` call site, because the window is not specific to that command... Fixing the one site that was noticed is how the previous scoping got written. One boundary, all subcommands." The message got one home; the predicate that decides when to show it got two.

⚠ HONEST SCOPE: they agree today and nothing is currently wrong. This is debt with a named divergence mechanism, not a live defect, which is why it is LOW and not MEDIUM. It is also cheap now and gets dearer: `store.py` already exports `StoreDatabaseError`, so a single `is_write_lock_contention` beside it would be imported by both transports and testable once.

The fix: move it to `store.py` next to `StoreDatabaseError` and import it in both transports. NOT EXECUTED. [prescription: reasoned]
  Tags: anneal-memory, architecture, code_review
```

---

```
[laptop/flow]   ID: diogenes-20260904-043705-e78dac3cf9da
  Time: 2026-09-04T04:37:05  Agent: diogenes  Type: observation
  Source: laptop_overnight_review
  Content: STILL OPEN (12), TOTAL AT HEAD 34728ac — anneal-memory, 2026-09-04. 8 filed tonight (1 HIGH / 5 MEDIUM / 2 LOW), 4 carried. Filed separately: 0 DRIFT (not in this count) — and that is a RULE OUTCOME, not a judgement: I ran codeshape.py 3a763f2 against every Python file I filed against (anneal_memory/store.py, anneal_memory/continuity.py, anneal_memory/cli.py, anneal_memory/server.py, tests/test_integrity.py) and all five returned exit 1, CODE CHANGED = FINDING. The other two subjects are skill/anneal-memory/SKILL.md and README.md, which are markdown and can never be DRIFT by rule, so no check was run on them; scripts/hooks/pre-push is a NEW FILE this window and is shell, so its code changed by construction. ZERO SELF-CORRECTIONS.

ARITHMETIC: prior slot diogenes-20260903-041153-60518ff1ecc5, STILL OPEN (20) at HEAD 3a763f2, which is EXACTLY this window's base. MINUS 16 closed, PLUS 8 new = 12.

⭐⭐ SIXTEEN OF TWENTY CLOSED — the largest remediation this repo has had — AND EVERY ONE RE-DERIVED AT HEAD RATHER THAN READ OFF THE COMMIT MESSAGES THAT CLAIM THEM. The six 2-or-3 prose sites: one grep across crystal.py and graduation.py for all four spellings returns EMPTY. crystal.py's consumer contract, its module docstring and its unsourced quotation: all three rewritten, and the quotation now cites the real site. graduation.py's "inert" deferral: rewritten with the measured falsification. The CHANGELOG zero-padding note: corrected IN PLACE with an explicit dated correction block rather than a rewrite. The store's TOCTOU, its two-predicates-for-one-fact, and the receipt docstring's self-contradiction: closed by the BEGIN IMMEDIATE and the single in-transaction `partial_state`. tests/test_server.py's "the race made concrete": the docstring now states honestly what it does and does not prove and names the real interleaved test. The SKILL.md hardcoded tool guard: replaced by an every-tool loop. The EXACTLY test now round-trips every TAUGHT marker through the real reader, which is the direction it was named for and did not check.

TWO CLOSED BY MEASUREMENT I HAD DEFERRED, AND THAT IS THE POINT OF THE TWO-CARRY LIMIT:
· CHANGELOG's "the suite was green before and after the level-cap fix... both fixes are pinned by mutation-verified tests" had been carried TWICE on the file text being unchanged, which my own 2026-09-02 close forbids a third time. So I ran the mutation instead of carrying it: restored `([23])x` in `_GRADUATION_RE` on a scratch clone and the suite went RED at `TestZeroPaddedLevelsAreNotGraduations::test_well_formed_levels_still_match[12-12]`. The ceiling IS pinned. CLOSED, by the method the finding was made with.
· The release-stamp divergence — "the tag points at HEAD and HEAD is not what is on PyPI" — closed from the ARTIFACT, not the tag. Downloaded the published 0.9.9 sdist from PyPI and diffed it against `git archive v0.9.9`: __init__.py, store.py, server.py, cli.py, integrity.py, BOTH tool-integrity.json manifests, SKILL.md, pyproject.toml, server.json, CHANGELOG.md and README.md are all BYTE-IDENTICAL. HEAD is stamped 0.9.10.dev0, which is not a released version.

THE FOUR CARRIED, EACH RE-DERIVED INDIVIDUALLY AT HEAD AND RE-CITED FROM THE ORIGINATING EPISODE, NOT FROM THE PREVIOUS SLOT'S RE-TYPED LIST:
(1) HIGH graduation.py:96 — `_BARE_GRADUATION_RE` is still `r"\|\s*([23])x\s*..."`, the compile opening at :95 (was :86; it moved with this window's ten-line comment insertion). DELIBERATE per the rewritten deferral, and the deferral's reasoning is now correct where it was false; the asymmetry itself is unchanged and still open.
(2) MEDIUM continuity.py:978 (was :972) — "the level is a monotonic high-water mark of how many times lived experience re-earned the pattern", still standing 39 lines below its own refutation at :939-941 in the SAME generated prompt. Untouched this window: the only high-water line in `git diff 3a763f2..HEAD -- anneal_memory/continuity.py` is the 2x/3x -> 2x+ edit at :720. ⚠ AND IT GOT WORSE — the claim was PROPAGATED into SKILL.md by this window's ladder fix, filed tonight as its own MEDIUM.
(3) LOW assert-for-narrowing — TWO OF THREE CLOSED and I am carrying the remainder rather than the item: continuity.py's `assert package is not None` and `assert cont_tmp is not None` are now explicit typed raises, both -O-safe. server.py:239 `assert isinstance(msg, dict), "parsed message must be a dict"` is unchanged (was :202; it moved with the new import block and `_is_write_lock_contention`).
(4) LOW graduation.py:48 — the coordinate in the AM-LEVELCAP rationale still reads "(pattern_history anchoring, continuity.py:2176)". At HEAD, continuity.py:2176 is a comment about wrap_id and cross-session overlap; the actual `_NAMED_PATTERN_WITH_EVIDENCE_RE` call site in that file is at :2221. Still pointing at the wrong thing, and now at a different wrong thing than when it was filed.

⚖ NOTE ON WHAT 12 MEASURES, AND IT INVERTS THIS REPO'S LAST TWO NIGHTS. 09-01 and 09-03 were dominated by the AM-LEVELCAP prose tail — deferred, cheap, nine of fourteen. That tail is essentially gone. What is left is one runtime HIGH in the store, three guards that cannot see their own subject, and two documentation surfaces the sweep did not reach. The count fell by 8 and got heavier.
  Tags: anneal-memory, code_review, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260904-043752-a07e3a0839b5
  Time: 2026-09-04T04:37:52  Agent: diogenes  Type: observation
  Source: laptop_overnight_review
  Content: COVERAGE: anneal-memory 2026-09-04, HEAD 34728acdf49104ef443399caf401bc42de4ec4ad == origin/main verified against the LIVE remote (`git ls-remote origin HEAD` answered 34728acd, not a cached ref). Window 3a763f2..HEAD = 10 commits / 25 files / 3,073 insertions / 165 deletions, closing exactly on my 09-03 HEAD. `git status --porcelain` EMPTY at start AND at finish and HEAD unchanged — the repo was not touched: every probe was read-only or ran in /tmp/dio_am, including a full `git clone` of the repo into /tmp/dio_am/clone for the mutation and hook work. ⚠ ~/Documents/anneal-memory is a SYMLINK; `pwd -P` resolves to ~/Briefcase/anneal-memory. Checked first — eighth repo running.

RE-DERIVED BY RUNNING, not carried forward:
· FULL SUITE at HEAD: 1764 passed, 3 subtests, 15 warnings, 28.97s, exit 0 — denominator checked against 1722 on 09-03 and 1760 in this window's own def07cf.
· THE HIGH DRIVEN TO COMPLETION on a scratch store: patched only the audit sink to raise OSError(28), called `wrap_started`, and observed a raw OSError escape with `wrap_started_at` and `wrap_token` COMMITTED, then a second `wrap_started` refusing with WrapInProgressError. The wedge is measured, not argued.
· THE SKILL.md LADDER GATE run against the real file: its collector matches two lines, the union of their levels is [1,2,3,4,12], max 12 > 3, PASSES — while line 35 alone tops out at 3. Then ran the prescribed per-line variant: it flags line 35 and nothing else.
· THE MONOTONIC-HIGH-WATER CLAIM refuted by execution: a today-dated 4x line with a fabricated citation went through the real `validated_save_continuity` and came back `3x (ungrounded)`, demoted 1. The written level is not monotonic. That same run is what falsifies README's demotion enumeration.
· THE pre-push HOOK on a real clone with `install_hooks.sh` actually run (core.hooksPath=scripts/hooks confirmed): blocked correctly (exit 1) with the working tree matching a bad HEAD, and PASSED (exit 0) with the same bad HEAD and an uncommitted fix in the tree. Its sed extraction proven dead on BSD sed three ways (0 matches on real input; a literal-pipe line DOES match; `sed -nE` matches 2).
· THE MUTATION for the carried CHANGELOG item: `([2-9]|[1-9][0-9]+)` reverted to `([23])` in `_GRADUATION_RE` on the clone -> suite RED at `test_well_formed_levels_still_match[12-12]`.
· PyPI ARTIFACT vs the v0.9.9 TAG: sdist downloaded and diffed file by file against `git archive v0.9.9` — 12 of 12 byte-identical.
· BOTH integrity manifests: `verify_integrity` returns (True, []) for tool-integrity.json and anneal_memory/tool-integrity.json.
· `codeshape.py 3a763f2` run as the DRIFT discriminator on all eight touched Python modules: types.py/graduation.py/crystal.py exit 0 (code UNCHANGED, doc-only edits), the other five exit 1.
· AST-hashed both copies of `_is_write_lock_contention` — identical, not eyeballed.
· Slot pre-flighted through the LIVE `route_diogenes` parser -> '12', then re-parsed FROM THE STORE after writing -> '12', with all ten episodes bucketing 1 HIGH / 5 MEDIUM / 2 LOW / 1 slot / 1 COVERAGE-OPEN.

NOT CHECKED — stated plainly, not implied:
· NO PYTHON EXCEPT 3.13. The window's last commit exists BECAUSE the suite passed locally against a file that could not be collected on 3.10/3.11, and I reproduced that exposure exactly: I ran 3.13 only. The new `TestSourceCompilesCleanlyOnEveryTargetPython` claims to close the version gap by promoting SyntaxWarning to an error; I ran it and it passes, but I did NOT verify its premise that an invalid escape is a hard SyntaxError under pytest's rewriter on 3.10/3.11 — that claim is carried, not measured, and it is the load-bearing one.
· NO CONCURRENCY TESTING OF MY OWN. The BEGIN IMMEDIATE work is the window's centre and I verified it by reading the code and running the suite; I did NOT independently drive a two-connection race against `wrap_started` or `wrap_cancelled`, so my read that the CAS is sound is REASONED. The tests that claim it are the ones the commit says it rewrote, and I did not adversarially attack them.
· NO MCP TRANSPORT RUN. `server.py` gained ~150 lines including the whole WrapOwnershipError branch set and a contention message; I read every branch and ran none of them through a live stdio subprocess. `WrapOwnershipError.__reduce__` — the pickling fix — was NOT exercised.
· CLI wrap-cancel NEVER INVOKED, in any form. The `--wrap-token` validator, the three refusal messages and the new `main()` contention boundary are read, not run.
· CHANGELOG.md: +388 lines this window. I read the two sections a finding touched and grepped the rest; the 0.9.9 release narrative is largely unread and unverified against the code it describes.
· `docs/library-quickstart.md`, `Dockerfile`, `glama.json`, `examples/` — not opened. `tests/test_store.py` gained 719 lines; I read the concurrency test names and one body, not the file.
· The audit-after-commit class is filed once at `wrap_started`; I established that `wrap_completed`, `prune` and `continuity_saved` share the shape by reading, and did NOT reproduce the failure at those three sites.
  Tags: anneal-memory, code_review, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260904-043920-89e3036d90cb
  Time: 2026-09-04T04:39:20  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: SELF-CORRECTION — diogenes-20260904-043440-396444e9d69c and diogenes-20260904-043456-7f13a034525d (tonight's two pre-push MEDIUMs) BOTH CARRY A WRONG SUBJECT COORDINATE. I wrote `scripts/hooks/pre-push:23` for the dead sed extraction and `scripts/hooks/pre-push:17` for the working-tree gate. Greped against HEAD immediately before the wrap:
  47:    sed -n '/FAILED\|AssertionError\|Error/p' /tmp/anneal_prepush.$$ | head -8 >&2
  41:if ! "$PY" -m pytest tests/test_integrity.py -q \
CORRECTED: the sed extraction is `scripts/hooks/pre-push:47`; the gate invocation is `scripts/hooks/pre-push:41`.

⛔ NO DIAGNOSIS CHANGES. Both defects were established by EXECUTION, not by position: BSD sed matching 0 lines on real pytest output while `sed -nE` matches 2, and a real clone where the hook exits 0 with a 0.9.9 HEAD and an uncommitted fix. The quoted strings in both episodes are verbatim and are what a reader should search on. Only the numbers are wrong.

⛔ AND THIS IS THE DERIVED-VS-TYPED DISCRIMINATOR FAILING FOR THE FIRST TIME ON A **SUBJECT** COORDINATE IN WEEKS, WHICH MAKES IT THE MOST INFORMATIVE THING I DID TONIGHT. Every subject coordinate I have filed since 2026-08-30 came from a `grep -n` run at the moment of writing, and every one was exact; the slips have all been SUPPORTING references. These two are subjects, and they are the two that came from a NEW-FILE DIFF instead of a grep. `pre-push` did not exist before this window, so it appeared to me only inside `@@ -0,0 +1,58 @@` — and I read positions off the diff display rather than the file. That is the 2026-08-06 error shape (a line number carried from a diff-relative read) at a coordinate I had never greped because the file was new.

▶ THE MECHANISM IS NARROWER AND MORE ACTIONABLE THAN "I GOT LAZY": a file added in the window has NO disk-greped coordinate anywhere in my session history, so the habit that protects every other subject — I had to grep it to find it — never fires. The diff IS the only place I ever saw it.

▶ CLOSE, mechanical and testable: WHEN THE SUBJECT FILE IS NEW IN THE WINDOW (`git diff --name-status` shows `A`), THE COORDINATE MUST BE GREPED AGAINST HEAD BEFORE THE EPISODE IS WRITTEN — the diff hunk header is never the source. One extra grep, gated on a condition I can compute from the diffstat I already run.

▶ ALSO RECORDED, NOT EXCUSED: the 08-30 subtractive close broke twice tonight in the slot, where I attached line numbers to two SUPPORTING references (`continuity.py:939-941` and `:720`). Both were derived from `sed -n` runs in the same session and both verify exact — but the rule exists so that correctness does not have to depend on that. Fifth night recording this class without changing behaviour; the rule is not the gap, the CHECK is, and the check still has to fire at composition time rather than at the wrap.
  Tags: anneal-memory, code_review, semantic
```
