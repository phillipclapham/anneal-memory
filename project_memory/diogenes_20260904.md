# DIOGENES OVERNIGHT — anneal-memory, 2026-09-04

**Source selector:** `episodic.py read --since 24h --tags anneal-memory  (filtered to Agent: diogenes at 2026-09-04*)`
**Stated count: 21 episode(s).** Severity: HIGH 2 · LOW 3 · MEDIUM 9 — 14 of 21 episode(s) carry a severity; the other 7 are COVERAGE 2 · COVERAGE-OPEN 2 · SELF 1 · STILL OPEN 2
**STILL OPEN: 12** — **8 filed tonight** (measured at HEAD this window) · **4 carried**. A carried item was NOT necessarily re-checked this window; only the slot block below says which ones were.

> ⚠ **UNTRIAGED AND VERBATIM.** Generated by `scripts/route_diogenes.py` from the episodic store without judgement. Nothing here has been checked against disk except where a line says so explicitly. Diogenes' own `[prescription: ...]` tags are his assessment, not a ruling.
> ⚠ **ROUTED LATE.** This is not the morning it was filed; the STILL-OPEN count inside is as of the review date, not today.

---

```
[laptop/flow]   ID: diogenes-20260904-042041-99d42a420567
  Time: 2026-09-04T04:20:41  Agent: diogenes  Type: observation
  Source: laptop_overnight_review
  Content: COVERAGE-OPEN: anneal-memory 2026-09-04, HEAD 34728acdf49104ef443399caf401bc42de4ec4ad (== origin/main? YES — `git ls-remote origin HEAD` answered 34728acd against the LIVE remote, not a cached ref), `git status --porcelain` EMPTY, branch main, window 3a763f2..HEAD = 10 commits (closes EXACTLY on my 2026-09-03 review HEAD 3a763f2, so no un-reviewed gap). NOTE: ~/Documents/anneal-memory is a SYMLINK — `pwd -P` resolves to /Users/phillipclapham/Briefcase/anneal-memory; same repo, not two targets. Carried baseline to verify: 20 open at 3a763f2 (6 filed 09-03 + 14 carried). REVIEW STARTED, NOT FINISHED — if no COVERAGE line for anneal-memory exists in this run, THIS RUN DIED and the finding list is PARTIAL. [prescription: none]
  Tags: anneal-memory, code_review, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260904-043355-0ef5a13bfca1
  Time: 2026-09-04T04:33:55  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: HIGH: anneal_memory/store.py:2088 — THE AUDIT GUARD ADDED TONIGHT AS A codex L3 HIGH LANDED ON ONE OF FIVE SITES, AND THE ONE IT MISSED IS THE ONLY ONE THAT WEDGES THE STORE. `wrap_cancelled` gained a try/except around its post-commit audit write this window, with a comment stating the rule: "THE CANCELLATION IS ALREADY COMMITTED ABOVE. Letting an audit I/O failure (disk full, permissions, a failed rotation) propagate would raise out of this method with the metadata ALREADY cleared and no receipt returned". `wrap_started` — modified in this same window, forty lines above — still calls the audit bare, AFTER its own commit and OUTSIDE `_db_boundary`: [quoted: `            self._audit.log(`] followed by [quoted: `                "wrap_started",`]. `_audit_log` does not swallow either, so `wrap_completed`, `prune` and `continuity_saved` carry the same shape; filed ONCE, at the worst site.

RUN-VERIFIED end to end in /tmp/dio_am on a scratch store, patching only the audit sink to raise OSError(28, "No space left on device") — the exact class the fixed sibling's comment names:
  before: wrap_started_at = ''
  RAISED: OSError [Errno 28] No space left on device
  after:  wrap_started_at = '2026-09-04T08:21:54.756198Z'   wrap_token = 'tok-A'
  SECOND wrap_started -> WrapInProgressError

THREE THINGS ARE WRONG AT ONCE AND THE THIRD IS THE SEVERE ONE. (a) The wrap IS started and the caller is told it failed. (b) A raw `OSError` escapes a Store method — not a StoreError — breaking the caller-consistency primitive the comment eighteen lines above defends by name ("EVERY store failure surfaces through StoreError"; "An optimisation outside the error boundary is a hole in the boundary"). (c) The store is left WEDGED: the next `prepare_wrap` raises WrapInProgressError, and the caller cannot use the new proven cancel because it never received the token — so recovery requires an UNPROVEN `wrap_cancel`, which this same window built the CAS to discourage. That is Alex De Groodt's three-day lockout, reachable through the release that closed it, and the commit body names that lockout four times.

The policy is not new and the file states it: `_batch` swallows deferred audit-flush exceptions after a successful commit, "because propagating one would trick the caller's outer except clause into cleaning up tmp files that represent committed state — a data-loss path." Verified on disk: that flush is wrapped in try/except Exception: pass.

The fix: wrap the `wrap_started` audit call in the same try/except-warn-swallow pattern `wrap_cancelled` now uses, including the inner catch around `warnings.warn` for the `-W error` case. NOT EXECUTED. [prescription: reasoned]
  Tags: anneal-memory, code_quality, code_review
```

---

```
[laptop/flow]   ID: diogenes-20260904-043410-deeae08b4c60
  Time: 2026-09-04T04:34:10  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: MEDIUM: tests/test_integrity.py:712 — THE GUARD WRITTEN TONIGHT TO PIN SKILL.md TAKES max() OVER THE UNION OF ALL LADDER LINES, SO ONE CORRECTED LINE CERTIFIES EVERY UNCORRECTED ONE — AND THERE IS AN UNCORRECTED ONE. The assertion is [quoted: `        assert max(levels) > 3, (`], where `levels` is built as [quoted: `        levels = {`] / [quoted: `            int(m)`] / [quoted: `            for line in ladder_lines`] / [quoted: `            for m in re.findall(r"(\d+)x", line)`] — a set comprehension flattened across EVERY matched line, not per line.

RUN-VERIFIED against the real file. The collector matches two lines:
  line 35: levels=[1, 2, 3]      "Graduated patterns (1x→2x→3x) live in continuity's `## Patterns`, always loaded..."
  line 85: levels=[1, 2, 3, 4, 12]  "Patterns graduate 1x → 2x → 3x → 4x → … with **no top rung**..."
  UNION = [1, 2, 3, 4, 12], max = 12, assert max>3 -> True. PASSES.

So the FIRST ladder sentence an agent reads in the doc an AGENT LOADS still teaches the exact ceiling AM-LEVELCAP removed on 2026-08-14, fifty lines above the correction, and the test added the same day to stop precisely that cannot see it. The docstring's own mutation check — "restore `1x → 2x → 3x` as the taught ladder and this fails" — passes because it mutates the one line already fixed; it cannot distinguish "one line fixed" from "the file fixed", which is the property the test is named for.

The commit's own message calls this class out: "an EXACTLY test checked one direction... a naive scan fails on correct text, the same over-matching trap as the SKILL.md 'CLI only' guard." Same commit, new instance, in the guard rather than the guarded.

The fix: assert per ladder line rather than over the union, then correct the line it flags. RAN THE PREDICATE HALF against the real file — a per-line max flags exactly line 35 and nothing else, so it neither misses nor over-matches. Did NOT edit the test or SKILL.md. [prescription: reasoned]
  Tags: anneal-memory, code_quality, code_review
```

---

```
[laptop/flow]   ID: diogenes-20260904-043424-67bcb251cc29
  Time: 2026-09-04T04:34:24  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: MEDIUM: skill/anneal-memory/SKILL.md:85 — THE LADDER FIX IMPORTED A CLAIM THE LIBRARY'S OWN GENERATED INSTRUCTIONS EXPLICITLY REFUTE, INTO THE ONE DOC AN AGENT LOADS. The rewritten sentence reads [quoted: `the level is a monotonic high-water mark of how many times lived experience re-earned the pattern, so a mature pattern is re-stamped `12x`, never flattened back to `3x`.`]. The wrap instructions this library GENERATES say the opposite in as many words: "⚠ The level you WRITE is not immutable: an ungrounded re-stamp can be demoted, so the visible `Nx` is current standing, not a ratchet. The monotonic high-water mark is kept by the library (`max_level_reached`), not by this line."

RUN-VERIFIED that the library is right and SKILL.md is wrong. Scratch store, a today-dated 4x line with a fabricated citation id, through the real `validated_save_continuity`:
  result demoted: 1
  AFTER: - widgets_and_gears | 4x -> `- widgets_and_gears | 3x (2026-09-04) (ungrounded)`
The written level went DOWN. It is not monotonic; `max_level_reached` is.

THE SHAPE: this is the carried finding I have had open on this repo since 2026-09-01 — the same refuted framing standing next to its own correction inside one generated prompt — PROPAGATED into a second file by the commit that fixed that file's other defect. The correction the source carries did not travel with the claim, so SKILL.md now teaches, with no caveat, the thing continuity.py stops to warn about. The half that WAS fixed (no top rung) is right and worth keeping; only the "monotonic high-water mark" gloss is false of the written level.

Note this is why it matters more here than in a comment: an agent that believes the written Nx is a ratchet will re-stamp a demoted line back up, which is the sycophancy path the immune system exists to catch.

The fix: say what continuity.py says — the level climbs with no ceiling, the visible Nx is current standing, and the monotonic high-water mark is `max_level_reached`, held by the library. NOT EXECUTED. [prescription: reasoned]
  Tags: anneal-memory, code_review, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260904-043440-396444e9d69c
  Time: 2026-09-04T04:34:40  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: MEDIUM: scripts/hooks/pre-push:23 — THE HOOK'S "HERE IS WHY" BLOCK MATCHES NOTHING ON macOS, SO EVERY BLOCKED PUSH GETS A HARDCODED DIAGNOSIS THAT IS WRONG FOR HALF THE GATE. The extraction is [quoted: `    sed -n '/FAILED\|AssertionError\|Error/p' /tmp/anneal_prepush.$$ | head -8 >&2`]. BSD sed has no `\|` alternation in a basic regex — it matches a LITERAL pipe.

PROVEN, not inferred, on this machine:
  input:  "FAILED foo" / "E   AssertionError: bar" / "plain line"
  the hook's form            -> [matched 0 lines]
  a line containing the literal string `FAILED|AssertionError|Error` -> MATCHES (proving it is a literal, not alternation)
  `sed -nE '/FAILED|AssertionError|Error/p'` -> [matched 2 lines]

Then reproduced on the REAL path: a scratch clone stamped at 0.9.9 on a post-tag HEAD. The hook correctly blocked (exit 1) — and printed a blank gap where the reason goes, while the underlying pytest output contains both `E       AssertionError: HEAD is stamped '0.9.9'...` and `FAILED tests/test_integrity.py::TestReleaseStampIsNotAPublishedVersion::...`. The evidence was there; the extractor could not see it.

WHY THAT IS MORE THAN COSMETIC: the hook gates TWO unrelated classes — [quoted: `        -k "ReleaseStampIsNotAPublishedVersion or SourceCompilesCleanlyOnEveryTargetPython" \`] — and the message under the dead extraction is unconditional: "HEAD is stamped at an already-released version. Bump to the next .devN (pyproject.toml, anneal_memory/__init__.py, server.json x2)". An invalid escape sequence blocking the push is therefore reported as a version-stamp problem with four remediation steps that do nothing, and the real cause was suppressed by the one line meant to surface it. The hook's own header says it "Deliberately FAILS CLOSED... a guard that degrades to a pass when it cannot run is the exact shape of the defect it guards against" — it fails closed correctly and then misnames why.

The fix: `sed -nE '/FAILED|AssertionError|Error/p'`. EXECUTED against the same input: 2 lines, both the ones wanted. [prescription: run]
  Tags: anneal-memory, code_quality, code_review
```

---

```
[laptop/flow]   ID: diogenes-20260904-043456-7f13a034525d
  Time: 2026-09-04T04:34:56  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: MEDIUM: scripts/hooks/pre-push:17 — THE GATE READS THE WORKING TREE AND THE PUSH PUBLISHES COMMITS, SO AN UNCOMMITTED FIX LETS THE BAD COMMIT THROUGH THE ONE INSTRUMENT IN THE WINDOW. The gate is [quoted: `if ! "$PY" -m pytest tests/test_integrity.py -q \`], and the test it runs reads the stamp by importing the package — `from anneal_memory import __version__` — i.e. off disk. The hook reads NOTHING from stdin, where git supplies `<local ref> <local sha> <remote ref> <remote sha>`, so it never learns which commits are being pushed. Its own failure text then asserts a fact about a different object: "HEAD is stamped at an already-released version."

RUN-VERIFIED on a scratch clone at /tmp/dio_am/clone with the hook really installed via `bash scripts/install_hooks.sh` (core.hooksPath=scripts/hooks confirmed):
  1. BASELINE — commit a 0.9.9 stamp on a post-tag HEAD, working tree matching:
       HOOK EXIT=1, blocked with the bump instructions. The guard works for the case it was built for.
  2. THE HOLE — leave that commit as HEAD, edit the four stamp sites in the WORKING TREE ONLY to 0.9.10.dev0, commit nothing:
       working tree: __version__ = "0.9.10.dev0"
       HEAD (what a push publishes): __version__ = "0.9.9"
       HOOK EXIT=0
The bad commit goes public and the gate reports health, because it looked at a file nobody is pushing.

THIS IS THE REACHABLE ORDER, NOT AN EXOTIC ONE: you notice the stamp is wrong, you fix the four files, and you push before committing — which is the same sequence that produced the incident (`a90c2cb`, "I committed the exact defect spore-710 exists to prevent"). It also covers `git push origin <branch>` while checked out somewhere else. And by the hook's own reckoning it is the ONLY thing in this window: "CI fires after the push, which is after the damage. This closes that window."

The commit body says "Verified on the real path rather than simulated" — and it verified case 1. Case 2 is the one the verification could not see, the same shape as the L3 note this repo wrote into cli.py the same day: "the first test could not see it: it held the lock BEFORE opening, so the post-open race was never exercised."

The fix: read the refs from stdin and check the stamp in each pushed commit — e.g. `git show "$local_sha":anneal_memory/__init__.py` — rather than importing the working tree. NOT EXECUTED. [prescription: reasoned]
  Tags: anneal-memory, architecture, code_review
```

---

```
[laptop/flow]   ID: diogenes-20260904-043527-17bf12be404b
  Time: 2026-09-04T04:35:27  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: MEDIUM: README.md:208 — THE ELEVEN-SURFACE LEVELCAP SWEEP NEVER TOUCHED README.md, SO THE PUBLIC SURFACE STILL TEACHES THE CEILING AT THREE SITES. `git show --stat def07cf` lists CHANGELOG.md, continuity.py, crystal.py, graduation.py, SKILL.md, test_continuity.py, test_graduation.py, test_integrity.py. README.md is not among them, and it is what an adopter reads on GitHub and PyPI.

The three live sites, quoted from disk:
- "A 2x/3x graduation that fails either check above demotes (3x→2x, 2x→1x)" — both halves wrong: the scope excludes 4x+, and the enumeration has no 4x→3x rung.
- "│ decision     │── wrap ───→│ ## Patterns (1x→3x)  │" (the architecture diagram)
- "Patterns can only be promoted (1x -> 2x -> 3x) during compression"

RUN-VERIFIED that a 4x+ line really does validate and demote, so these are false rather than merely dated. Scratch store, today-dated `- widgets_and_gears | 4x (2026-09-04) [evidence: <fabricated id> ...]` through the real `validated_save_continuity`: demoted 1, and the line came back as `- widgets_and_gears | 3x (2026-09-04) (ungrounded)` — a transition the README's own parenthetical says does not exist.

WHY THIS IS THE SAME DEFECT AND NOT A LEFTOVER. This repo's own commit message names the class as CAUSATIVE, not cosmetic: "at least one consumer implements exactly that, so the cap the release removed was RE-DERIVED FROM THE DOCUMENTATION by every reader who trusted it", and "A WIDENING EMITS NO ERROR SIGNAL ANYWHERE DOWNSTREAM... the only symptom is absence." The consumer that was caught doing it was in-house and has since been fixed; README is the surface reached by consumers who are not.

⚠ The asymmetry is now inverted from the one the same commit corrected. On 2026-09-03 the complaint was that README got the generalising tool guard while SKILL.md got a hardcoded one; SKILL.md now has both a tool guard and a ladder guard, and README has neither ladder guard nor a ladder fix.

The fix: correct all three sentences and extend the ladder assertion over README.md as well as SKILL.md, so the two docs cannot diverge again. NOT EXECUTED. [prescription: reasoned]
  Tags: anneal-memory, code_review, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260904-043527-d4876879d6d5
  Time: 2026-09-04T04:35:27  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: LOW: anneal_memory/continuity.py:2693 — THE REWORDED WARNING NOW NAMES A TIER WIDER THAN ITS OWN GATE, AND CONTRADICTS THE COMMENT EIGHT LINES ABOVE IT. The operator-facing string is [quoted: `            f"{len(graduate_out)} Proven-tier pattern(s) were carried forward "`], while the predicate that fills it is [quoted: `            if cf.max_level_reached >= 3 and not cf.provenance`].

This repo established in THIS window that "Proven-tier" means 2x-and-up with no ceiling — types.py now reads "existing Proven (2x+) pattern names", graduation.py "Patterns at Proven-tier (2x+)", crystal.py "level >= MIN_PROVEN_LEVEL" where MIN_PROVEN_LEVEL = 2. The gate is >= 3. So the sentence enumerates a set strictly narrower than the category it names, and the comment directly above says so in the opposite direction: "Lower-tier carries (2x) are held silently — they are the normal domain-blind-erosion-fix case, not a graduate-out decision", under a heading that still reads "assisted ... surface for TOP-tier patterns held this wrap."

⭐ THIS IS A GENUINE IMPROVEMENT WITH A NEW EDGE, NOT A REGRESSION — I want that on the record. The prior text said "top-tier (3x) pattern(s)", which called a 12x pattern 3x on every wrap; that is my carried 2026-09-01 MEDIUM and it is CLOSED. What replaced it is imprecise rather than false: a 3x carry IS Proven-tier, so no statement here is untrue. The cost is that a reader acting on the message ("review these") is looking at a list that silently excludes part of the tier it names, and the exclusion is deliberate and unstated.

The blanket 2x/3x -> Proven-tier rename was right at every other site I checked; this is the one site where the threshold genuinely is 3 and not MIN_PROVEN_LEVEL, so the sweep's substitution does not hold.

The fix: name the actual gate — "pattern(s) at 3x or above" — or state the exclusion in the sentence. NOT EXECUTED. [prescription: reasoned]
  Tags: anneal-memory, code_review, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260904-043543-b4c40d67eb7b
  Time: 2026-09-04T04:35:43  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: LOW: anneal_memory/cli.py:275 — THE CONTENTION DISCRIMINATOR WAS COPIED INTO TWO MODULES IN ONE COMMIT, INSIDE A CHANGE SET WHOSE STATED PURPOSE IS CLOSING land-somewhere-not-everywhere. `_is_write_lock_contention` is defined at cli.py:275 and again in server.py, both new this window. Verified IDENTICAL, not eyeballed: parsing both with `ast`, stripping docstrings and hashing the dumped body gives `bb2f6004b0da510c` for each.

Both copies carry the same three-element result-code tuple — [quoted: `            "SQLITE_BUSY",`], [quoted: `            "SQLITE_BUSY_SNAPSHOT",`], [quoted: `            "SQLITE_BUSY_TIMEOUT",`] — and the same Python-3.10 text fallback [quoted: `    return "database is locked" in text or "database is busy" in text`]. Every one of those is a thing that changes: a fourth SQLITE_BUSY_* code, the 3.10 floor being dropped, or a different sqlite build's message wording. Two copies means the CLI and the MCP transport can start disagreeing about whether the same exception is contention, silently and in opposite directions — the CLI printing a traceback where the server prints the recovery guidance, or vice versa.

⚠ THE FILE ITSELF ARGUES AGAINST THIS, TWICE, IN THE SAME COMMIT. cli.py's `_print_contention` docstring: "One function so the open-time path and the command-time path cannot drift into saying different things about the same condition." And the boundary comment: "It sits HERE, not at the `wrap_cancelled()` call site, because the window is not specific to that command... Fixing the one site that was noticed is how the previous scoping got written. One boundary, all subcommands." The message got one home; the predicate that decides when to show it got two.

⚠ HONEST SCOPE: they agree today and nothing is currently wrong. This is debt with a named divergence mechanism, not a live defect, which is why it is LOW and not MEDIUM. It is also cheap now and gets dearer: `store.py` already exports `StoreDatabaseError`, so a single `is_write_lock_contention` beside it would be imported by both transports and testable once.

The fix: move it to `store.py` next to `StoreDatabaseError` and import it in both transports. NOT EXECUTED. [prescription: reasoned]
  Tags: anneal-memory, architecture, code_review
```

---

```
[laptop/flow]   ID: diogenes-20260904-043705-e78dac3cf9da
  Time: 2026-09-04T04:37:05  Agent: diogenes  Type: observation
  Source: laptop_overnight_review
  Content: STILL OPEN (12), TOTAL AT HEAD 34728ac — anneal-memory, 2026-09-04. 8 filed tonight (1 HIGH / 5 MEDIUM / 2 LOW), 4 carried. Filed separately: 0 DRIFT (not in this count) — and that is a RULE OUTCOME, not a judgement: I ran codeshape.py 3a763f2 against every Python file I filed against (anneal_memory/store.py, anneal_memory/continuity.py, anneal_memory/cli.py, anneal_memory/server.py, tests/test_integrity.py) and all five returned exit 1, CODE CHANGED = FINDING. The other two subjects are skill/anneal-memory/SKILL.md and README.md, which are markdown and can never be DRIFT by rule, so no check was run on them; scripts/hooks/pre-push is a NEW FILE this window and is shell, so its code changed by construction. ZERO SELF-CORRECTIONS.

ARITHMETIC: prior slot diogenes-20260903-041153-60518ff1ecc5, STILL OPEN (20) at HEAD 3a763f2, which is EXACTLY this window's base. MINUS 16 closed, PLUS 8 new = 12.

⭐⭐ SIXTEEN OF TWENTY CLOSED — the largest remediation this repo has had — AND EVERY ONE RE-DERIVED AT HEAD RATHER THAN READ OFF THE COMMIT MESSAGES THAT CLAIM THEM. The six 2-or-3 prose sites: one grep across crystal.py and graduation.py for all four spellings returns EMPTY. crystal.py's consumer contract, its module docstring and its unsourced quotation: all three rewritten, and the quotation now cites the real site. graduation.py's "inert" deferral: rewritten with the measured falsification. The CHANGELOG zero-padding note: corrected IN PLACE with an explicit dated correction block rather than a rewrite. The store's TOCTOU, its two-predicates-for-one-fact, and the receipt docstring's self-contradiction: closed by the BEGIN IMMEDIATE and the single in-transaction `partial_state`. tests/test_server.py's "the race made concrete": the docstring now states honestly what it does and does not prove and names the real interleaved test. The SKILL.md hardcoded tool guard: replaced by an every-tool loop. The EXACTLY test now round-trips every TAUGHT marker through the real reader, which is the direction it was named for and did not check.

TWO CLOSED BY MEASUREMENT I HAD DEFERRED, AND THAT IS THE POINT OF THE TWO-CARRY LIMIT:
· CHANGELOG's "the suite was green before and after the level-cap fix... both fixes are pinned by mutation-verified tests" had been carried TWICE on the file text being unchanged, which my own 2026-09-02 close forbids a third time. So I ran the mutation instead of carrying it: restored `([23])x` in `_GRADUATION_RE` on a scratch clone and the suite went RED at `TestZeroPaddedLevelsAreNotGraduations::test_well_formed_levels_still_match[12-12]`. The ceiling IS pinned. CLOSED, by the method the finding was made with.
· The release-stamp divergence — "the tag points at HEAD and HEAD is not what is on PyPI" — closed from the ARTIFACT, not the tag. Downloaded the published 0.9.9 sdist from PyPI and diffed it against `git archive v0.9.9`: __init__.py, store.py, server.py, cli.py, integrity.py, BOTH tool-integrity.json manifests, SKILL.md, pyproject.toml, server.json, CHANGELOG.md and README.md are all BYTE-IDENTICAL. HEAD is stamped 0.9.10.dev0, which is not a released version.

THE FOUR CARRIED, EACH RE-DERIVED INDIVIDUALLY AT HEAD AND RE-CITED FROM THE ORIGINATING EPISODE, NOT FROM THE PREVIOUS SLOT'S RE-TYPED LIST:
(1) HIGH graduation.py:96 — `_BARE_GRADUATION_RE` is still `r"\|\s*([23])x\s*..."`, the compile opening at :95 (was :86; it moved with this window's ten-line comment insertion). DELIBERATE per the rewritten deferral, and the deferral's reasoning is now correct where it was false; the asymmetry itself is unchanged and still open.
(2) MEDIUM continuity.py:978 (was :972) — "the level is a monotonic high-water mark of how many times lived experience re-earned the pattern", still standing 39 lines below its own refutation at :939-941 in the SAME generated prompt. Untouched this window: the only high-water line in `git diff 3a763f2..HEAD -- anneal_memory/continuity.py` is the 2x/3x -> 2x+ edit at :720. ⚠ AND IT GOT WORSE — the claim was PROPAGATED into SKILL.md by this window's ladder fix, filed tonight as its own MEDIUM.
(3) LOW assert-for-narrowing — TWO OF THREE CLOSED and I am carrying the remainder rather than the item: continuity.py's `assert package is not None` and `assert cont_tmp is not None` are now explicit typed raises, both -O-safe. server.py:239 `assert isinstance(msg, dict), "parsed message must be a dict"` is unchanged (was :202; it moved with the new import block and `_is_write_lock_contention`).
(4) LOW graduation.py:48 — the coordinate in the AM-LEVELCAP rationale still reads "(pattern_history anchoring, continuity.py:2176)". At HEAD, continuity.py:2176 is a comment about wrap_id and cross-session overlap; the actual `_NAMED_PATTERN_WITH_EVIDENCE_RE` call site in that file is at :2221. Still pointing at the wrong thing, and now at a different wrong thing than when it was filed.

⚖ NOTE ON WHAT 12 MEASURES, AND IT INVERTS THIS REPO'S LAST TWO NIGHTS. 09-01 and 09-03 were dominated by the AM-LEVELCAP prose tail — deferred, cheap, nine of fourteen. That tail is essentially gone. What is left is one runtime HIGH in the store, three guards that cannot see their own subject, and two documentation surfaces the sweep did not reach. The count fell by 8 and got heavier.
  Tags: anneal-memory, code_review, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260904-043752-a07e3a0839b5
  Time: 2026-09-04T04:37:52  Agent: diogenes  Type: observation
  Source: laptop_overnight_review
  Content: COVERAGE: anneal-memory 2026-09-04, HEAD 34728acdf49104ef443399caf401bc42de4ec4ad == origin/main verified against the LIVE remote (`git ls-remote origin HEAD` answered 34728acd, not a cached ref). Window 3a763f2..HEAD = 10 commits / 25 files / 3,073 insertions / 165 deletions, closing exactly on my 09-03 HEAD. `git status --porcelain` EMPTY at start AND at finish and HEAD unchanged — the repo was not touched: every probe was read-only or ran in /tmp/dio_am, including a full `git clone` of the repo into /tmp/dio_am/clone for the mutation and hook work. ⚠ ~/Documents/anneal-memory is a SYMLINK; `pwd -P` resolves to ~/Briefcase/anneal-memory. Checked first — eighth repo running.

RE-DERIVED BY RUNNING, not carried forward:
· FULL SUITE at HEAD: 1764 passed, 3 subtests, 15 warnings, 28.97s, exit 0 — denominator checked against 1722 on 09-03 and 1760 in this window's own def07cf.
· THE HIGH DRIVEN TO COMPLETION on a scratch store: patched only the audit sink to raise OSError(28), called `wrap_started`, and observed a raw OSError escape with `wrap_started_at` and `wrap_token` COMMITTED, then a second `wrap_started` refusing with WrapInProgressError. The wedge is measured, not argued.
· THE SKILL.md LADDER GATE run against the real file: its collector matches two lines, the union of their levels is [1,2,3,4,12], max 12 > 3, PASSES — while line 35 alone tops out at 3. Then ran the prescribed per-line variant: it flags line 35 and nothing else.
· THE MONOTONIC-HIGH-WATER CLAIM refuted by execution: a today-dated 4x line with a fabricated citation went through the real `validated_save_continuity` and came back `3x (ungrounded)`, demoted 1. The written level is not monotonic. That same run is what falsifies README's demotion enumeration.
· THE pre-push HOOK on a real clone with `install_hooks.sh` actually run (core.hooksPath=scripts/hooks confirmed): blocked correctly (exit 1) with the working tree matching a bad HEAD, and PASSED (exit 0) with the same bad HEAD and an uncommitted fix in the tree. Its sed extraction proven dead on BSD sed three ways (0 matches on real input; a literal-pipe line DOES match; `sed -nE` matches 2).
· THE MUTATION for the carried CHANGELOG item: `([2-9]|[1-9][0-9]+)` reverted to `([23])` in `_GRADUATION_RE` on the clone -> suite RED at `test_well_formed_levels_still_match[12-12]`.
· PyPI ARTIFACT vs the v0.9.9 TAG: sdist downloaded and diffed file by file against `git archive v0.9.9` — 12 of 12 byte-identical.
· BOTH integrity manifests: `verify_integrity` returns (True, []) for tool-integrity.json and anneal_memory/tool-integrity.json.
· `codeshape.py 3a763f2` run as the DRIFT discriminator on all eight touched Python modules: types.py/graduation.py/crystal.py exit 0 (code UNCHANGED, doc-only edits), the other five exit 1.
· AST-hashed both copies of `_is_write_lock_contention` — identical, not eyeballed.
· Slot pre-flighted through the LIVE `route_diogenes` parser -> '12', then re-parsed FROM THE STORE after writing -> '12', with all ten episodes bucketing 1 HIGH / 5 MEDIUM / 2 LOW / 1 slot / 1 COVERAGE-OPEN.

NOT CHECKED — stated plainly, not implied:
· NO PYTHON EXCEPT 3.13. The window's last commit exists BECAUSE the suite passed locally against a file that could not be collected on 3.10/3.11, and I reproduced that exposure exactly: I ran 3.13 only. The new `TestSourceCompilesCleanlyOnEveryTargetPython` claims to close the version gap by promoting SyntaxWarning to an error; I ran it and it passes, but I did NOT verify its premise that an invalid escape is a hard SyntaxError under pytest's rewriter on 3.10/3.11 — that claim is carried, not measured, and it is the load-bearing one.
· NO CONCURRENCY TESTING OF MY OWN. The BEGIN IMMEDIATE work is the window's centre and I verified it by reading the code and running the suite; I did NOT independently drive a two-connection race against `wrap_started` or `wrap_cancelled`, so my read that the CAS is sound is REASONED. The tests that claim it are the ones the commit says it rewrote, and I did not adversarially attack them.
· NO MCP TRANSPORT RUN. `server.py` gained ~150 lines including the whole WrapOwnershipError branch set and a contention message; I read every branch and ran none of them through a live stdio subprocess. `WrapOwnershipError.__reduce__` — the pickling fix — was NOT exercised.
· CLI wrap-cancel NEVER INVOKED, in any form. The `--wrap-token` validator, the three refusal messages and the new `main()` contention boundary are read, not run.
· CHANGELOG.md: +388 lines this window. I read the two sections a finding touched and grepped the rest; the 0.9.9 release narrative is largely unread and unverified against the code it describes.
· `docs/library-quickstart.md`, `Dockerfile`, `glama.json`, `examples/` — not opened. `tests/test_store.py` gained 719 lines; I read the concurrency test names and one body, not the file.
· The audit-after-commit class is filed once at `wrap_started`; I established that `wrap_completed`, `prune` and `continuity_saved` share the shape by reading, and did NOT reproduce the failure at those three sites.
  Tags: anneal-memory, code_review, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260904-043920-89e3036d90cb
  Time: 2026-09-04T04:39:20  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: SELF-CORRECTION — diogenes-20260904-043440-396444e9d69c and diogenes-20260904-043456-7f13a034525d (tonight's two pre-push MEDIUMs) BOTH CARRY A WRONG SUBJECT COORDINATE. I wrote `scripts/hooks/pre-push:23` for the dead sed extraction and `scripts/hooks/pre-push:17` for the working-tree gate. Greped against HEAD immediately before the wrap:
  47:    sed -n '/FAILED\|AssertionError\|Error/p' /tmp/anneal_prepush.$$ | head -8 >&2
  41:if ! "$PY" -m pytest tests/test_integrity.py -q \
CORRECTED: the sed extraction is `scripts/hooks/pre-push:47`; the gate invocation is `scripts/hooks/pre-push:41`.

⛔ NO DIAGNOSIS CHANGES. Both defects were established by EXECUTION, not by position: BSD sed matching 0 lines on real pytest output while `sed -nE` matches 2, and a real clone where the hook exits 0 with a 0.9.9 HEAD and an uncommitted fix. The quoted strings in both episodes are verbatim and are what a reader should search on. Only the numbers are wrong.

⛔ AND THIS IS THE DERIVED-VS-TYPED DISCRIMINATOR FAILING FOR THE FIRST TIME ON A **SUBJECT** COORDINATE IN WEEKS, WHICH MAKES IT THE MOST INFORMATIVE THING I DID TONIGHT. Every subject coordinate I have filed since 2026-08-30 came from a `grep -n` run at the moment of writing, and every one was exact; the slips have all been SUPPORTING references. These two are subjects, and they are the two that came from a NEW-FILE DIFF instead of a grep. `pre-push` did not exist before this window, so it appeared to me only inside `@@ -0,0 +1,58 @@` — and I read positions off the diff display rather than the file. That is the 2026-08-06 error shape (a line number carried from a diff-relative read) at a coordinate I had never greped because the file was new.

▶ THE MECHANISM IS NARROWER AND MORE ACTIONABLE THAN "I GOT LAZY": a file added in the window has NO disk-greped coordinate anywhere in my session history, so the habit that protects every other subject — I had to grep it to find it — never fires. The diff IS the only place I ever saw it.

▶ CLOSE, mechanical and testable: WHEN THE SUBJECT FILE IS NEW IN THE WINDOW (`git diff --name-status` shows `A`), THE COORDINATE MUST BE GREPED AGAINST HEAD BEFORE THE EPISODE IS WRITTEN — the diff hunk header is never the source. One extra grep, gated on a condition I can compute from the diffstat I already run.

▶ ALSO RECORDED, NOT EXCUSED: the 08-30 subtractive close broke twice tonight in the slot, where I attached line numbers to two SUPPORTING references (`continuity.py:939-941` and `:720`). Both were derived from `sed -n` runs in the same session and both verify exact — but the rule exists so that correctness does not have to depend on that. Fifth night recording this class without changing behaviour; the rule is not the gap, the CHECK is, and the check still has to fire at composition time rather than at the wrap.
  Tags: anneal-memory, code_review, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260904-123145-2d0db8d6b4e6
  Time: 2026-09-04T12:31:45  Agent: diogenes  Type: observation
  Source: laptop_overnight_review
  Content: COVERAGE-OPEN: anneal-memory 2026-09-04 (SECOND review of this repo today), HEAD 06e5858e6f2ebd334655f018d9d76af80b1acbd8 (== origin/main? YES — `git ls-remote origin HEAD` answered 06e5858e against the LIVE remote, not a cached ref), `git status --porcelain` EMPTY, window 34728acd..HEAD = 7 commits / 55 files / 10,347 insertions / 144 deletions. NOTE THE WINDOW ARITHMETIC: the dispatch said 12 commits in 24h and git says 17 since '24 hours ago'; the UN-REVIEWED window is 7, because my 04:39 EDT review of this same repo today closed at 34728acd and the other ten commits were already reviewed by me (STILL OPEN (12) filed then). ~/Documents/anneal-memory is a SYMLINK to ~/Briefcase/anneal-memory — checked first. REVIEW STARTED, NOT FINISHED — if no COVERAGE line for anneal-memory exists in this run, THIS RUN DIED and the finding list is PARTIAL. [prescription: none]
  Tags: anneal-memory, code_review, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260904-123708-b6cb03adcb6b
  Time: 2026-09-04T12:37:08  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: HIGH: anneal_memory/cli.py:485 — THE DEGRADED-AUDIT COUNTER WAS ROUTED TO THE ONE TRANSPORT WHERE IT IS STRUCTURALLY ALWAYS ZERO, AND THE README TELLS OPERATORS TO POLL IT. The new field is [quoted: `                    "write_failures": status.audit_write_failures,`], added under a comment that states its own purpose: [quoted: `                    # is swallowed by design, so this is the only surface an`] / [quoted: `                    # operator can POLL for it — a UserWarning had to be caught`] / [quoted: `                    # existed on StoreStatus and reached NO transport: the`] / [quoted: `                    # channel documented as "pollable" was not, on the surface`] / [quoted: `                    # anyone actually polls.`]

⛔ THE COUNTER IS A PLAIN INSTANCE ATTRIBUTE — [quoted: `        self._audit_write_failures: int = 0`] at store.py:1287, set in __init__ and never read from or written to disk. status() returns it directly ([quoted: `            audit_write_failures=self._audit_write_failures,`], store.py:1904). So it counts failures IN THIS PROCESS ONLY. A CLI invocation is a one-shot process that opens a Store, runs one subcommand and exits, and `status` performs no mutations — so no CLI `status` can ever observe a non-zero value. The only command that could set it is a different invocation, in a process that is already gone.

RUN-VERIFIED end to end, not reasoned. Real Store, real audit sink failure (AuditTrail.log raising OSError(ENOSPC, 'No space left on device')), record() called:
  process A (same instance):  record() SUCCEEDED, 1 UserWarning, status().audit_write_failures = 1, audit_last_failure = "record: OSError(28, 'No space left on device')"  -> the four channels work
  fresh Store, same db:       audit_write_failures = 0, audit_last_failure = None
  `anneal-memory --db ... status --json`:  {"entry_count": 1, "write_failures": 0, "last_failure": null}
  `anneal-memory --db ... status` (human): "Episodes:   2 total" / "Audit:      enabled — 1 entries" and NO warning line
The store is genuinely missing an audit entry, both numbers proving it are printed two lines apart, and the field built to report it prints 0.

⚠ SCOPE, STATED PRECISELY RATHER THAN OVERCLAIMED: the MCP path DOES work. server.py:1188 constructs one Store and hands it to `Server(store)` for the process lifetime, so a long-lived server accumulates correctly and server.py:844's new warning line is reachable. This is a CLI defect plus a documentation defect, not a dead field everywhere. types.py:122 is also honest — it says "the trail is incomplete for this process". The scope is stated in the library type and dropped at both surfaces an operator meets.

⛔ AND THE PUBLIC SURFACE INSTRUCTS THE FAILING PATH. README.md:218, rewritten in this same window: [quoted: `Poll \`status()\` rather than relying on having caught the warning.`] — with no process scope anywhere in the paragraph. README is what an adopter reads on GitHub and PyPI, about the feature the project advances as its compliance claim (audit.py cites EU AI Act Article 12(2) by name). An operator who follows that sentence from a cron job or a shell gets a permanent, structural 0 and reads it as health.

⚡ THE DURABLE SIGNAL WAS ALREADY IN HAND AND WAS NOTICED. store.py:1281 says so: "status() already reports audit_entry_count and would happily show a count that trails total_episodes with nothing computing the difference." That difference is cross-process and on disk. The pass observed it, then added a field that cannot cross a process.

The fix: persist the failure count where it survives the process — the SQLite metadata table, which audit.py's own __init__ comment already names as the right home ("persisting outside the sink that is already failing (the SQLite metadata table is a different I/O path)") and explicitly defers. Until that lands, the CLI field must be labelled process-local or dropped, and README.md:218 must not tell an operator to poll it. NOT EXECUTED — I ran the diagnosis in both directions and wrote no code. [prescription: reasoned]
  Tags: anneal-memory, code_quality, code_review
```

---

```
[laptop/flow]   ID: diogenes-20260904-123833-2a753e24e414
  Time: 2026-09-04T12:38:33  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: MEDIUM: tests/test_audit.py:1686 — THE GUARD NAMED 'REACHES EVERY TRANSPORT' IS A SUBSTRING GREP OVER THE SOURCE, WRITTEN FOR A DEFECT ITS OWN DOCSTRING SAYS SOURCE-LEVEL ASKING COULD NOT CATCH. The assertion is [quoted: `        assert "status.audit_write_failures" in text, (`] against [quoted: `        text = source.read_text(encoding="utf-8")`]. The sibling at :1696 is the same shape over server.py, and :1719 is the same shape over every module in the package ([quoted: `            if "audit_write_failures" not in text:`]).

⛔ THE DOCSTRING NAMES THE EXACT TRAP AND THE TEST WALKS INTO IT. tests/test_audit.py:1652 reads [quoted: `    status handler composes its own line. Found by L4 on 2026-09-04 by`] / [quoted: `    running the real CLI — not by any of the 1812 tests, all of which asked the`] / [quoted: `    Python API.`] — and the regression test written from that lesson does not run the CLI either. It asks the source file. :1683's own comment states the intent correctly — [quoted: `        # so assert against the assembling code, not the dataclass.`] — and then greps the assembling code instead of executing it.

MUTATION-VERIFIED, BOTH DIRECTIONS, ON A FULL COPY OF THE REPO AT HEAD. I edited cli.py so the --json payload hardcodes the value and the token survives only in a comment:
    # was: status.audit_write_failures / status.audit_last_failure
    "write_failures": 0,
    "last_failure": None,
and disabled the human-output branch (`if status.audit_write_failures:` -> `if False:`).
  TestDegradedAuditHealthReachesEveryTransport ->  3 PASSED
  the FULL suite                               ->  1822 PASSED, 0 failed
The CLI now reports a hardcoded 0 for degraded audit health on every store, and nothing in 1822 tests notices. The in-process API still returns 1, which is all any test checks.

⚡ AND THE GREP IS SATISFIABLE BY TEXT THAT IS NOT CODE. `assert "status.audit_write_failures" in text` matches a comment, a docstring, or a dead branch — my mutant passed on precisely a comment plus an `if False:` branch. Same over-matching shape this repo names in test_integrity.py:734 ("a naive scan fails on correct text") and closed there THIS WINDOW by moving from a scan to per-run structural extraction. The technique was available in a sibling file, in the same window, and this guard did not get it.

⚠ HONEST SCOPE: this is a coverage defect, not a runtime one. The runtime consequence — the CLI counter being structurally always 0 across a process boundary — is filed separately and is not double-counted here. What this finding adds is that nothing would catch a regression of it either.

The fix: invoke the CLI for real (`cmd_status` against a degraded Store, or a subprocess) and assert on the emitted JSON, the same way test_integrity.py:784 moved from scanning text to extracting structure. NOT EXECUTED — I mutated the subject to prove the guard is blind, and wrote no replacement guard. [prescription: reasoned]
  Tags: anneal-memory, code_quality, code_review
```

---

```
[laptop/flow]   ID: diogenes-20260904-123928-b4858fdd4d02
  Time: 2026-09-04T12:39:28  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: LOW: anneal_memory/store.py:71 — THE STRUCTURAL COLLISION GUARD COVERS FOUR OF THE SIX NAMES THAT CAN COLLIDE, AND ITS DOCSTRING CLAIMS THE STRUCTURAL PROPERTY IT DOES NOT HAVE. The set is [quoted: `_RESERVED_AUDIT_KWARGS = frozenset(`] / [quoted: `    {"method", "committed", "batch_aware", "stacklevel"}`].

The flush call site passes `event` and `payload` POSITIONALLY — store.py:4810 onward reads [quoted: `                self._audit_log_after_commit(`] / [quoted: `                    event,`] / [quoted: `                    payload,`] / [quoted: `                    method=origin_method,`] — and then splats the queued kwargs. So a queued key named `event` or `payload` produces the SAME uncatchable TypeError at the SAME call site as the four that are refused.

MEASURED, not reasoned — signature introspected and both splats reproduced against the real method:
  callee params            : ['self','event','payload','method','committed','batch_aware','stacklevel','kwargs']
  collision-capable        : ['event','payload','method','committed','batch_aware','stacklevel']
  _RESERVED_AUDIT_KWARGS   : ['batch_aware','committed','method','stacklevel']
  UNGUARDED                : ['event','payload']
  _reject_reserved_audit_kwargs({'event':'x'})   -> PASSED THROUGH {'event': 'x'}
  _reject_reserved_audit_kwargs({'payload':'x'}) -> PASSED THROUGH {'payload': 'x'}
  splat {'event':'x'}   -> TypeError: Store._audit_log_after_commit() got multiple values for argument 'event'
  splat {'payload':{}}  -> TypeError: Store._audit_log_after_commit() got multiple values for argument 'payload'
  splat {'actor':'src'} -> reached the callee body normally (the real pass-through is unaffected)

⚠ WHY THIS IS A GAP AND NOT A NON-ISSUE: unreachability is not the discriminator, because the four names that ARE guarded are equally unreachable today. The docstring says so itself — [quoted: `    Not reachable today (``actor=`` is the only pass-through in the codebase);`] / [quoted: `    this refuses it structurally instead of relying on no future caller`] / [quoted: `    choosing an overlapping name.`] The whole justification for the guard is future callers, and for two of the six names it still relies on exactly the thing it says it is replacing.

⛔ AND THE TEST PINS THE INCOMPLETE SET AS THOUGH IT WERE COMPLETE. tests/test_audit.py:1759 asserts [quoted: `        assert _RESERVED_AUDIT_KWARGS == {`] with the four names, then loops [quoted: `        for name in sorted(_RESERVED_AUDIT_KWARGS):`] — it derives its cases from the set under test, so it can only ever confirm the names already there. It cannot notice a missing one, which is a smaller version of the union/per-line problem the same window fixed in test_integrity.py.

The fix: derive the set from the signature rather than typing it — `frozenset(inspect.signature(Store._audit_log_after_commit).parameters) - {'self','kwargs'}` — so adding a parameter cannot silently widen the hole, and have the test assert against the signature too. NOT EXECUTED. [prescription: reasoned]
  Tags: anneal-memory, code_quality, code_review
```

---

```
[laptop/flow]   ID: diogenes-20260904-124123-05faf5d20383
  Time: 2026-09-04T12:41:23  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: MEDIUM: pyproject.toml:53 — 984 KB OF INTERNAL PROJECT MEMORY ENTERED THE PUBLISHABLE SDIST THIS WINDOW, AND THE ONE CONFIG LINE THAT NOTICED THE DIRECTORY IS SPECIAL ONLY TOLD RUFF. The added stanza is [quoted: `exclude = ["project_memory"]`] under [quoted: `[tool.ruff]`], justified at :48 by [quoted: `# project_memory/ holds project-tracking documents and the ARCHIVED Phase-1`] / [quoted: `# hostile-testing scripts, which moved into this repo on 2026-09-04 with the`] / [quoted: `# project memory. They are receipts of what was run then, not package source`]. "Not package source" is the correct judgement and it was applied to the linter and not to the build.

⛔ MEASURED BY BUILDING BOTH SDISTS, not by reasoning about hatchling defaults. Clean venv, hatchling 1.32.0, `python -m build --sdist` at each revision:
  34728acd (my 04:39 HEAD): 1,073,554 bytes,  project_memory files = 0
  06e5858  (HEAD):          1,436,684 bytes,  project_memory files = 39
  delta: +363,130 bytes compressed, +33.8%, entirely project_memory (984 KB on disk).
The wheel is safe — [quoted: `packages = ["anneal_memory"]`] at :45 scopes `[tool.hatch.build.targets.wheel]`. There is NO `[tool.hatch.build.targets.sdist]` section and no MANIFEST.in, so the sdist takes hatchling's default (the whole VCS tree): the archive also carries `.github`, `.gitignore`, `Dockerfile`, `tests/` and `scripts/`.

WHAT SPECIFICALLY GOES PUBLIC. Six of my own overnight review reports — `project_memory/diogenes_20260811.md`, `_20260815`, `_20260816`, `_20260901`, `_20260903`, `_20260904` — which enumerate open, unfixed defects in the shipped product by file and line. Plus `hostile_test_results.md`, `experiment_results.md`, `next_steps.md`, `flow_migration_scoping.md`, the Phase-1 attack scripts and their captured logs, and two Dockerfiles. THREE of those files contain the operator's absolute home path (`grep -rl "/Users/phillipclapham" project_memory` -> flow_migration_scoping.md, diogenes_20260901.md, diogenes_20260904.md), including the layout of an unrelated private repo and its hook configuration.

⚠ NO CREDENTIALS — I looked, and I am not claiming a secret leak. This is disclosure and hygiene, and the reason it is not LOW is that the action is IRREVERSIBLE: a PyPI sdist cannot be reliably unpublished once mirrors and caches have it, and this repo's own history shows the artifact really is the tree — my 04:39 review diffed the published 0.9.9 sdist against `git archive v0.9.9` and found 12 of 12 files byte-identical. HEAD is stamped 0.9.10.dev0 and nothing has shipped yet, so this is catchable exactly once, before the next `twine upload`.

The fix: add `[tool.hatch.build.targets.sdist]` with an `exclude` (or an explicit `include`) covering `project_memory`, and decide deliberately whether `.github`/`Dockerfile` belong in it too. NOT EXECUTED — I built both artifacts to establish the delta and changed no configuration. [prescription: reasoned]
  Tags: anneal-memory, architecture, code_review
```

---

```
[laptop/flow]   ID: diogenes-20260904-124345-07f99d1c3339
  Time: 2026-09-04T12:43:45  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: MEDIUM: project_memory/CLAUDE.md:41 — THE REPO'S ONLY CLAUDE.md ARRIVED THIS WINDOW WITH THREE MEASURABLY FALSE ENUMERATIONS AND A THREE-RELEASE-STALE 'CURRENT', AND THE IMPORT IS WHAT MADE IT REACHABLE FROM HERE. The Architecture paragraph states [quoted: `6 MCP tools + 2 resources. 21 CLI subcommands.`] and the Status block at :29 states [quoted: `CLI shipped (24 subcommands incl. operator surface).`] — so the file also disagrees with itself about the subcommand count.

RE-DERIVED BY RUNNING, from the two artifacts that cannot be argued with:
  MCP tools     — `tool-integrity.json`, the SHIPPED manifest that `test_shipped_manifest_verifies` guards: **17** (crystal_index, crystal_recall, delete_episode, prepare_wrap, recall, record, save_continuity, spore_add, spore_ascend, spore_descend, spore_get, spore_list, spore_surface, spore_touch, spore_update, status, wrap_cancel). Claimed 6.
  CLI subcommands — parsed off the LIVE argparse choices by running `anneal-memory --help` through `cli.main()`: **30**. Claimed 21 in one paragraph and 24 in another.

AND THE STATUS LINE. :13 reads [quoted: `**CURRENT (2026-06-30): 0.9.6 SHIPPED PUBLIC — first PyPI publish of the 0.9.x line, DECOUPLED from Slice C (spore-222).**`]. Checked against the LIVE PyPI JSON API, not a memory: latest is **0.9.9**, and 0.9.7, 0.9.8 and 0.9.9 are all published. HEAD is stamped 0.9.10.dev0. The word CURRENT is the damage — the block is three published releases behind, and 0.9.7 is AM-LEVELCAP, the release whose removed ceiling this repo has spent three nights chasing out of its documentation.

⚡ WHY THIS IS THIS WINDOW'S FINDING AND NOT AN INHERITED ONE — THE IMPORT CHANGED ITS REACH. `e20ada6` ("bring anneal's project memory into the repo it describes") moved this file from flow's project memory into this repository. `git ls-files | grep -i claude.md` returns exactly one path, this one, so it is now the only CLAUDE.md in the tree; its own second line still describes the old reach, [quoted: `> Loaded when @project anneal_memory invoked.`]. It is also now inside the publishable sdist (filed separately). A file whose staleness was contained to a flow session is now in the repo an adopter clones.

⭐ THE SIBLINGS DID THIS RIGHT AND THAT IS WHY THIS ONE IS FILED ALONE. `next_steps.md` is current to today (it carries the 2026-09-04 move note and last night's routed Diogenes block). `projectbrief.md` is v0.5.0-era and SAYS SO IN ITS OWN STATUS LINE — [quoted: `⚠️ This brief.md is otherwise v0.3.x-era — **canonical current state is `next_steps.md`**`]. Two of the three imported orientation files carry an honest staleness pointer. The third asserts CURRENT and counts three things wrong. I am not filing "the archive is old"; the other 36 imported files are receipts and reading them as live claims would be the error.

The fix: replace the numbers with the derivation ("tools: see tool-integrity.json; subcommands: `anneal-memory --help`") rather than restating them — the same subtraction this constellation ruled on 2026-08-31, since a restated count is what rotted here — and either refresh the Status block or give it the pointer `projectbrief.md` already has. NOT EXECUTED. [prescription: reasoned]
  Tags: anneal-memory, code_review, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260904-124528-41a3c731af4c
  Time: 2026-09-04T12:45:28  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: MEDIUM: tests/test_integrity.py:783 — THE LADDER GATE'S STATED SAFETY PROPERTY IS FALSE, AND THE COUNTEREXAMPLE IS ONE BACKTICK PAIR. The comment claims [quoted: `        # with per-token backticks. This fails in the SAFE direction — forget`] / [quoted: `        # the backticks around a historical quote and the gate fires loudly;`] / [quoted: `        # it cannot silently pass a real ceiling.`] The first half is true. The second is not.

The exemption is unconditional on intent — [quoted: `                if run.startswith("`") and run.endswith("`") and "`" not in run[1:-1]:`] / [quoted: `                    continue  # a whole-span quote of a retired ladder`] at :792. It skips ANY whole-span-backticked ascending run, so it cannot tell a historical quote from live teaching that happens to be code-formatted. The convention at :779 is enforced in one direction only.

MUTATION-VERIFIED, BOTH DIRECTIONS, on a clean clone at HEAD. Identical sentence re-teaching the removed ceiling as live guidance on SKILL.md line 35, differing only by the backtick pair:
  "Graduated patterns graduate `1x -> 2x -> 3x` and then stop, ... that is the ladder, full stop."   -> 1 PASSED
  "Graduated patterns graduate 1x -> 2x -> 3x and then stop, ... that is the ladder, full stop."       -> 1 FAILED ("ladder topping out at 3x or below")
Same claim, same line, same file. Backticks on, the gate is blind; backticks off, it fires. That IS a silent pass of a real ceiling, which the comment says cannot happen.

⚠ AND CODE-FORMATTING A LADDER IS THE NATURAL MARKDOWN INSTINCT, NOT AN EXOTIC INPUT. The fix landed in this same window did exactly that to line 35 — [quoted: `Graduated patterns (`1x`→`2x`→`3x`→`4x`→… , no top rung) live in continuity's `## Patterns``] — reaching for backticks around the ladder tokens as the first move. It happened to use PER-TOKEN backticks, which is the spelling the gate can see. One writer choosing the whole-span spelling, for a ladder they believe is correct, restores the exact 20-day rot this gate exists to prevent, with a green suite.

⚡ WHY THIS MATTERS MORE THAN A NORMAL GUARD GAP: the gate's own docstring at :723 says [quoted: `        ⚠ THIS ROTTED FOR TWENTY DAYS AND NOTHING FIRED. AM-LEVELCAP (0.9.7,`] and :731 says [quoted: `        ⛔ A WIDENING EMITS NO ERROR SIGNAL ANYWHERE DOWNSTREAM.`] — this gate is the ONLY downstream signal that exists for this class, by its own argument. A single-spelling blind spot in the only detector is the whole detector.

⭐ NOT A REGRESSION — THE GATE IS STRICTLY BETTER THAN THE UNION FORM IT REPLACED, and it killed my own prescribed per-line fix as still-a-proxy, which was the right call. This is the residue of a real improvement, and what is wrong is the sentence claiming the residue is not there.

The fix: decide the historical case by CONTEXT rather than by formatting — require an explicit marker the writer must mean (a `said`/`until`/dated-correction cue on the line, which line 85's real historical quote already has), or invert it so a whole-span quote must ALSO sit inside a sentence the gate recognises as retrospective. Failing that, delete the claim at :783 so the blind spot is documented rather than denied. NOT EXECUTED — I mutated the subject in both spellings and wrote no replacement predicate. [prescription: reasoned]
  Tags: anneal-memory, code_quality, code_review
```

---

```
[laptop/flow]   ID: diogenes-20260904-124716-6dd7370dfdab
  Time: 2026-09-04T12:47:16  Agent: diogenes  Type: observation
  Source: laptop_overnight_review
  Content: STILL OPEN (12), TOTAL AT HEAD 06e5858 — anneal-memory, 2026-09-04, SECOND REVIEW OF THIS REPO TODAY. 6 filed this window (1 HIGH / 4 MEDIUM / 1 LOW), 6 carried. Filed separately: 0 DRIFT (not in this count) — and that is a RULE OUTCOME, not a judgement: I ran codeshape.py 34728acd against all four Python files I filed against (anneal_memory/cli.py, anneal_memory/store.py, tests/test_audit.py, tests/test_integrity.py) and every one returned exit 1, CODE CHANGED = FINDING. project_memory/CLAUDE.md is markdown and can never be DRIFT by rule, so no check was run on it. pyproject.toml is neither Python nor markdown, and the evidence I quote there is a live config statement rather than comment text, so DRIFT's first half fails by construction. ZERO SELF-CORRECTIONS.

ARITHMETIC: prior slot diogenes-20260904-043705-e78dac3cf9da, STILL OPEN (12) at HEAD 34728ac, which is EXACTLY this window's base. MINUS 6 closed, PLUS 6 new = 12.

⚠ WINDOW NOTE: the dispatch said 12 commits in 24h and git says 17 since '24 hours ago'. The UN-REVIEWED window is 34728acd..HEAD = 7 commits / 55 files / 10,347 insertions, because my 04:39 EDT review of this same repo today closed at 34728acd. I reviewed the 7, not the 17.

⭐ SIX OF TWELVE CLOSED, EVERY ONE RE-DERIVED BY RUNNING RATHER THAN READ OFF THE COMMIT MESSAGES THAT CLAIM THEM:
· The HIGH (wrap_started emitting bare and outside the policy) — CLOSED and RUN-VERIFIED with the same OSError(ENOSPC) sink I used at 04:39: wrap_started now RETURNS NORMALLY, one UserWarning, wrap_started_at committed, status().audit_write_failures = 1, and NO raw OSError. The caller keeps its token, so the lockout path is gone. `grep -rn '_audit\.log('` over the package returns three hits, all inside the two policy helpers.
· The pre-push BSD-sed MEDIUM — CLOSED and IMPROVED ON. `sed -nE` at :57/:58, plus a fallback to a raw tail my prescription did not include, and the two gate classes are now run separately with class-matched advice.
· The pre-push working-tree MEDIUM — CLOSED and fixed BETTER than I prescribed. I prescribed reading refs from stdin; they added `test_committed_head_stamp_is_not_an_already_released_version`, which reads `git show HEAD:anneal_memory/__init__.py`. REPRODUCED MY OWN HOLE ON A REAL CLONE with the hook installed via install_hooks.sh: HEAD committed at 0.9.9, working tree bumped to 0.9.10.dev0, nothing committed — HOOK EXIT=1 where it was 0 at 04:39, and the `explain` block printed the real assertion text.
· The ladder-union MEDIUM — CLOSED, and the fix killed MY prescription as still-a-proxy (line 85's demotion sentence also mentions 12x/18x, so a per-line max survives the mutant). It now extracts ladder RUNS.
· SKILL.md:85's imported high-water claim — CLOSED at BOTH sites, and the generator half is now pinned by `test_teaching_never_calls_the_VISIBLE_level_monotonic`.
· The carried continuity.py high-water contradiction — CLOSED in place at :983.

⚙ THE SIX CARRIED, EACH RE-DERIVED INDIVIDUALLY AT HEAD AND RE-CITED FROM THE ORIGINATING EPISODE:
(1) MEDIUM README.md:208 — the levelcap sweep STILL has not reached README, second night. All three sites verbatim at HEAD: :208 "demotes (3x→2x, 2x→1x)", :298 the "## Patterns (1x→3x)" diagram, :431 "promoted (1x -> 2x -> 3x)". README changed by exactly 4 lines this window and all four are audit-best-effort text.
(2) HIGH graduation.py:95 — `_BARE_GRADUATION_RE` still `([23])x`. Deliberate, unchanged.
(3) LOW continuity.py:2706 (was :2693) — the warning still reads "Proven-tier pattern(s) were carried forward" while its gate five lines up is `cf.max_level_reached >= 3`.
(4) LOW cli.py:275 — `_is_write_lock_contention` still defined twice, cli.py:275 and server.py:69. Re-derived by AST body hash with docstrings stripped: both 39d29e10c5b70bc5. ⚠ `git log -S` reports e20ada6 touching this symbol — that is a FALSE HIT on my own imported review report in project_memory/, not the code; per-file diffs of cli.py and server.py show the function unchanged.
(5) LOW server.py:239 — `assert isinstance(msg, dict)` unchanged, -O-unsafe.
(6) LOW graduation.py:48 — still "(pattern_history anchoring, continuity.py:2176)"; the real `_NAMED_PATTERN_WITH_EVIDENCE_RE` call site is :2223, and :2176 is a comment about cross-session overlap.

⚖ WHAT 12 MEASURES, AND IT MOVED OFF THE RUNTIME. At 04:39 the count was one runtime HIGH plus five instruments. Tonight the runtime code is the strongest part of the window — I attacked the audit policy hard and it held. Four of six new findings are in GUARDS AND SURFACES: a counter that cannot cross a process, a transport guard that greps source, a safety claim that one backtick falsifies, and a collision guard covering four of six names. The other two are what the project-memory IMPORT dragged in: a stale orientation file and 984 KB of internal receipts in the publishable sdist. Same count, third consecutive shape change.
  Tags: anneal-memory, code_review, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260904-124915-c6a7550fc11a
  Time: 2026-09-04T12:49:15  Agent: diogenes  Type: observation
  Source: laptop_overnight_review
  Content: COVERAGE: anneal-memory 2026-09-04 SECOND REVIEW, HEAD 06e5858e == origin/main VERIFIED AGAINST THE LIVE REMOTE (`git ls-remote origin HEAD` answered 06e5858e at start AND at finish, not a cached tracking ref). `git status --porcelain` EMPTY at start and EMPTY at finish, HEAD unchanged — the repo was not touched: everything read-only or under /tmp/dio_am2 (a full clone with hooks really installed, a full repo COPY for mutation, two clean-venv sdist builds, four probe stores). ~/Documents/anneal-memory is a SYMLINK — checked first, ninth repo running.

RE-DERIVED BY RUNNING, NOT CARRIED FORWARD:
· pytest full suite at HEAD — 1822 passed, 3 subtests, exit 0 (was 1764 at my 04:39 review; the CHANGELOG's 1822 confirmed). mypy — Success, 18 source files. ruff — 63 errors, and `ruff check project_memory` alone = 52, so BOTH CHANGELOG figures ('ruff 63', 'added 52 findings') reproduce exactly.
· LIVE PyPI JSON API — latest 0.9.9, with 0.9.6/0.9.7/0.9.8/0.9.9 all published. Used to ground the CLAUDE.md staleness finding rather than trusting any file in the tree.
· BUILT BOTH SDISTS in a clean venv (hatchling 1.32.0): 34728acd -> 1,073,554 bytes / 0 project_memory files; HEAD -> 1,436,684 bytes / 39. That is the packaging finding, measured rather than reasoned from hatchling defaults.
· THE PRE-PUSH HOOK ON A REAL CLONE, hooks installed via install_hooks.sh (core.hooksPath confirmed): baseline blocked, and MY OWN 04:39 HOLE re-run — HEAD committed at 0.9.9, working tree bumped, nothing committed — now HOOK EXIT=1 where it was 0. Closed, and by a better fix than I prescribed.
· THE AUDIT SWALLOW PATH END TO END with a real OSError(ENOSPC) sink: `record` and `wrap_started` both return normally with one warning and the counter at 1; a fresh Store on the same db reports 0; the CLI's --json and human `status` both report 0 beside 'Episodes: 2 total / 1 entries'.
· TWO MUTATION EXPERIMENTS, both on full copies at HEAD. (a) cli.py hardcoding `"write_failures": 0` -> the transport guard passed AND THE FULL 1822-TEST SUITE PASSED. (b) SKILL.md line 35 re-teaching the removed ceiling, run in both spellings -> whole-span-backticked PASSES, unbackticked FAILS.
· THE LIVE DEMOTION PIPELINE through `validated_save_continuity` at five levels: 2x->1x, 3x->2x, 4x->3x, 12x->11x, 18x->17x, each tagged `(ungrounded)`.
· codeshape.py 34728acd on all four Python subject files (all exit 1); AST body hash of `_is_write_lock_contention` in both modules (identical, 39d29e10c5b70bc5); signature introspection plus a reproduced splat for the event/payload collision.
· Slot pre-flighted through the LIVE parser -> '12', then RE-PARSED FROM THE STORE after writing -> '12', with all 8 episodes bucketing 1 HIGH / 4 MEDIUM / 1 LOW + COVERAGE-OPEN + STILL OPEN.

⭐ FIVE CANDIDATES DIED ON DISK, AND THAT IS A REAL RESULT. (1) I predicted `[tool.ruff] exclude` would silently drop ruff's DEFAULT exclude list and start linting build/dist/.venv — it does override, and it is fully masked by .gitignore + respect-gitignore: 63 either way, ZERO hits from those directories, proven by planting lint-error files in all three. (2) SKILL.md:85's brand-new "measured 2026-09-04: 4x->3x, 12x->11x, 18x->17x" is EXACT. (3) Every CHANGELOG number I could check reproduces. (4) I predicted `validated_save_continuity`'s emit inherits the batch-ordering hazard the store comment describes — it sits OUTSIDE `with store._batch():`, so `batch_aware=True` is harmless there. (5) `git log -S` said `_is_write_lock_contention` changed this window; that is a FALSE HIT on my own imported review report in project_memory/, and per-file diffs show the code untouched.

⚠ NEAR-MISS, RECORDED BECAUSE THE METHOD SLIPPED EVEN THOUGH NOTHING WAS PUBLISHED: my first severity() pre-flight passed the raw episode CONTENT where the function expects a `Content:` BLOCK, and reported all eight episodes as OTHER. I nearly wrote 'the histogram is broken' as an instrument finding. I read the function's source instead of believing my own probe, found my error, and re-ran correctly. The 2026-09-01 close generalises: a probe whose OUTPUT is the evidence has to be validated before its output is.

⛔ NOT CHECKED — STATED, NOT IMPLIED, AND THE FIRST TWO ARE SECOND-NIGHT REPEATS:
· PYTHON 3.13 ONLY, again. The cross-version gate's load-bearing premise (an invalid escape is a hard SyntaxError on 3.10/3.11 under pytest's rewriter) is STILL unverified by me. Second night naming it.
· NO CONCURRENCY TESTING of my own against the BEGIN IMMEDIATE / wrap-token CAS work. Second night. My read that it is sound is INHERITED from 04:39, not re-established.
· THE MCP TRANSPORT WAS NEVER RUN — and this is the honest limit on my own HIGH. I read server.py's new warning line and the single `Store(...)` construction at :1188 and REASONED that a long-lived server accumulates the counter correctly. I never started a server, never drove a status call through it, never forced a failure through it. The 'MCP works, CLI does not' half of that finding's scope is reasoned; only the CLI half is run.
· THE WINDOW'S HEADLINE FIX WAS READ, NOT ATTACKED. The audit-append rollback (the codex L3 HIGH — record the pre-append size, truncate on failure) I read closely and judged sound for the single-writer contract; I did NOT independently reproduce the fsync-EIO or partial-write cases, and relied on the suite for them. The whole ROTATION path is likewise untouched by me — `_rotate_if_needed`, the gzip seal, the manifest, orphan adoption, and the MED the CHANGELOG itself files about an exception mid-rotation. Those tests RAN inside the suite; I did not read or attack them.
· `wrap-cancel` never invoked in any form, third night on this repo. `WrapOwnershipError.__reduce__` never exercised.
· 36 OF THE 39 IMPORTED project_memory FILES WERE NOT READ. I read CLAUDE.md in full, next_steps.md's first 25 lines and projectbrief.md's first 14; the six diogenes reports, hostile_test_results.md, experiment_results.md, the Phase-1 attack scripts and every .log were listed, grepped for absolute paths, and otherwise unopened. My CLAUDE.md finding is deliberately scoped to the one file that asserts CURRENT — I am not claiming the archive was audited.
· tests/test_audit.py is ~2,000 lines and I read roughly 350 (the four classes I name). The 200 new CHANGELOG lines I read in full, but its mutation tallies (5 killed, 15 killed) I did NOT re-derive. No wheel build, no pip install, no smoke: the sdist was BUILT and inspected, never installed. No push, no publish; the only network was the PyPI JSON API and `git ls-remote`.

⛔ AND I BROKE THE 08-30 SUBTRACTIVE CLOSE IN ALL SIX FINDINGS — SIXTH CONSECUTIVE NIGHT RECORDING THIS CLASS. Supporting (non-subject) references carrying line numbers: store.py:1287/:1904/:1281/:4810, types.py:122, README.md:218, server.py:1188/:844, test_integrity.py:734/:784, and the rest. Every one was derived from a grep or a run in this session and every one is exact — and that is precisely why the rule exists, so correctness does not have to depend on it. Six nights recorded, zero nights changed. The rule is not the gap; the check is, and it still is not firing at composition time.

⚙ ONE OBSERVATION I AM NOT FILING HERE BECAUSE ITS SUBJECT IS ANOTHER REPO: route_diogenes' coordinate gauge reported of my 04:39 report '1 could NOT be confirmed — anneal_memory/cli.py:275 -> quoted text is at :291'. My subject coordinate :275 is correct (the `def` is there); the gauge located my QUOTED line instead and read the disagreement as drift. That belongs to flow, and a repo-scoped reviewer cannot close it. [prescription: none]
  Tags: anneal-memory, code_review, semantic
```
