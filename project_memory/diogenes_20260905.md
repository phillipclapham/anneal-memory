# DIOGENES OVERNIGHT — anneal-memory, 2026-09-05

**Source selector:** `episodic.py read --since 24h --tags anneal-memory  (filtered to Agent: diogenes at 2026-09-05*)`
**Stated count: 9 episode(s).** Severity: HIGH 1 · LOW 3 · MEDIUM 2 — 6 of 9 episode(s) carry a severity; the other 3 are COVERAGE 1 · COVERAGE-OPEN 1 · STILL OPEN 1
**STILL OPEN: 7**

> ⚠ **UNTRIAGED AND VERBATIM.** Generated by `scripts/route_diogenes.py` from the episodic store without judgement. Nothing here has been checked against disk except where a line says so explicitly. Diogenes' own `[prescription: ...]` tags are his assessment, not a ruling.

---

```
[laptop/flow]   ID: diogenes-20260905-044402-9c4bec127d52
  Time: 2026-09-05T04:44:02  Agent: diogenes  Type: observation
  Source: laptop_overnight_review
  Content: COVERAGE-OPEN: anneal-memory 2026-09-05, HEAD 5b791728 (== origin/main? YES — `git ls-remote origin HEAD` answered 5b79172843c851491e55d30f2b11859282f535f3 against the LIVE remote, not a cached tracking ref), `git status --porcelain` EMPTY, branch main. ~/Documents/anneal-memory is a SYMLINK to ~/Briefcase/anneal-memory — checked first.

WINDOW: 06e5858..HEAD = 25 commits / 21 files / 2,682 insertions, 187 deletions. NOT the dispatch's 31/24h — git says 32 since "24 hours ago", but my own 2026-09-04 12:49 review of this repo closed at 06e5858, so the UN-REVIEWED window is 25. `git merge-base --is-ancestor 06e5858 HEAD` = YES, so the baseline slot is reachable and its count is live.

CARRIED BASELINE, TAKEN FROM THE STORE AND NOT FROM CONTINUITY (the 2026-09-05 solitaire self-correction's mechanical close, firing for the third repo running): the latest unsuperseded anneal-memory slot is diogenes-20260904-124716-6dd7370dfdab, STILL OPEN (12) at HEAD 06e5858 — which is EXACTLY this window's base. Checked for a SUPERSEDES token against it: none.

REVIEW STARTED, NOT FINISHED — if no COVERAGE line for anneal-memory exists in this run, THIS RUN DIED and the finding list is PARTIAL.
  Tags: anneal-memory, code_review, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260905-045550-1477bb83c354
  Time: 2026-09-05T04:55:50  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: HIGH: anneal_memory/store.py:4498 — THE DURABLE AUDIT-FAILURE COUNTER IS PERMANENTLY LOST WHENEVER THE FAILURE HAPPENS INSIDE SOMEONE ELSE'S TRANSACTION, BECAUSE TWO OF THE THREE FLUSH POINTS THE CODE PROMISES DO NOT EXIST. `_persist_audit_health` correctly refuses to commit inside a caller's open transaction and leaves the delta pending — but nothing ever flushes it afterwards, so the count dies with the process and `status().audit_write_failures` reports 0 on a store that really lost an audit entry. That is the exact defect this window's headline feature was built to close, reintroduced through the batched path.

[quoted, store.py:4496-4499]
            # A caller owns an open transaction — committing here would
            # publish their uncommitted DML. Leave the delta pending; the next
            # flush (another failure, batch exit, or close) writes it.
            return

MECHANISM, DERIVED NOT ASSUMED: `grep -n "_persist_audit_health" anneal_memory/*.py` returns exactly ONE call site — store.py:4761, inside the post-commit audit-failure handler. There is no flush at `_batch()` exit and none in `close()` (I read `close()` at store.py:5738: it checks `_closed`, refuses inside `_batch`, then goes straight to `self._conn.close()`). So of the three flush points the comment names, only "another failure" exists, and it only helps if that later failure happens to land outside a transaction.

RUN-VERIFIED, both directions, on `git archive HEAD` trees under /tmp/dio_am5. Scenario = the repo's OWN measured case, taken verbatim from tests/test_audit.py::test_a_health_write_never_destroys_a_batch_that_SUCCEEDS: inside `store._batch()`, `record()` DEFERS its audit write while `save_continuity()` is not batch-aware and logs immediately, so its audit failure lands with `in_transaction` True. I added the one realistic extra beat — the sink HEALS before the batch exits (a transient ENOSPC; the disk is freed):

  HEAD (unpatched)            pending delta at close = 1 | AFTER REOPEN write_failures = 0 | last_failure = None | metadata row ABSENT
  WITH THE PRESCRIBED FLUSH   pending delta at close = 0 | AFTER REOPEN write_failures = 1 | last_failure = "save_continuity: OSError(28, 'No space left on device') [dropped before audit seq 0] at 2026-09-05T08:50:17Z"

THE FIX, EXECUTED: `self._persist_audit_health()` at the end of `_batch()` (after the deferred-audit replay loop, where nobody owns a transaction) and once more in `close()` immediately before `self._conn.close()`. Both inserted into a full copy at HEAD; the probe inverts as above and the FULL SUITE STAYS GREEN under the patch — 1848 passed, 5 skipped, 3 subtests, identical to unpatched HEAD.

WHY 1848 TESTS DO NOT SEE THIS, AND IT IS THE SAME SHAPE THE WINDOW SPENT ITSELF ON: the guard class written for exactly this field, `TestDegradedAuditHealthReachesEveryTransport`, loses its writes through `_store_that_lost_audit_writes` (tests/test_audit.py:1675), which calls plain UNBATCHED `store.record()` in a loop — the one path where the flush always succeeds. Its sibling `test_a_health_write_never_destroys_a_batch_that_SUCCEEDS` DOES drive the batched path, and asserts only `total_episodes == 1`: it grades whether the caller's DML survived and never asks whether the count landed. The batch case is exercised and the batch case's own property is not.

BLAST RADIUS — three shipped surfaces state the property this breaks. README.md:218 "**`audit_write_failures` is durable and lifetime-scoped** ... so `anneal-memory status --json` from a cron job reports losses caused by a process that has already exited". types.py:126 "⛔ LIFETIME-SCOPED AND DURABLE, not process-local — the distinction is the whole value of the field." CHANGELOG.md:33 "It is persisted with the failure count, so the position outlives the process that saw it." All three are true on the unbatched path and false on the batched one, and `validated_save_continuity` — the canonical wrap pipeline — is batched.

⚠ SCOPE, STATED NARROWLY: this is not "every batched failure is lost". If the sink is still broken when `_batch()` exits, the deferred audit replays, fails again OUTSIDE the transaction, and that handler flushes both deltas — I measured that case landing correctly at 2. The loss needs the last audit failure of the process to be one the guard deferred, which a transient sink failure or a batch with no deferred audit writes produces. [prescription: run]
  Tags: anneal-memory, code_quality, code_review
```

---

```
[laptop/flow]   ID: diogenes-20260905-045609-0b40ba6b93ef
  Time: 2026-09-05T04:56:09  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: MEDIUM: README.md:210 — THE CEILING CENSUS THIS WINDOW RAN EDITED THE LINE TWO ABOVE AND LEFT THE EXACT WORDING IT HAD JUST RULED WRONG STANDING IN THE SHIPPED PACKAGE DESCRIPTION. README:210 still reads "a top-tier (3x) carry surfaces an assisted 'graduate out to a stable home or retire' warning".

The gate it describes is `continuity.py:2708`: `if cf.max_level_reached >= 3 and not cf.provenance`. A 12x or 18x carry trips it and is not 3x.

THE SAME WINDOW SAID SO IN WRITING. `continuity.py:2696`, added at `f6cf04a`: "The prior wording before that was "top-tier (3x)", which called a 12x pattern 3x on every wrap. Stating the PREDICATE avoids both, and cannot drift when a tier is renamed or MIN_PROVEN_LEVEL moves." The warning string was changed accordingly — `git diff 06e5858..HEAD` on continuity.py shows `"{len(graduate_out)} Proven-tier pattern(s) were carried forward"` -> `"{len(graduate_out)} pattern(s) at 3x or higher were carried forward"`.

AND THE CENSUS TOUCHED THIS FILE, TWO LINES UP. The README diff for this window is 12 lines across 6 hunks; one of them rewrites README:208 from "A 2x/3x graduation that fails either check above demotes (3x→2x, 2x→1x)" to "demotes **by one level** — from *any* level, not just from 3x; there is no ceiling (`4x` → `3x`, `12x` → `11x`)". README:298 went `(1x→3x)` -> `(1x→Nx)`, README:431 gained "-> ... , no ceiling", and docs/architecture.md got two more. Line 210 sits between two edited lines and was not read.

`grep -rn "top-tier" $(git ls-files | grep -v '^project_memory/')` returns six hits; the other five are correct or historical — CHANGELOG.md:1171 states the predicate ("A **top-tier (`max_level_reached >= 3`) carry**"), CHANGELOG:586/916 are dated release records, continuity.py:1005 and :2683 say "top-tier carry" with no level attached. README:210 is the only site that attaches a number to the predicate.

Not DRIFT by rule: markdown has no code half, so it is a finding at its real severity. MEDIUM rather than LOW because `readme = "README.md"` makes this the package description embedded in PKG-INFO, so it is the copy an adopter reads on PyPI, and because the enumeration-not-rule class is what three commits in this window exist to eliminate.

THE FIX: state the predicate — "a carry at 3x or higher" — matching the warning the reader will actually see. [prescription: reasoned]
  Tags: anneal-memory, code_review, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260905-045626-dd80a4b9dc95
  Time: 2026-09-05T04:56:26  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: MEDIUM: pyproject.toml:104 — "EVERY PATTERN IS ANCHORED" IS FALSE OF THE LIST DIRECTLY BENEATH IT, AND THE ONE UNANCHORED ENTRY DEMONSTRABLY RE-ADMITS A `project_memory/` PATH INTO THE SDIST — the precise hole the paragraph exists to warn about.

[quoted, pyproject.toml:104-107]
# ⚠ EVERY PATTERN IS ANCHORED WITH A LEADING SLASH ON PURPOSE. These are
# gitignore-style patterns: an unanchored ``README.md`` matches at ANY depth and
# would have re-admitted ``project_memory/testbed/README.md`` — the fix quietly
# reopening the hole it closed. Verified by building, not by reading.

Ten of the eleven `include` entries are anchored. `pyproject.toml:123` is `    "tool-integrity.json",` — no leading slash. `project_memory/testbed/README.md` is a real tracked file, so the paragraph's own example is not hypothetical.

RUN-VERIFIED against hatchling's real file-selection API, in a `--no-hardlinks` clone at /tmp/dio_am5/clone. I planted `project_memory/testbed/tool-integrity.json` (a copy of the root manifest), committed it, and asked `SdistBuilder.recurse_included_files()`:

  project_memory paths admitted into the sdist: ['project_memory/testbed/tool-integrity.json']

THE FIX, EXECUTED: change the entry to `"/tool-integrity.json"`. Re-ran the same API on the same clone with the hostile file still planted —

  project_memory leak now : []
  root copy still ships   : True
  package copy still ships: True

⛔ AND THE COMMENT'S STATED REASON FOR LEAVING IT UNANCHORED IS ITSELF WRONG. pyproject.toml:91 says "tool-integrity.json  matches BOTH the package copy ... and the byte-identical repo-root copy". It does not need to: `/anneal_memory` already carries `anneal_memory/tool-integrity.json`, which the run above confirms — both copies still ship under the anchored form. The unanchored pattern buys nothing and costs the anchoring invariant.

⚠ NOT A LIVE LEAK, AND SAY SO PLAINLY. `tests/test_packaging.py` caught my planted file in both of its assertions (`2 failed, 1 passed`), and went green again under the anchored fix with the probe files still present. The defence-in-depth worked. What is defective is the config-level claim: a maintainer who reads line 104 and adds an unanchored pattern believes an invariant that the file itself already breaks, and their leak is then caught only by a test that must be run with `hatchling` installed — which it was not, in CI, until this same window.

Not DRIFT-eligible: pyproject.toml is neither Python nor markdown, and its config lines changed in this window (the entire sdist block is new), so DRIFT's "code unchanged" half cannot be established. FINDING by rule. [prescription: run]
  Tags: anneal-memory, code_review, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260905-045654-48508a9d4e34
  Time: 2026-09-05T04:56:54  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: LOW: pyproject.toml:99 — THE DISCLOSURE RECORD LISTS `.gitignore` AS DROPPED FROM THE SDIST; IT SHIPS, AND THE PROJECT'S OWN GATE SAYS SO.

[quoted, pyproject.toml:99-100]
# Dropped: project_memory (the reported site), .github, .gitignore, Dockerfile,
# scripts (contributor git hooks), glama.json (third-party directory metadata),

MEASURED BY BUILDING, not by reading the config: `python -m build --sdist --no-isolation` in a clean venv (hatchling 1.32.0) against `git archive HEAD` produced `anneal_memory-0.9.10.dev0.tar.gz`, 986,180 bytes, 69 files. `tar tzf … | grep gitignore` returns `anneal_memory-0.9.10.dev0/.gitignore`. (The headline claim of the same block DOES hold: `project_memory` count in the tarball is 0, down from the 39 files / 1,436,684 bytes measured at 06e5858.)

⛔ THE TWO RECORDS OF ONE DECISION DISAGREE, AND THE OTHER ONE IS RIGHT. tests/test_packaging.py's `EXPECTED_SDIST_TOP_LEVEL` carries `.gitignore` with the correct reason attached — "forced by hatchling regardless of `include` — see _sdist_distribution_paths" — and `_sdist_distribution_paths` explains the mechanism: `get_default_build_data()['force_include']` is added unconditionally at build time and bypasses `include` entirely, "pyproject.toml, the ``readme =`` target, the license, and every VCS ignore file". So the test knows; only the config comment does not. `pyproject.toml` itself ships for the same reason and appears in neither the kept nor the dropped list.

⚠ THE SHARP PART IS THE CLOSING SENTENCE OF THE SAME BLOCK: pyproject.toml:107 reads "Verified by building, not by reading." A build falsifies one of its own entries. Nothing hazardous ships — a `.gitignore` is not a disclosure risk — but this block is the repo's written record of what a public, effectively unretractable artifact contains, and one line of it was not derived.

THE FIX: move `.gitignore` (and `pyproject.toml`) out of "Dropped" into a third clause naming them as hatchling force-includes that `include` cannot control, matching the wording test_packaging.py already uses. [prescription: reasoned]
  Tags: anneal-memory, code_review, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260905-045654-49d1613a9773
  Time: 2026-09-05T04:56:54  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: LOW: anneal_memory/store.py:1089 — A COMMENT BLOCK WAS LANDED ON TOP OF THE WRONG DEFINITIONS: it explains the two audit-health metadata keys and sits above the two SQLite result-code constants, with a second comment spliced in between it and the code it belongs to.

[quoted verbatim, store.py:1089-1101]
# Durable degraded-audit-health keys in the ``metadata`` table. Deliberately
# NOT in ``_DEFAULT_METADATA``: an absent row is a store that has never lost an
# audit write, which ``_seed_audit_health`` already reads as zero, and seeding
# a "0" into every database on earth to say "nothing has gone wrong yet" is
# storage spent on the absence of news.
# SQLite PRIMARY result codes for lock contention. The low byte of any
# extended code is its primary code, so these two cover every SQLITE_BUSY_*
# and SQLITE_LOCKED_* variant, including ones added after this was written.
_SQLITE_BUSY = 5
_SQLITE_LOCKED = 6

_AUDIT_FAILURES_KEY = "audit_write_failures"
_AUDIT_LAST_FAILURE_KEY = "audit_last_failure"

Two independent comment blocks are concatenated with no blank line, so the reader's eye takes all eight lines as one preamble to `_SQLITE_BUSY`. The `_DEFAULT_METADATA` argument — which is about `_AUDIT_FAILURES_KEY` / `_AUDIT_LAST_FAILURE_KEY` — is stranded five lines and one blank line away from its subject, and reads as an assertion about two integers that have nothing to do with the metadata table. Both blocks are individually TRUE; the reasoning I checked holds (`_seed_audit_health` at store.py:4408 does read an absent row as 0, and BUSY=5 / LOCKED=6 are the correct primary codes).

Two definitions arrived in the same commit and one comment was inserted ahead of the other rather than beside it. No runtime effect.

Not DRIFT: `codeshape.py 06e5858 anneal_memory/store.py` returns exit 1, CODE CHANGED — FINDING by rule.

THE FIX: move the `_AUDIT_*_KEY` block down to sit immediately above its two constants and put a blank line between the two comment blocks. [prescription: reasoned]
  Tags: anneal-memory, code_review, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260905-045710-3cc2282ac426
  Time: 2026-09-05T04:57:10  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: LOW: anneal_memory/store.py:84 — THE DOCSTRING NAMES THE DISCRIMINATOR THE INLINE COMMENT TEN LINES BELOW IT EXPLICITLY FORBIDS, INSIDE THE FUNCTION THAT WAS REWRITTEN THIS WINDOW TO MAKE EXACTLY THAT DISTINCTION.

[quoted, store.py:83-86 — the docstring]
    both ``OperationalError``. The result-code name is the real discriminator
    and arrived in Python 3.11, so it is guarded — ``requires-python`` is >=3.10.

[quoted, store.py:92-99 — the comment immediately below, added this window]
    # ⛔ CLASSIFY BY PRIMARY RESULT CODE, NOT BY A NAME ALLOWLIST.
    # This listed three extended names and returned False for every other
    # one ... A hand-kept list of extended codes is the same shape as a
    # hand-kept list of anything else ... The low byte of the extended code
    # IS the primary code, which is the stable classification SQLite documents.
    errorcode = getattr(cause, "sqlite_errorcode", None)

The docstring sentence is inherited verbatim from the two pre-window copies in cli.py and server.py, both of which read `getattr(cause, "sqlite_errorname", None)` and matched against a three-name tuple. `git diff 06e5858..HEAD` shows both of those deleted and the surviving definition switched to `sqlite_errorcode`; `grep -n "sqlite_errorname" anneal_memory/` now returns nothing. The docstring's clause travelled with the merge and was not re-read.

⚠ WHAT SURVIVES AND WHAT DOES NOT, STATED PRECISELY, because half the sentence is still true: `sqlite_errorcode` arrived in Python 3.11 alongside `sqlite_errorname`, so the guarding rationale and the >=3.10 fallback are correct as written. Only the word "name" is wrong — and it is wrong in the direction that matters, because a name allowlist is the specific defect codex filed against this function on 2026-09-04 and the whole reason it was rewritten.

The function is otherwise sound: `_SQLITE_BUSY = 5` / `_SQLITE_LOCKED = 6` are the correct primary codes and `errorcode & 0xFF` is the documented way to reach them. This is the docstring, not the behaviour — but the docstring is what a reader hits first, and it currently argues for the approach the body was rewritten to abandon.

Not DRIFT: `codeshape.py 06e5858 anneal_memory/store.py` returns exit 1, CODE CHANGED — FINDING by rule.

THE FIX: "The primary result CODE is the real discriminator" — one word, and the two records inside one function stop disagreeing. [prescription: reasoned]
  Tags: anneal-memory, code_review, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260905-045758-7a0bdd47a350
  Time: 2026-09-05T04:57:58  Agent: diogenes  Type: observation
  Source: laptop_overnight_review
  Content: STILL OPEN (7), TOTAL AT HEAD 5b791728 — anneal-memory, 2026-09-05. 6 filed this window (1 HIGH / 2 MEDIUM / 3 LOW), 1 carried. Filed separately: 0 DRIFT (not in this count) — and that is a RULE OUTCOME, not a judgement: `codeshape.py 06e5858` returned exit 1, CODE CHANGED, on every Python subject I filed against (anneal_memory/store.py) and on the four other Python files in the window I checked (audit.py, cli.py, tests/test_audit.py, tests/test_integrity.py). README.md is markdown and can never be DRIFT-eligible by rule, so no check was run on it. pyproject.toml is neither Python nor markdown and its config lines changed in this window, so DRIFT's "code unchanged" half cannot be established — FINDING, by the rule that resolves the ambiguous case to the count. ZERO SELF-CORRECTIONS.

ARITHMETIC: prior slot diogenes-20260904-124716-6dd7370dfdab, STILL OPEN (12) at HEAD 06e5858, which is EXACTLY this window's base (`git merge-base --is-ancestor 06e5858 HEAD` = YES). MINUS 11 closed, PLUS 6 new = 7. Baseline taken FROM THE STORE and checked for a SUPERSEDES token (none) — the 2026-09-05 solitaire self-correction's mechanical close, third repo running.

⚠ WINDOW NOTE: git says 32 commits since "24 hours ago" and the dispatch said 31. The UN-REVIEWED window is `06e5858..HEAD` = 25 commits / 21 files / 2,682 insertions, because my own 12:49 review of this repo yesterday closed at 06e5858. I reviewed the 25.

⭐⭐ ELEVEN OF TWELVE CLOSED — the largest remediation this repo has had from me — AND I RE-DERIVED EVERY ONE rather than reading the commit messages that claim them. The levelcap census reached all three README sites (`grep` at :208 now "by one level ... no ceiling", :298 `(1x→Nx)`, :431 "no ceiling") plus two in docs/architecture.md. `_is_write_lock_contention` is ONE definition in store.py imported by both surfaces, and the `sqlite_errorname` allowlist is gone entirely. server.py's `assert isinstance` is an explicit `raise TypeError`. graduation.py:48's stale coordinate was removed rather than corrected, with the reason written in. The transport guard's substring grep is replaced by tests that cross a PROCESS boundary. The ladder gate now demands a retrospective cue, not just a backtick pair. `_RESERVED_AUDIT_KWARGS` is DERIVED — I ran `inspect.signature` at HEAD and it returns all six collision-capable names. project_memory/CLAUDE.md's counts are gone, replaced by an Orientation table of commands, and I executed three of its claims: `from anneal_memory import prepare_wrap_package` really does raise ImportError, continuity.py:947 really does say "the marker KINDS are a closed set", and cli.py:2894 really does declare `aliases=["recall"]`.

⭐ AND THE PACKAGING FIX WAS VERIFIED FROM THE ARTIFACT, NOT THE CONFIG: two clean-venv sdist builds. At 06e5858 it was 1,436,684 bytes / 39 project_memory files; at HEAD, 986,180 bytes / 69 files / project_memory count ZERO.

⛔ THE ONE CARRIED: `anneal_memory/graduation.py:98` — `_BARE_GRADUATION_RE` is still `r"\|\s*([23])x\s*\((\d{4}-\d{2}-\d{2})\)"`, so the bare-graduation sunset path cannot see a 4x+ line. Re-derived by grep at HEAD, unchanged. It is DELIBERATE and documented at graduation.py:68 ("⚠ KNOWN, DELIBERATE ASYMMETRY"), and it stays in the count because the count is open defects at HEAD, not un-triaged ones.

⚡ THE NIGHT'S SHAPE, AND IT INVERTS LAST NIGHT'S ON THIS REPO. Yesterday the runtime held and the instruments did not. Tonight the instruments are the strongest part of the window — I attacked the schema guard, the packaging gate, the derived kwargs set and the rotation recovery hard and every one of them held under measurement — and the HIGH is back in the RUNTIME: the durable audit-failure counter, the window's headline feature, is structurally lost on the batched wrap path because two of the three flush points its own comment promises do not exist. The guard written for that field exercises only the unbatched path. Five of the six findings are a sentence that describes a design more complete than the code delivers, and in the HIGH's case that gap is a lost record.
  Tags: anneal-memory, code_review, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260905-045931-abf4775f1470
  Time: 2026-09-05T04:59:31  Agent: diogenes  Type: observation
  Source: laptop_overnight_review
  Content: COVERAGE: anneal-memory 2026-09-05, HEAD 5b791728 **== origin/main VERIFIED AGAINST THE LIVE REMOTE** (`git ls-remote origin HEAD` answered 5b79172843c851491e55d30f2b11859282f535f3 at start AND at finish, not a cached tracking ref). `git status --porcelain` EMPTY at start and EMPTY at finish, HEAD unchanged — the repo was not touched: everything read-only or under /tmp/dio_am5 (two `git archive HEAD` trees, one patched full copy, a `--no-hardlinks` clone with hostile files planted, a clean venv with hatchling, one built sdist). ~/Documents/anneal-memory is a SYMLINK — checked first, tenth repo running. Window `06e5858..HEAD` = 25 commits / 21 files / 2,682 insertions / 187 deletions.

RE-DERIVED BY RUNNING, NOT CARRIED FORWARD:
· FULL SUITE at HEAD in a clean archive tree — **1848 passed, 5 skipped, 3 subtests, 22 warnings** (was 1822 at my 12:49 review). I read the OUTPUT, not the exit code, because my own `${PIPESTATUS}` capture came back EMPTY — the 09-05 levain near-miss firing correctly one repo later. `-rs` names all five skips: two `test_integrity.py` "not a git checkout" (an archive tree has no .git — an artifact of MY harness, not the repo) and three `test_packaging.py` hatchling skips.
· THE PACKAGING GATE RUN THE WAY CI NOW RUNS IT — installed hatchling in a venv and re-ran tests/test_packaging.py: **3 passed**, not skipped. So the `053af48` CI fix is real and the gate can actually fire.
· TWO SDIST BUILDS, from the artifact rather than the config: `python -m build --sdist --no-isolation` produced 986,180 bytes / 69 files with `project_memory` count **ZERO**, against the 1,436,684 / 39-files measurement at 06e5858. `.gitignore` and both `tool-integrity.json` copies confirmed present by `tar tzf`.
· THE HIGH, END TO END, IN BOTH DIRECTIONS. The repo's own batched scenario plus a healing sink: unpatched HEAD gives AFTER-REOPEN `write_failures = 0` with the metadata row ABSENT; with my prescribed flush at `_batch()` exit and in `close()`, AFTER-REOPEN `= 1` with the full `[dropped before audit seq 0] at …` string. Full suite under the patch: **1848 passed, 5 skipped** — identical, so the fix breaks nothing.
· THE PACKAGING ANCHORING DEFECT, MEASURED THROUGH HATCHLING'S REAL SELECTION API on a clone with `project_memory/testbed/tool-integrity.json` planted: leak present unanchored, `[]` anchored, both manifest copies still shipping either way, and test_packaging going 2-failed -> 3-passed across the fix.
· `_refuse_a_newer_schema` ATTACKED FOUR WAYS and it held every time: format_version 99 REFUSED on both the writer and the read-only path; an unparseable value OPENS on both (the documented deliberate divergence from the sidecars); and a refused store's `PRAGMA journal_mode` measured `delete` BEFORE and `delete` AFTER — the pragma-ordering fix is real, not asserted. `_SCHEMA_VERSION` read live = 1, so the CHANGELOG's "no existing store can trip this" holds.
· `_RESERVED_AUDIT_KWARGS` read live via `inspect.signature` at HEAD: exactly `['batch_aware','committed','event','method','payload','stacklevel']` — all six collision-capable names, `event`/`payload` now included.
· `codeshape.py 06e5858` as the DRIFT discriminator on five Python files (store.py, audit.py, cli.py, test_audit.py, test_integrity.py) — all exit 1.
· THE SLOT pre-flighted DRY through the LIVE `_parse_slot_count`/`extract_still_open` -> `'7'`, then re-parsed FROM THE STORE after writing, tag-filtered exactly as the router selects -> `'7'`, 8 blocks, 0 OTHER.

⭐ CANDIDATES THAT DIED ON DISK, AND THEY ARE THE REAL RESULT OF THE NIGHT:
· I predicted `anneal-memory verify` would still return a false TAMPERING verdict after a failed rotation, because the audit.py comment says so in the present tense and the fix only reaches the WRITE path. I reproduced a failed rotation with a broken `gzip.open` and asked the classmethod directly: `AuditTrail.verify(db).valid` = **True**, error None. The false verdict needs a subsequent append to exist at all, and any append triggers the new adoption. Internally coherent, and simply false.
· I predicted the docstring's "verbatim" quote of the sidecars was not verbatim. `crystal.py:399` and `spores.py:337` both carry the string exactly. False.
· The "three other early returns legitimately omit `skipped_lines` — they run BEFORE `skipped` exists" claim: construction sites are at audit.py 383/392/395, `skipped = 0` at 402. Exact.
· Three project_memory/CLAUDE.md claims executed rather than read: `prepare_wrap_package` raises ImportError; continuity.py:947 says what it is quoted saying; cli.py:2894 declares `aliases=["recall"]`.
· `note_write_failure()`'s returned seq: I checked whether "dropped BEFORE seq N" mis-names an existing entry. `_seq` is the NEXT seq (assigned at audit.py:186, incremented at :265 only after fsync), so the wording is exactly right.

⚠ A METHOD SLIP RECORDED BECAUSE NOTHING SHIPPED FROM IT, AND IT IS THE SAME CLASS TWICE IN ONE COMMAND. My repo-state check ran `cd ~/Briefcase/flow && … && git status --porcelain && git rev-parse HEAD` — the `cd` persisted across the `&&` chain, so I measured FLOW's tree and got a dirty porcelain and HEAD `068ac295`, which is not this repo's HEAD at all. In the SAME probe I filtered episodes by timestamp instead of by tag and got a HIGH count of 2, because wisp's 04:42 finding fell in the window. I re-ran both correctly — `pwd` first, and tag-filtered the way the router actually selects. **Neither wrong number reached a filed episode**, but both are the 09-04 close ("a probe whose OUTPUT is the evidence must be validated before its output is") and it is now 5 for 5.

⛔ NOT CHECKED — stated plainly, not implied:
· **NO CONCURRENCY WORK AT ALL, and this window is where it would bite.** `_persist_audit_health` uses `BEGIN IMMEDIATE` against another writer; I never ran two processes at the store. The repo's own `test_two_writers_cannot_make_the_lifetime_count_decrease` I READ and did not re-derive. My HIGH's scenario is single-process throughout. **Third night running that I have named this gap on this repo.**
· **Python 3.13 ONLY** — third night. The `sqlite_errorcode`/`sqlite_errorname` 3.10-fallback branch in `_is_write_lock_contention` is the exact code my LOW is about and it is UNEXERCISED here; nothing in this run touched a 3.10 or 3.11 interpreter, and CI is where that matrix lives.
· **NO MCP TRANSPORT.** `server.py` changed 45 lines including the `assert` -> `raise TypeError` conversion, and I never started the server. The durable-counter claim for a long-lived MCP Store — the half `_current_audit_failures` was written for — is REASONED from the code, not run.
· **NO mypy, NO ruff this run.** Both were clean at my 12:49 review; I did not re-derive either, and 2,682 insertions landed since.
· **THE ROTATION PATH IS STILL LARGELY MINE TO OWE.** I drove exactly one failed-gzip scenario. Seal, manifest, orphan dedup between .gz and .jsonl, retention cleanup and the `.tmp` sweep are all unexercised — fourth night that path has gone untouched.
· **~350 of tests/test_audit.py's ~2,650 lines read.** The file grew 818 lines this window and I opened five classes of it. test_store.py's 182 new lines I read only as a `grep` of its assert/def lines, not in full.
· **PROJECT-MEMORY PROSE: ~540 of the 539+ changed lines in next_steps.md and diogenes_20260904.md are UNAUDITED.** I read project_memory/CLAUDE.md closely and executed its claims; the other two I sampled for numeric claims only. CHANGELOG.md gained 160 lines and I verified its `_refuse_a_newer_schema` paragraph by running and its audit-counter paragraph by measurement — the rest I read without checking.
· NO WHEEL BUILT, no install, no smoke test. `wrap-cancel` never invoked — fourth night.
  Tags: anneal-memory, code_review, semantic
```
