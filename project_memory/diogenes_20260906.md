# DIOGENES OVERNIGHT — anneal-memory, 2026-09-06

**Source selector:** `episodic.py read --since 24h --tags anneal-memory  (filtered to Agent: diogenes at 2026-09-06*)`
**Stated count: 14 episode(s).** Severity: LOW 1 · MEDIUM 4 — 5 of 14 episode(s) carry a severity; the other 9 are COVERAGE 1 · COVERAGE-OPEN 1 · SELF 6 · STILL OPEN 1
**STILL OPEN: 6**

> ⚠ **UNTRIAGED AND VERBATIM.** Generated by `scripts/route_diogenes.py` from the episodic store without judgement. Nothing here has been checked against disk except where a line says so explicitly. Diogenes' own `[prescription: ...]` tags are his assessment, not a ruling.

---

```
[laptop/flow]   ID: diogenes-20260906-022047-51a94ae68cad
  Time: 2026-09-06T02:20:47  Agent: diogenes  Type: observation
  Source: laptop_overnight_review
  Content: COVERAGE-OPEN: anneal-memory 2026-09-06, HEAD a12e66ab30164e52d6c48e73adc1a958389277d1 (== origin/main? YES -- `git ls-remote origin HEAD` answered a12e66ab against the LIVE remote, not a cached tracking ref), `git status --porcelain` EMPTY. Window 5b79172..HEAD = 17 commits (the dispatch said 16; `git log --since="24 hours ago"` lists exactly the same 17, and 5b79172 is my own 2026-09-05 04:59 review HEAD, so the window tiles cleanly onto the last slot with nothing between). Diffstat 10 files / 1673 insertions / 41 deletions, concentrated in anneal_memory/store.py (+315), tests/test_audit.py (+514), project_memory/ (+501), pyproject.toml (+44). `~/Documents/anneal-memory` is a SYMLINK to `~/Briefcase/anneal-memory` -- checked first. REVIEW STARTED, NOT FINISHED -- if no COVERAGE line for this repo exists in this run, THIS RUN DIED and the finding list is PARTIAL.
  Tags: anneal-memory, code_review, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260906-023004-68e5ea04fdd3
  Time: 2026-09-06T02:30:04  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: MEDIUM: anneal_memory/store.py:6064 — THE FLUSH POINT ADDED TO ``close()`` CARRIES A WRITTEN PROMISE THAT IT CANNOT TURN A CLEAN CLOSE INTO A RAISING ONE, AND TWELVE COMMITS LATER IN THE SAME WINDOW IT CAN. When ``_persist_audit_health`` re-raises ``SystemExit``, ``close()`` never reaches ``self._conn.close()`` and never sets ``self._closed`` — so the sqlite connection is leaked and the store is left believing it is open, on the ONE path the re-raise's own rationale singles out as the one it changes.

[quoted, store.py:6059-6064 — the comment on the call]
        # ⛔ FLUSH POINT 3 OF 3 — THE LAST ONE THIS PROCESS WILL EVER GET.
        # A delta still pending here is one no failure handler and no batch
        # exit reached, and the connection is about to go away. Swallows its
        # own errors (see the method), so it cannot turn a clean close into a
        # raising one. Deliberately BEFORE the boundary block: a flush after
        # ``self._conn.close()`` would have no connection to write through.
        self._persist_audit_health()

[quoted, store.py:4726-4736 — the comment that falsified it, in the method it points to]
            # ⚠ THE WRAP IS STILL PROTECTED, and that is why this is safe HERE
            # and not in ``_batch()``: on the batched path this re-raise is
            # caught by ``_batch()``'s own post-commit ``except BaseException``,
            # so a committed wrap's staged sidecars are never unlinked. What
            # changes is the ``close()`` path, where nothing is staged and the
            # caller genuinely is trying to exit.
            if isinstance(exc, SystemExit):
                raise

COMMIT ORDER, DERIVED NOT ASSUMED: ``git log -S "FLUSH POINT 3 OF 3"`` -> 16dab2b, position 2 of 17 in this window. ``git log -S "isinstance(exc, SystemExit)"`` -> ae9eeec, position 14 of 17. The promise and its falsification are the same day, same file, same author, and the comment still says "see the method" for a property the method no longer has.

RUN-VERIFIED, ALL THREE ARMS, in a `git archive HEAD` tree with a proxy connection raising from ``commit()`` while a delta is pending:
  KeyboardInterrupt    raised=None            _closed=True   conn_open=False
  SystemExit(0)        raised=SystemExit(0)   _closed=False  conn_open=True
  OSError              raised=None            _closed=True   conn_open=False
SystemExit is the ONLY one of the three that leaks, so the discrimination is exact and this is not a general "close can raise" claim. ``in_transaction`` was False in every arm — the rollback half of the widening works; it is the ordering in ``close()`` that does not.

⛔ AND ``close()``'s OWN DOCSTRING DOES NOT LIST IT. The ``Raises:`` block names exactly two: ``StoreError`` (called inside ``_batch``) and ``StoreDatabaseError`` (the sqlite close failed), and it is the second that says "On failure, ``self._closed`` stays ``False`` — the connection is in an indeterminate state the caller can inspect or retry closing". A third escape now produces that same indeterminate state from a different cause, and the docstring says the connection failed to close when in fact it was never attempted. The scenario the re-raise was written for is the live one: a long-lived MCP server whose SIGTERM handler calls ``sys.exit(0)``.

THE FIX — put the close in a ``finally`` so the re-raise keeps its intended semantics without costing the connection:
        try:
            self._persist_audit_health()
        finally:
            with self._db_boundary("close"):
                self._conn.close()
            self._closed = True
EXECUTED in the scratch tree and it inverts in both directions — SystemExit still propagates (the complement L3 ruling is preserved) AND `_closed=True`, `conn_open=False` on all three arms. Suite on the patched tree 1862 passed / 5 skipped, IDENTICAL to an unpatched `git archive HEAD` control tree measured in the same way (the repo's own working tree reads 1864/3; two tests skip outside a git worktree, which is why the control was built rather than the repo figure reused). [prescription: run]
  Tags: anneal-memory, code_quality, code_review
```

---

```
[laptop/flow]   ID: diogenes-20260906-023027-ed08dfdd0081
  Time: 2026-09-06T02:30:27  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: MEDIUM: anneal_memory/store.py:1663 — THE VERSION STAMP'S "NEVER LOWER IT" PROPERTY IS CLAIMED AS STRUCTURAL AND IS NOT: A ``format_version`` THAT DOES NOT PARSE AS AN INTEGER CASTS TO 0 AND IS SILENTLY REWRITTEN TO "1" — destroying exactly the evidence the same comment says the strictly-less-than predicate exists to protect, and overruling the guard's own documented decision to leave that value alone.

[quoted, store.py:1663-1669 — the stamp]
        self._conn.execute(
            "INSERT INTO metadata (key, value) VALUES (?, ?) "
            "ON CONFLICT(key) DO UPDATE SET value = excluded.value "
            "WHERE CAST(metadata.value AS INTEGER) "
            "    < CAST(excluded.value AS INTEGER)",
            ("format_version", str(_SCHEMA_VERSION)),
        )

[quoted, store.py:1652-1654 — the claim]
        # A schema generation only ever advances, so a strictly-less-than
        # predicate is the honest one and the no-lower property is structural
        # rather than a rule someone has to remember.

[quoted, store.py:4448-4452 — ``_refuse_a_newer_schema``'s ruling on the same field, which the stamp overturns]
        · value UNPARSEABLE → left alone. The sidecars refuse here, but a
          sidecar is a cause and this is the user's memory; locking someone
          out of every episode they own over a garbled metadata string is a
          worse outcome than proceeding, and the audit trail is a separate
          integrity surface.

MEASURED IN A `git archive HEAD` TREE, planting each value and reopening (guard verdict, SQLite's own CAST, and the marker after the open):
  planted='2'        CAST->2   guard=StoreError   after='2'
  planted='10'       CAST->10  guard=StoreError   after='10'
  planted='2.0'      CAST->2   guard=proceeds     after='2.0'
  planted='1'        CAST->1   guard=proceeds     after='1'
  planted='v2'       CAST->0   guard=proceeds     after='1'     <-- DESTROYED
  planted='garbled'  CAST->0   guard=proceeds     after='1'     <-- DESTROYED
  planted=''         CAST->0   guard=proceeds     after='1'     <-- DESTROYED
The property holds over values SQLite's CAST reads as their true integer and fails over every other one. A marker with a leading digit ('2.0') survives; one without ('v2') does not — so the exposure is precisely a foreign or corrupt marker, which is the case that matters.

⚡ THE OUTCOME IS THE ONE THE COMMENT ARGUES AGAINST, VERBATIM. Its own account of the predicate it replaced reads "it DESTROYED THE EVIDENCE that a newer binary had been here, turning a one-shot race into a permanent defeat of the guard." A store marked 'v2' by some future or foreign writer reads '1' after ONE open by this version, and every open after that is waved through by a marker this process invented. That the guard has an explicit, deliberately-reasoned branch for the unparseable case is the repo asserting the case is expected — it is not a hypothetical I supplied.

THE FIX — keep it a single atomic statement (a SELECT-then-UPDATE would reintroduce the very ``spore-773`` race the stamp is written against) and add a conjunct that the stored text is exactly its own canonical integer rendering:
            "  AND CAST(CAST(metadata.value AS INTEGER) AS TEXT) "
            "      = TRIM(metadata.value)"
EXECUTED in a second archive tree. Both directions invert: 'v2' / 'garbled' / '' are now PRESERVED, while the legitimate raise still fires ('0' -> '1') and a genuinely newer '2' is still refused by the guard. Suite 1862 passed / 5 skipped, identical to the unpatched archive control. [prescription: run]
  Tags: anneal-memory, code_quality, code_review
```

---

```
[laptop/flow]   ID: diogenes-20260906-023041-ea32b03aed96
  Time: 2026-09-06T02:30:41  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: SELF-CORRECTION: diogenes-20260906-023027-ed08dfdd0081 (MEDIUM, store.py:1663, the format_version stamp) contains a MISQUOTE inside a ``[quoted]`` block, which is a grounding-floor violation regardless of whether it changes the finding.

WHAT I WROTE: "The sidecars refuse here, but a sidecar is a cause and this is the user's memory".
WHAT store.py:4448-4449 ACTUALLY SAYS, re-read from disk: "The sidecars refuse here, but a
          sidecar is a cache and this is the user's memory".

`cause` should be `cache`. I retyped a line I had read minutes earlier instead of re-deriving it from the file — the exact failure mode the quoting rule exists to prevent, committed in the same episode where I filed against someone else's description drift.

THE FINDING IS UNAFFECTED AND STANDS. The quoted sentence is supporting context (the guard's rationale for tolerating an unparseable marker); the load-bearing evidence is the measured table, which came from running the code, and the four words that carry the argument — "value UNPARSEABLE → left alone" — are verbatim correct. Severity, mechanism, coordinate and prescription are unchanged.
  Tags: anneal-memory, code_review, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260906-023233-d47668120e1b
  Time: 2026-09-06T02:32:33  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: MEDIUM: anneal_memory/store.py:5238 — THE FIX FOR AN INCOMPLETE SAFETY-CONTRACT LIST LEFT IT INCOMPLETE, IN BOTH DIRECTIONS, BY THE LIST'S OWN STATED PREDICATE. This window added five names to ``_batch()``'s batch-aware roster and wrote a ⚠ paragraph about why omissions there are dangerous. Two more batch-aware methods are still missing, and a public write method appears in NEITHER list although the second one is worded as an exhaustive universal.

[quoted, store.py:5238-5239 — the defining predicate]
        **Batch-aware methods** (check ``_defer_commit`` and route
        audit events through :meth:`_audit_log`):

[quoted, store.py:5252-5258 — the paragraph this window added]
        ⚠ The last five were MISSING from this list until 2026-09-05 (L3
        complement) while being batch-aware in fact — each passes
        ``commit=not self._defer_commit`` and emits through
        ``_audit_log_after_commit`` with the default ``batch_aware=True``. The
        list is read as a safety contract, so an omission here reads as "not
        safe inside a batch" for a method that is, which is the more dangerous
        direction for a list like this to be wrong in.

[quoted, store.py:5260-5262 — the universal]
        All other write methods (``prune``, ``wrap_started``,
        ``wrap_cancelled``, ``save_continuity``, ``save_meta``)
        commit immediately and are NOT safe to call inside a batch —

DERIVED BY AST OVER ``Store``, not by grep proximity — every method walked for a ``_defer_commit`` attribute read and for the ``batch_aware=`` keyword on each ``_audit_log_after_commit`` call:

MISSING FROM THE BATCH-AWARE LIST, and both satisfy its stated first clause verbatim:
  upsert_pattern_history  (store.py:3353)  `if not self._defer_commit:` / `self._conn.commit()`
  seed_pattern_max_level  (store.py:3488)  `if not self._defer_commit:` / `self._conn.commit()`
They emit no audit event, so the parenthetical's SECOND clause is inapplicable — but the heading names two properties and the first holds, which is precisely the "reads as not-safe for a method that is" direction the new paragraph calls the more dangerous one.

IN NEITHER LIST, against a sentence that begins "All other write methods":
  set_section_schema      (store.py:4299)  unconditional `self._conn.commit()` inside its `_db_boundary`, then `_audit_log_after_commit(..., batch_aware=False)`
It is public, it writes ``metadata``, it commits immediately, and it is exactly the shape the second list enumerates. A caller consulting this contract about ``set_section_schema`` finds it in neither list and gets no answer from a paragraph that claims to cover the complement.

RUN-VERIFIED that the two omitted methods really are batch-aware rather than merely looking it — driven inside a live ``_batch()`` with a SECOND sqlite3 connection watching:
  DURING batch, seen by the other connection: episodes=0  pattern_history=0
  AFTER  batch:                               episodes=1  pattern_history=2
Both rows are invisible until the batch commits, so neither breaks the single-transaction invariant and both belong on the list.

THE FIX: add ``upsert_pattern_history`` and ``seed_pattern_max_level`` to the batch-aware list, and ``set_section_schema`` to the immediate-commit list. ⚠ The deeper answer is the one this list has now been wrong about twice in two days: a roster of method names hand-maintained beside the code it describes is the same shape as the name allowlist ``_is_write_lock_contention`` was rewritten to abandon, thirty lines into this very file. A test that walks ``Store`` with ``ast`` and asserts the two documented lists partition the methods that read ``_defer_commit`` would make the omission impossible rather than noticed — the repo already does exactly this in ``test_google_transport_bounds`` (``ast.walk`` over Call nodes) and in ``_RESERVED_AUDIT_KWARGS`` (derived from ``inspect.signature``). I did not write that test. [prescription: reasoned]
  Tags: anneal-memory, code_review, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260906-023547-213412857f05
  Time: 2026-09-06T02:35:47  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: LOW: CHANGELOG.md:28 — THE DURABLE RECORD WRITTEN SO A REFUSED SUGGESTION "DOES NOT RETURN" RESTS ON A CALLER CENSUS THAT IS SHORT BY ONE, AND THE OMITTED SITE IS THE BROADEST OF THE THREE. The conclusion is correct; the enumeration that supports it is not, in a block whose entire function is to be the standing rebuttal a future reviewer checks.

[quoted, CHANGELOG.md:26-31]
⛔ **Two same-round suggestions were REFUSED, with reasons, so they do not return.** A breadth seat
filed both `except BaseException` sites as HIGH and prescribed re-raising *everything* — that
prescription **reintroduces the data-loss defect the handler exists to close**, demonstrated by
mutation: applying it makes the wrap-protection test abort the run. The same seat filed the widened
lock-contention heuristic as risking an "infinite retry loop"; **there is no retry loop** — both
callers (`cli.py:270`, `server.py:712`) use the predicate to produce a better error message and then
stop, one with `sys.exit(1)`.

THERE ARE THREE CALLERS, not two. `grep -rn "_is_write_lock_contention" anneal_memory/` at HEAD returns, excluding the definition and the two import lines:
  anneal_memory/cli.py:270      `if _is_write_lock_contention(exc):`
  anneal_memory/cli.py:3345     `if not _is_write_lock_contention(exc):`   <-- ABSENT FROM THE RECORD
  anneal_memory/server.py:712   `if _is_write_lock_contention(exc):`

⚡ THE OMITTED ONE IS THE WIDEST-SCOPED AND ITS CONTROL FLOW IS THE INVERSE. It is the negated form, and its own comment describes it as the general boundary rather than a per-command one:

[quoted, cli.py:3327 and cli.py:3339-3341]
    # Dispatch to subcommand handler, behind the ONE contention boundary.
    # ⭐ It sits HERE, not at the `wrap_cancelled()` call site, because the
    # window is not specific to that command — every subcommand that writes has
    # it. Fixing the one site that was noticed is how the previous scoping got
    # written. One boundary, all subcommands.

THE CONCLUSION SURVIVES — I CHECKED THE THIRD SITE RATHER THAN ASSUMING IT BREAKS THE CLAIM. cli.py:3345 re-raises when the predicate is false and otherwise calls `_print_contention(...)` then `sys.exit(1)`. So all three sites message-and-stop and "there is no retry loop" is TRUE of every caller, not just the two named. This is an evidence defect, not a wrong verdict, which is why it is LOW.

⛔ WHY IT STILL MATTERS: the sentence's stated purpose is that the refusal not have to be re-litigated. A reviewer who returns to this claim and re-derives the census finds a site the record does not mention, whose form (`if not ...`) reads at a glance like a different control shape — and the natural conclusion is that the refusal was reached without seeing it. A rebuttal is only as durable as the census under it.

THE FIX: state the rule rather than the roster — "every caller of the predicate messages and stops; none retries" is true, checkable and cannot go stale when a fourth site is added, whereas a list of two coordinates was already wrong on the day it was written. (This is the same shape as the ``_batch()`` method-list finding filed tonight and as the name-allowlist ``_is_write_lock_contention`` itself abandoned thirty lines into store.py.) [prescription: reasoned]
  Tags: anneal-memory, code_review, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260906-023628-03bd9f03ca9c
  Time: 2026-09-06T02:36:28  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: SELF-CORRECTION (AMENDMENT, NOT RETRACTION): diogenes-20260906-023547-213412857f05 (LOW, CHANGELOG.md:28, the two-of-three caller census) is correct as filed and nothing in it is withdrawn. It is INCOMPLETE in a way that changes what "fixed" would mean, so I am recording the second site rather than leaving the finding to half-close.

THE SAME SHORT CENSUS IS IN A SECOND DURABLE SURFACE, and there it is explicitly labelled as disk-verified.

[quoted, project_memory/next_steps.md:99-100]
an "infinite retry loop" — **there is no retry loop** (verified on disk: `cli.py:270` prints and
`sys.exit(1)`; `server.py:712` returns a tool result), and its proposed `"schema" not in text`

Two coordinates named, three callers at HEAD, and `cli.py:3345` — the negated, widest-scoped one, the CLI's "ONE contention boundary" covering every writing subcommand — is absent from both records. The phrase "verified on disk" is doing real work here: it tells the next reader the enumeration was checked, so they will not re-check it.

⚡ THIS IS THE WINDOW'S OWN SELF-FILED CLASS, FIRED AT THE RECORD OF THE FIX. Commit e0abdb3 in this very window is titled "the durable census reached one of three README sites, which is this morning's MED". A correction applied to CHANGELOG.md alone would leave next_steps.md — the file the morning ritual actually opens — still naming two of three.

THE FINDING'S SEVERITY, MECHANISM AND VERDICT ARE UNCHANGED: all three callers message-and-stop, so "there is no retry loop" remains TRUE. The fix is unchanged in kind and now has two addresses: state the rule ("every caller messages and stops; none retries") in BOTH surfaces instead of a coordinate roster in either. Still LOW, still one defect, still counted once. [prescription: reasoned]
  Tags: anneal-memory, code_review, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260906-023651-2089c0b96261
  Time: 2026-09-06T02:36:51  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: SELF-CORRECTION: diogenes-20260906-023628-03bd9f03ca9c (the amendment to the caller-census LOW) cites the wrong coordinate for its second site.

WHAT I WROTE: `project_memory/next_steps.md:99-100`.
WHAT IS TRUE AT HEAD, re-derived with `grep -n` against the file: `project_memory/next_steps.md:117-118`.

MECHANISM, AND IT IS A NEW ONE FOR ME: I read that passage out of a `git diff | grep -n` pipeline, whose line numbers count POSITIONS IN THE GREP OUTPUT, not lines in the file. The number looked like a coordinate and was an artifact of my own probe. Every other coordinate I filed tonight came from a `grep -n` or `sed -n` against the file itself; this one came from a pipeline and I did not re-derive it before writing.

▶ THE MECHANICAL CLOSE, which generalises past this file: A LINE NUMBER PRODUCED BY A PIPELINE OVER `git diff` IS NEVER A FILE COORDINATE. `grep -n` numbers whatever stream it is handed. Re-derive every coordinate against the file at HEAD before it enters an episode — the same discipline as the 2026-09-04 close about files added in-window, one layer out: there the diff hid the coordinate, here the diff INVENTED a plausible one.

THE QUOTED TEXT ITSELF IS VERBATIM AND CORRECT — I re-read `sed -n '115,120p'` and the two lines match what I filed, so the evidence stands and only its address was wrong. The parent finding diogenes-20260906-023547-213412857f05 is unaffected (its coordinate, CHANGELOG.md:27-28, re-derived by `grep -n "infinite retry loop" CHANGELOG.md` -> 27, is correct). Severity, verdict and prescription unchanged; still ONE defect, still counted once.
  Tags: anneal-memory, code_review, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260906-023955-63d5825e3b60
  Time: 2026-09-06T02:39:55  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: MEDIUM: tests/test_audit.py:2050 — THE WINDOW'S HEADLINE HIGH IS PINNED BY A TEST THAT CANNOT GO RED WHEN THE GUARD IS REMOVED, AND WHICH NEVER LOOKS AT THE FILE THE HIGH IS ABOUT. The fix is real — I proved it end to end — but the property protecting a destroyed continuity file is unguarded against regression, and the test's own docstring records that it does not fail under the mutation and accepts it.

[quoted, tests/test_audit.py — the test's own admission]
        MUTATION-CHECKED, and the failure MODE is the point: narrowing the
        post-commit ``except BaseException`` in ``_batch()`` back to ``except
        Exception`` does not turn this test red — the interrupt escapes the
        ``with`` block and ABORTS the pytest run at that line. A caller has no
        more defence against it than the test runner does, which is the whole
        argument for containing it at the source.

[quoted, the same test's only assertions]
            assert store.status().total_episodes == 1
                "the committed episode did not survive — a post-commit "
                "interrupt was allowed to look like a failed batch"

It drives ``store._batch()`` DIRECTLY. It never calls ``validated_save_continuity``, and it never reads the continuity file. `grep -rn "KeyboardInterrupt\|SystemExit" tests/*.py` matched against continuity returns NOTHING — no test in the repo drives the canonical pipeline with an interrupt.

⚡ AND THE CLAIM IT LEAVES UNGRADED IS THE ONE THE CHANGELOG LEADS WITH: "a post-commit interrupt could destroy a committed wrap's continuity file ... the new continuity text is destroyed while the wrap row and the episodes' wrap assignments stay durable."

RUN-VERIFIED, BOTH ARMS, THROUGH THE REAL PIPELINE (`prepare_wrap` -> `validated_save_continuity`, in two `git archive HEAD` trees, with `_audit.log` raising KeyboardInterrupt during the deferred-audit replay). Wrap 1 saves OLD text normally; wrap 2 is interrupted while saving NEW text:

  ARM A — HEAD, fix present:
    continuity file exists: True   contains NEW: True   contains OLD: False
    staged sidecars left: []       wrap rows committed: 2
  ARM B — mutant, `except BaseException` -> `except Exception` in _batch():
    continuity file exists: True   contains NEW: False  contains OLD: True
    staged sidecars left: []       wrap rows committed: 2

⭐ THE FIX IS CONFIRMED LOAD-BEARING AND I AM SAYING SO PLAINLY: ARM B reproduces the described loss exactly — the new text is gone, the OLD text is back, the sidecars were UNLINKED rather than preserved, and the wrap row is committed anyway. That is `continuity.py:2396` firing with `db_committed` still False. This is the strongest positive result of the night and it is not the finding; the finding is that nothing in the suite would notice if it regressed.

⛔ THE DOCSTRING'S ARGUMENT IS FALSIFIED BY THAT ARM. It claims a red test is unavailable because the interrupt "ABORTS the pytest run". ARM B did not abort — `validated_save_continuity`'s own `except BaseException` catches the interrupt at the caller level, so a probe that wraps the CALL rather than the BATCH observes the outcome and continues. The discriminating assertion exists; it just is not at the coordinate the test chose. A test that cannot fail for the reason it was written is the class this repo graduated on 2026-09-05 (`a_gate_nothing_can_pass_is_not_a_gate`), fired at the guard for its own headline HIGH.

▶ AND IT IS THE SAME SHAPE, ONE LEVEL UP, AS THE DEFECT THIS WINDOW FIXED. My 2026-09-05 HIGH said of the sibling test: "it drives this exact batched scenario and then asserts ``total_episodes == 1`` — it grades whether the CALLER'S DML survived and never asks whether the count landed." The new test grades that the batch exited and the episode survived, and never asks whether the continuity file did. The scenario was exercised; the scenario's own property was not.

THE FIX: a test that calls `validated_save_continuity` with `_audit.log` raising `KeyboardInterrupt`, and asserts the continuity file on disk holds the NEW text. EXECUTED as described above and it discriminates cleanly in both directions without aborting the runner. I built and ran it in scratch trees; I did NOT add it to `tests/`. [prescription: run]
  Tags: anneal-memory, code_quality, code_review
```

---

```
[laptop/flow]   ID: diogenes-20260906-024021-9480a31c7532
  Time: 2026-09-06T02:40:21  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: SELF-CORRECTION: diogenes-20260906-023955-63d5825e3b60 (MEDIUM, the untestable guard for the wrap-destruction HIGH) carries an off-by-one subject coordinate.

WHAT I WROTE: `tests/test_audit.py:2050`.
WHAT IS TRUE AT HEAD: `tests/test_audit.py:2051` — `grep -n "def test_a_keyboardinterrupt_in_the_replay_cannot_look_like_a_failed_batch" tests/test_audit.py` returns 2051. Line 2050 is blank; 2049 is the section comment `# -- #4c: a post-commit BaseException must not reach the caller (09-05) --`.

Nothing else changes: the quoted docstring and assertions are verbatim, both measured arms stand, the severity and prescription are unchanged.

⛔ THE METHOD FAILURE IS THE POINT, AND THIS IS THE THIRD COORDINATE DEFECT I HAVE FILED TONIGHT — after `next_steps.md:99` (a grep-pipeline line number mistaken for a file line) and a misquoted word inside a `[quoted]` block. All three share ONE mechanism: I wrote the coordinate or the quote from what I had READ MINUTES EARLIER instead of re-deriving it at the moment of writing. The derived-vs-typed discriminator held on every night this week and broke three times in one night here, and the reason is visible in the transcript — tonight's findings each rest on a long measured probe, and by the time the probe finished the coordinate was a memory rather than a command.

▶ CLOSE, MECHANICAL, AND IT IS NARROWER THAN "BE MORE CAREFUL": THE COORDINATE IS THE LAST THING WRITTEN, NOT THE FIRST. Re-run `grep -n` for the subject symbol immediately before the episode is composed — not while reading the file, not while running the probe. A coordinate derived at read-time and used at write-time has been carried across an unbounded interval of other work, which is the same cached-state-presented-as-source-read defect I file against others.
  Tags: anneal-memory, code_review, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260906-024114-a81db15ebe6e
  Time: 2026-09-06T02:41:14  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: SELF-CORRECTION: diogenes-20260906-023233-d47668120e1b (MEDIUM, the `_batch()` batch-aware list incomplete in both directions) carries FOUR wrong line numbers, all shifted by the same few lines and all from the same mechanism as tonight's other two.

WHAT I WROTE -> WHAT IS TRUE AT HEAD, each re-derived with `grep -n` against the file just now:
  subject coordinate   store.py:5238      -> store.py:5235   ("**Batch-aware methods** (check ``_defer_commit`` and route")
  the heading quote    store.py:5238-5239 -> store.py:5235-5236
  the ⚠ paragraph      store.py:5252-5258 -> store.py:5249-5255
  the universal        store.py:5260-5262 -> store.py:5256-5258

MECHANISM: I read that block through `sed -n '5215,5290p'`, which prints NO line numbers, and then reconstructed each coordinate by counting offsets from the window's start. Arithmetic over a numberless window is a guess wearing a coordinate's clothes.

▶ THIS IS THE FOURTH COORDINATE OR QUOTE DEFECT TONIGHT AND THEY ARE ONE CLASS, NOT FOUR MISTAKES: a misquoted word, a grep-pipeline line number, an off-by-one, and now an offset computed by hand. Every one is a coordinate that was DERIVED AT READ TIME AND USED AT WRITE TIME. The close I recorded in diogenes-20260906-024021-9480a31c7532 covers all four and is now earned rather than proposed: re-run `grep -n` for the subject symbol IMMEDIATELY BEFORE composing the episode, and never take a coordinate from `sed -n` output or from a pipeline's own numbering.

⛔ NOTHING SUBSTANTIVE CHANGES. Every quoted line is verbatim (re-read at the corrected coordinates), the AST census stands, the two-connection runtime probe stands, and the verdict, severity and prescription are unchanged. Still ONE defect, still counted once, still MEDIUM.
  Tags: anneal-memory, code_review, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260906-024143-64f669681972
  Time: 2026-09-06T02:41:43  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: SELF-CORRECTION: diogenes-20260906-024114-a81db15ebe6e — the correction I filed to fix wrong coordinates itself contains a wrong coordinate. Correcting it once, from a numbered listing held in front of me before a single character of this episode was written.

WHAT THAT CORRECTION SAID: the ⚠ paragraph is at store.py:5249-5255.
WHAT IS TRUE AT HEAD: store.py:5248-5254.

THE FULL, FINAL COORDINATE SET for diogenes-20260906-023233-d47668120e1b, every one read off `awk 'NR>=5233 && NR<=5259 {printf "%d: %s\n", NR, $0}' anneal_memory/store.py`:
  subject / heading   store.py:5235-5236
  the ⚠ paragraph     store.py:5248-5254
  the universal       store.py:5256-5258
The finding's other coordinates were derived independently and are correct: upsert_pattern_history store.py:3353, seed_pattern_max_level store.py:3488, set_section_schema store.py:4299.

⛔ THE MECHANISM OF THIS ONE IS DIFFERENT FROM THE FOUR IT WAS CORRECTING, AND WORSE. I put the `grep -n` commands and the `episodic.py write` in ONE shell invocation, so the episode text was composed BEFORE its own evidence printed. I did not mis-remember a number; I wrote a number I had not yet been shown, in the very episode whose subject is writing numbers you have not derived.

▶ THE CLOSE, AND IT SUPERSEDES THE WEAKER FORM I GAVE AN HOUR AGO. "Re-derive the coordinate immediately before writing" is not enough, because ORDER WITHIN A COMMAND IS NOT ORDER IN TIME for anything I compose. THE DERIVATION AND THE EPISODE MUST BE SEPARATE TOOL CALLS, derivation first, output READ, then the write. A probe and the claim that rests on it cannot share an invocation — which is the 2026-09-04 close ("a probe whose OUTPUT is the evidence must be validated before its output is") applied to my own composition step rather than to a target's code. Five instances in one night, one class, and the last one is the clean statement of it.

The finding diogenes-20260906-023233-d47668120e1b stands unchanged in substance: severity MEDIUM, the AST census, the two-connection runtime probe, the verdict and `[prescription: reasoned]` are all unaffected. ONE defect, counted once.
  Tags: anneal-memory, code_review, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260906-024319-30d012750ad9
  Time: 2026-09-06T02:43:19  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: STILL OPEN (6), TOTAL AT HEAD a12e66ab30164e52d6c48e73adc1a958389277d1 — anneal-memory, 2026-09-06. 5 filed this window (0 HIGH / 4 MEDIUM / 1 LOW) + 1 carried, re-derived individually at HEAD rather than re-typed from the previous slot. Filed separately: 0 DRIFT (not in this count). SIX SELF-CORRECTIONS, five of one class and one an amendment.

ARITHMETIC. Prior slot diogenes-20260905-045758-7a0bdd47a350, STILL OPEN (7) at HEAD 5b791728, taken FROM THE STORE (tag-filtered, SUPERSEDES-checked) and not from my continuity file. `git merge-base --is-ancestor 5b791728 HEAD` = YES, so that baseline is reachable and its count is live; 5b791728 is EXACTLY this window's base. MINUS 6 CLOSED, PLUS 5 NEW = 6.

THE SIX CLOSED, EVERY ONE RE-DERIVED BY MEASUREMENT AND NOT BY READING THE FIX COMMITS:
· HIGH store.py:4498 (the durable counter lost on the batched path) — `grep -n` returns THREE `_persist_audit_health()` call sites now (4969, 5469, 6064) against one; both new pinning tests pass on a clean archive tree. CLOSED.
· MED README.md:210 ("a top-tier (3x) carry" against a `>= 3` gate) — now "a carry at **3x or higher**"; no `top-tier (3x)` remains anywhere. CLOSED.
· MED pyproject.toml:104 (the unanchored `tool-integrity.json` breaking the anchoring invariant) — CLOSED BY BUILDING, not by reading the diff: `/tool-integrity.json` anchored, and with `project_memory/testbed/tool-integrity.json` AND `project_memory/testbed/README.md` planted in a clone, a real `python -m build --sdist` admits **0** project_memory paths. Both copies still ship.
· LOW pyproject.toml:99 (`.gitignore` listed as dropped when it ships) — CLOSED BY BUILDING: the sdist is 1,005,827 B / 69 files and `.gitignore` is in it; the record now carries a NOT DROPPABLE block naming the real cause. ⭐ And I tested that block's claim rather than accepting it — deleting `/README.md` and `/LICENSE` from `include` and rebuilding still ships both, plus `pyproject.toml` and `.gitignore`, at an identical 69 files. hatchling's force_include really does bypass `include`.
· LOW store.py:1089 (a comment block landed above the wrong definitions) — the two blocks now sit over their own constants. CLOSED.
· LOW store.py:84 (docstring naming the discriminator the body forbids) — reads "The primary result CODE is the real discriminator". CLOSED.

THE ONE CARRIED, coordinate derived at write time: `anneal_memory/graduation.py:98-99` — `_BARE_GRADUATION_RE` still compiles `([23])x`, and graduation.py:68 still documents the asymmetry as deliberate ("⚠ KNOWN, DELIBERATE ASYMMETRY ... Widening it would make every today-dated 4x+ line WITHOUT evidence newly eligible for bare demotion"). Unchanged, deliberate, counted.

THE FIVE FILED:
· MED store.py:6064 — `close()`'s flush comment promises the method "cannot turn a clean close into a raising one"; commit ae9eeec (position 14 of 17) made that false. SystemExit escapes, `self._conn.close()` never runs, `_closed` stays False. Run-verified across three exception arms; SystemExit is the only leaker. [run]
· MED store.py:1663 — the version stamp's "no-lower property is structural" claim is false over any marker SQLite's CAST does not read as its true integer: 'v2', 'garbled' and '' are all silently rewritten to '1', destroying the evidence the guard deliberately leaves alone. [run]
· MED store.py:5235 — the fix for an incomplete batch-safety list left it incomplete in BOTH directions (AST census: `upsert_pattern_history`, `seed_pattern_max_level` missing from the batch-aware list; `set_section_schema` in neither, against "All other write methods"). [reasoned]
· MED tests/test_audit.py:2051 — the guard for the window's headline HIGH is pinned by a test that cannot go red when the guard is removed and never reads the continuity file the HIGH is about. ⭐ The FIX ITSELF IS CONFIRMED: driven end-to-end through `prepare_wrap`/`validated_save_continuity`, HEAD keeps the NEW text and the mutant restores the OLD text with sidecars unlinked and the wrap row committed — the described loss, exactly. [run]
· LOW CHANGELOG.md:28 — a refusal written "so they do not return" names two of three callers; the omitted one is the widest-scoped. Verdict survives (all three message-and-stop); the census does not. Same short census at next_steps.md:117, labelled "verified on disk". [reasoned]

SLOT PRE-FLIGHTED DRY THROUGH THE LIVE PARSER before writing: `_parse_slot_count` -> '6', `extract_still_open` -> '6'. Re-read FROM THE STORE tag-filtered as the router selects: 12 blocks, buckets {COVERAGE-OPEN 1, MEDIUM 4, LOW 1, SELF-CORRECTION 6}, **0 OTHER**.

⛔ THE SIX SELF-CORRECTIONS ARE ONE CLASS AND I AM NOT SOFTENING THE COUNT. A misquoted word inside a `[quoted]` block, a grep-pipeline line number used as a file coordinate, an off-by-one, an offset computed by hand over numberless `sed` output, and finally a correction that itself carried an underived number because I put the grep and the write in ONE shell invocation. Ten clean sheets ended here. The close is in diogenes-20260906-024143-64f669681972 and it is sharper than "be careful": THE DERIVATION AND THE EPISODE MUST BE SEPARATE TOOL CALLS, derivation first, output READ, then the write — order within a command is not order in time for anything I compose. Every finding's substance survived every correction; it was the addresses that moved, never the measurements.
  Tags: anneal-memory, code_quality, code_review
```

---

```
[laptop/flow]   ID: diogenes-20260906-024413-04543a77d459
  Time: 2026-09-06T02:44:13  Agent: diogenes  Type: observation
  Source: laptop_overnight_review
  Content: COVERAGE: anneal-memory 2026-09-06, HEAD a12e66ab30164e52d6c48e73adc1a958389277d1 **== origin/main VERIFIED AGAINST THE LIVE REMOTE** (`git ls-remote origin HEAD` answered a12e66ab at start AND at finish, not a cached tracking ref). `git status --porcelain` EMPTY at start and EMPTY at finish, HEAD unchanged — the repo was NOT touched: every probe was read-only or under /tmp/dio_am6* (a `git archive HEAD` control tree, two patched copies, a mutant, a clean-venv sdist builder, a hostile-plant clone), all removed at close. `~/Documents/anneal-memory` is a SYMLINK to `~/Briefcase/anneal-memory` — checked first. Window 5b79172..HEAD = 17 commits (dispatch said 16; the 24h slice is the identical 17 and 5b79172 is my own 09-05 04:59 HEAD, so the window tiles with nothing between).

RE-DERIVED BY RUNNING, NOT CARRIED FORWARD:
· Full suite at HEAD — **1864 passed, 3 skipped, 3 subtests, exit 0** (1848/5 at the base). ⚠ AND I CHECKED WHAT THE SKIPS ARE rather than reading past them: all three are `tests/test_packaging.py`, skipped because hatchling is not importable to the repo's python. next_steps.md:48 warns about exactly this ("NEEDS hatchling INSTALLED OR IT SKIPS AND LOOKS GREEN") and it is right — a bare `pytest -q` here does NOT run the packaging gate.
· So I ran the packaging property MYSELF, in a clean venv with hatchling: a real `python -m build --sdist` = **1,005,827 bytes / 69 files, 0 project_memory paths**. Then with `project_memory/testbed/tool-integrity.json` and `.../README.md` planted: still **0**. Then with `/README.md` and `/LICENSE` DELETED from `include`: both still ship, plus pyproject.toml and .gitignore, at an identical 69 files — so the new NOT DROPPABLE block's force_include claim is TRUE and measured, not quoted.
· mypy — **Success, 18 source files**. ruff `check .` — **63 errors**, `check project_memory` — **52**. next_steps.md publishes 63 as a regression oracle and it is exact.
· A `git archive HEAD` CONTROL tree — 1862 passed / 5 skipped (two tests skip outside a git worktree). Both patched trees measured against that control, not against the repo figure, and both came in identical.
· ⭐ **THE CONCURRENCY GAP I RECORDED THREE NIGHTS RUNNING IS CLOSED, AND THE CODE WON.** 8 real processes × 5 flushes at one store through a file barrier: expected 40, **actual 40**, zero lost updates, and `audit_last_failure` ended on the NEWEST stamp despite 40 interleaved writes. Then a deterministic adversarial ordering (newest written FIRST, older flushing LAST) — the newer record survived — and the tie case behaved exactly as the comment admits it would. `_stored_failure_is_newer` is genuinely atomic under real contention. A candidate died on disk.
· End-to-end through the canonical pipeline (`prepare_wrap` -> `validated_save_continuity`) with an interrupt in the deferred-audit replay, in TWO trees: HEAD keeps the new continuity text; the mutant restores the OLD text with the sidecars unlinked and the wrap row committed anyway. The window's headline HIGH fix is CONFIRMED load-bearing.
· AST census over `Store` for the batch-safety list; a two-connection runtime probe proving the two omitted methods really are batch-aware; 7 planted `format_version` values measured against the guard AND against SQLite's own CAST; 3 exception arms through `close()`.
· `codeshape.py 5b79172` run as the DRIFT discriminator on all five Python files in the window: store.py / test_audit.py / test_store.py exit 1 (CODE CHANGED -> FINDING); types.py and test_continuity.py exit 0 (DRIFT-eligible) and I filed nothing against either. CHANGELOG.md, README.md and next_steps.md are markdown — FINDING by rule, and NO check was run on them. pyproject.toml is neither Python nor markdown and its config changed, so DRIFT's "code unchanged" half cannot be established; nothing was filed against it tonight anyway.
· Slot pre-flighted DRY through the LIVE parser -> '6', then re-read FROM THE STORE tag-filtered as the router selects -> 12 blocks, 0 OTHER.

NOT CHECKED — STATED, NOT IMPLIED:
· **PYTHON 3.13 ONLY, FOURTH NIGHT, AND THIS IS THE WINDOW WHERE IT BITES HARDEST.** The entire `_is_write_lock_contention` text fallback that this window rewrote IS the 3.10 path. I exercised it exactly the way the repo's own new test does — hand-built `OperationalError`s carrying no `sqlite_errorcode` — which proves the branch's LOGIC and proves nothing about a real 3.10 interpreter. `requires-python` is >=3.10 and no 3.10 ran here or, as far as I can tell, anywhere.
· **A THREAD I OPENED AND DID NOT FINISH, named rather than buried:** store.py:81-82 still warns that the wrapper's message "embeds the store PATH, so a database under a directory named `locked/` would fool a substring test", and the new fallback IS a substring test — on `str(cause)` rather than the wrapper, which is the distinction the docstring rests on. I never measured whether any sqlite3 `OperationalError` message actually embeds the store path, so I could not establish the hazard and did not file it. It is a real open question, not a dead one.
· **MCP TRANSPORT NEVER STARTED and the CLI never driven.** All three `_is_write_lock_contention` call sites were READ. `_print_contention` has never printed under real contention in my hands, and `server.py:712`'s tool result was read, not returned.
· **PROSE, and this is the biggest hole by volume.** `project_memory/diogenes_20260905.md` (+259, my own routed report) was NOT audited at all. Of `next_steps.md` (+242) I read the gauge block, the pickup headings, the caried-count warning and the caller-census passage — call it a third. Of `CHANGELOG.md` (+164) I read roughly the first 120 diff lines and left the rest unread.
· `tests/test_audit.py` — I read the 514 new lines; the other ~2,100 lines of that file were not read. No rotation scenario this window (fifth night). The crystal/spores sidecar layer was not attacked at all. ruff and mypy were run at HEAD only, never on the patched trees.
· ⛔ AND THE HONEST ONE ABOUT MYSELF: six self-corrections, five of them coordinates or quotes I carried from read-time to write-time. Every measurement in this report was re-derived; it is the ADDRESSES that failed, and a reader should weight the measured tables above the line numbers pointing at them. [prescription: none]
  Tags: anneal-memory, code_review, semantic
```
