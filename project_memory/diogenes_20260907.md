# DIOGENES OVERNIGHT — anneal-memory, 2026-09-07

**Source selector:** `episodic.py read --since 24h --tags anneal-memory  (filtered to Agent: diogenes at 2026-09-07*)`
**Stated count: 6 episode(s).** Severity: LOW 1 · MEDIUM 2 — 3 of 6 episode(s) carry a severity; the other 3 are COVERAGE-OPEN 1 · SELF 2
**STILL OPEN: NOT REPORTED BY DIOGENES THIS WINDOW.** No slot was emitted, so no count is written here. Do not infer one — an invented debt number is the defect `spore-498` exists to stop.

> ⚠ **UNTRIAGED AND VERBATIM.** Generated by `scripts/route_diogenes.py` from the episodic store without judgement. Nothing here has been checked against disk except where a line says so explicitly. Diogenes' own `[prescription: ...]` tags are his assessment, not a ruling.

---

```
[laptop/flow]   ID: diogenes-20260907-044903-a77d85f68f80
  Time: 2026-09-07T04:49:03  Agent: diogenes  Type: observation
  Source: laptop_overnight_review
  Content: COVERAGE-OPEN: anneal-memory 2026-09-07, HEAD 0e6f15647d0cc209b6ab5e67470ae5b5c20631e7 (== origin/main? YES — `git ls-remote origin HEAD` answered 0e6f1564 against the LIVE remote, not a cached tracking ref), `git status --porcelain` <empty>, window a12e66a..HEAD = 21 commits (dispatch said 20; the sorted-hash diff proves the 24h slice is the IDENTICAL 21-commit set). `~/Documents/anneal-memory` is a SYMLINK to `~/Briefcase/anneal-memory` — checked first; real path is the Briefcase one. Baseline to be taken FROM THE STORE (my 09-06 02:44 slot at a12e66ab), not from continuity's latest block. REVIEW STARTED, NOT FINISHED — if no COVERAGE line for anneal-memory exists in this run, THIS RUN DIED and the finding list is PARTIAL.
  Tags: anneal-memory, code_review, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260907-045711-21dbe392f261
  Time: 2026-09-07T04:57:11  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: MEDIUM: tests/test_audit.py:2219 — THE REGRESSION GATE WRITTEN THIS WINDOW TO ANSWER "A GATE NOTHING CAN PASS IS NOT A GATE" CARRIES A MUTATION RECIPE THAT NO LONGER GOES RED, BECAUSE TWO LATER COMMITS IN THE SAME WINDOW ADDED TWO MORE LAYERS OF CONTAINMENT. Following the docstring's own instruction verbatim returns `1 passed`. — THE FIX: replace the single-site recipe with the measured three-site one, or split the test so each containment layer has an arm that can fail alone. [prescription: reasoned]

THE CLAIM, verbatim from the test's own docstring:
[quoted, tests/test_audit.py:2218-2220] `        MUTATION-CHECKED IN BOTH DIRECTIONS, and it does NOT abort the runner:` / `        narrowing ``_batch()``'s post-commit ``except BaseException`` back to` / `        ``except Exception`` makes the interrupt escape the batch, so`
[quoted, tests/test_audit.py:2227] `        ⚠ RE-RUN THE MUTATION WITH THIS TEST SELECTED ALONE, by node id or`
[quoted, tests/test_audit.py:2232-2233] `        can — measured 2026-09-06: selected alone the mutant gives` / `        ``1 failed``, selected together it gives an abort at the sibling.`

MEASURED, NOT ARGUED — seven arms, each a `cp -R` of a `git archive HEAD` tree with `__pycache__` cleared, each run with EXACTLY the selector the docstring prescribes (`-k cannot_destroy_a_committed_wrap`). The three `except BaseException:` sites are store.py:5213 (`_audit_log_after_commit`), store.py:5858 (`_replay_deferred_audits`, per-event) and store.py:5798 (`_batch`, post-commit):

  arm | narrowed to `except Exception` | result
  ----|--------------------------------|--------
  A   | none (HEAD)                    | 1 passed
  B   | 5798 — THE DOCSTRING'S OWN ARM | **1 passed**  <- claim says `1 failed`
  C   | 5858                           | 1 passed
  D   | 5858 + 5798                    | 1 passed
  E   | 5213                           | 1 passed
  H   | 5213 + 5858                    | 1 passed
  G   | 5213 + 5858 + 5798             | **1 failed**

So the test CAN go red, but only when ALL THREE are removed at once. It is not a gate nothing can pass; it is a gate whose documented single-site mutation cannot move it.

THE MUTATION IS REAL AND HITS THE NAMED SITE — verified rather than assumed, because a mutation that silently missed would produce this same PASS. `ast` over the mutant tree puts line 5798 inside `_batch` (5549-5806); `inspect.getsource` on the IMPORTED module at runtime returns `'        except Exception:'` for that line and `__file__` resolves to the mutant tree, not the archive.

AND THE INTERRUPT LANDS EXACTLY WHERE THE DOCSTRING SAYS, so the recipe did not rot by aiming at the wrong place. Instrumented `extract_stack()` at the raise: `continuity.py:2111 validated_save_continuity` -> `store.py:5797 _batch` -> `store.py:5838 _replay_deferred_audits` -> `store.py:5212 _audit_log_after_commit`, event `'wrap_completed'`. What changed is that store.py:5213 now swallows the KeyboardInterrupt before it can reach either handler downstream of it.

WHY IT WENT FALSE, AND THE ORDER IS THE POINT. `7de2007` ("tests: grade the wrap-destruction guard on the artifact it protects") is the OLDEST commit in this window — it is the fix for my 2026-09-06 MEDIUM that the previous gate could not go red. Its mutation claim was true when written. `926be6a` (widening store.py:5213 to `BaseException`) and `200382d` (adding the per-event `except BaseException: continue` at store.py:5858) both landed LATER in the same window and each independently contains the interrupt.

⛔ WHY THIS IS NOT COSMETIC. This docstring is not description, it is the RE-DERIVATION RECIPE — it exists so the next reviewer can re-establish that the gate is live, and it is the direct answer to a filed finding. A reviewer who runs it gets a PASS and has two wrong conclusions available: that `_batch()`'s post-commit handler is unnecessary (it is not — `_persist_audit_health` re-raises SystemExit from the `finally` at store.py:5863 and that still reaches it), or that the test is broken. The runtime property is SAFE at HEAD; what is wrong is the instrument that proves it.

[prescription: reasoned] — I measured the matrix but wrote no fix; which of the two repairs is right (correct the recipe vs. split the arms) is a design call about how much containment redundancy the repo wants to pin.
  Tags: anneal-memory, code_quality, code_review
```

---

```
[laptop/flow]   ID: diogenes-20260907-050251-a8fd2d3cab0a
  Time: 2026-09-07T05:02:51  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: LOW: anneal_memory/store.py:1715 — THE PARAGRAPH THAT SAYS "MEASURED 2026-09-06" CARRIES A LATENCY FIGURE THAT IS THE CONFIGURED SETTING, NOT A MEASUREMENT. A blocked write-capable open takes ~11.7 s, not ~5 s — 2.4x. The half of the sentence that WAS measured (that it is the SAME as before the lock change) is TRUE and I confirmed it. — THE FIX: state the measured wall-clock, or drop the figure and keep the comparative claim, which is the load-bearing one. [prescription: reasoned]

[quoted, store.py:1712-1718]
        ⚠ THIS TAKES THE WRITE LOCK EARLIER THAN BEFORE, AND THAT IS NOT A
        NEW FAILURE MODE — MEASURED 2026-09-06. Opening a write-capable store
        while another process holds the write lock ALREADY failed, with
        ``database is locked`` after the same ~5s sqlite timeout, because the
        unconditional commit at the end of this method needed the same lock.
        ``BEGIN IMMEDIATE`` moves that acquisition earlier; it does not add
        contention that was not already there.

Same figure, same window, in the operator-facing record: [quoted, CHANGELOG.md:28] `lock *already* failed with `database is locked` after the same ~5s timeout, because the`

MEASURED THREE TIMES, TWO TREES, ONE OF THEM PRE-WINDOW. A second process holds `BEGIN IMMEDIATE` on the store; a `Store(path)` open then races it. Both trees are `git archive` extractions — `a12e66a` (this window's base, the code the sentence calls "already") and HEAD:

  · a12e66a  — StoreDatabaseError "database is locked" after **11.64 s**
  · HEAD     — StoreDatabaseError "database is locked" after **11.71 s**
  · HEAD, statement-isolated, replaying `__init__`'s exact sequence on a bare
    `sqlite3.connect(path)` (default 5.0 s timeout, which is what store.py:1566
    uses — it passes no `timeout=`): the SELECT and all three PRAGMAs return in
    0.00 s and `BEGIN IMMEDIATE` alone blocks **11.87 s** before raising
    `OperationalError: database is locked`.

⚡ SO THE COMPARATIVE CLAIM IS CONFIRMED AND THE NUMBER IS NOT. 11.64 vs 11.71 is the same behaviour to within noise — "it does not add contention that was not already there" is exactly right, and I re-derived it rather than reading it. What is wrong is the ~5s: that is `sqlite3.connect`'s default `timeout` parameter, and SQLite's busy-handler backoff overshoots it by 2.4x on this path. The number was reasoned from the setting inside a paragraph whose own heading asserts it was measured.

⚠ AND THE REPO ALREADY KNOWS THE DIFFERENCE, 800 LINES DOWN — the `wrap_started` note cites a real stopwatch reading against a real setting: [quoted, store.py:2507] `        # Measured 2026-09-04: 0.85s at busy_timeout=800ms, wrong error class.` That is what a measured latency claim looks like in this file. This one is the setting wearing the word.

⛔ WHY IT IS NOT ZERO-IMPACT. This is the paragraph an operator or a future maintainer reads to decide whether a hung `Store(...)` is the lock or a deadlock. At ~5s a 12-second hang looks like a NEW pathology introduced by `BEGIN IMMEDIATE` — the precise misreading this paragraph exists to prevent — and it is the risk-acceptance rationale for the window's largest structural change.

[prescription: reasoned] — I measured both trees and the isolated statement but edited nothing; whether to publish a wall-clock figure that is platform- and SQLite-version-dependent, or simply to delete it and keep the comparison, is a call about what the record should promise.
  Tags: anneal-memory, code_review, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260907-050558-68e28fba08de
  Time: 2026-09-07T05:05:58  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: MEDIUM: anneal_memory/audit.py:282 — THE ROLLBACK WIDENED TO `BaseException` THIS WINDOW COVERS ONLY THE PART OF THE WINDOW IT DESCRIBES. The chain-state advance sits OUTSIDE the `try`, so a terminal exception between the fsync and that advance still leaves the entry on disk with `_seq`/`_prev_hash` unchanged and no rollback — REPRODUCED AT HEAD as seqs `[0, 1, 1]` and `verify(): valid=False, Hash mismatch at seq 1`, the identical signature the comment says is closed. — THE FIX: move the three chain-state lines inside the `try`. EXECUTED: signature inverts to `[0, 1]` / `valid=True`, and the full suite is unchanged at 1875 passed / 5 skipped / 0 failed. [prescription: run]

THE CLAIM: [quoted, audit.py:251-257]
            # ⛔ ``BaseException``, NOT ``Exception`` (codex L3 HIGH,
            # 2026-09-06). The all-or-nothing property this block exists to
            # provide did not hold for terminal exceptions: a
            # ``KeyboardInterrupt`` landing after the line was written and
            # fsynced but before the chain state advanced left the entry ON
            # DISK with ``_seq``/``_prev_hash`` unchanged and NO rollback,
            # because it walked past this handler.

THE STRUCTURE THAT DEFEATS IT — the guarded region ends four lines before the state it guards:
[quoted, audit.py:245-250] `        try:` / `            with open(active, "a", encoding="utf-8") as f:` / `                f.write(json_line + "\n")` / `                f.flush()` / `                os.fsync(f.fileno())` / `        except BaseException:`
[quoted, audit.py:279-283] `            raise` / `` / `        # Update chain state` / `        self._prev_hash = self._compute_hash(json_line)` / `        self._seq += 1`

The comment's own description of the window is "after the line was written and fsynced but before the chain state advanced". `os.fsync` is at :249 and the advance is at :282. The `try` closes at :250. **Widening the handler cannot reach :281-287 no matter what it catches** — so the fix closes the part of the described window that is inside the block and leaves the rest exactly as it was.

REPRODUCED AT HEAD, NOT REASONED. Injected at :282's right-hand side (`_compute_hash`), which is the faithful stand-in for a signal delivered after the `with` exits and before the STORE_ATTR — same effect, no assignment, no rollback, entry durable:
  seqs on disk:   [0, 1, 1]
  events on disk: ['first', 'second', 'third']
  verify():       valid=False  total_entries=2  error=Hash mismatch at seq 1: expected sha256:171890268f723..., got sha256:89f5fad682b25...
The third append reuses the stale `seq` and `prev_hash` exactly as the comment predicts — the reachability argument the comment MAKES ("once the store started RECORDING the drop and CONTINUING, the next append reused the same seq and prev_hash") applies unchanged to the residual window.

PRESCRIPTION EXECUTED, BOTH DIRECTIONS. Moved `_prev_hash` / `_seq` / `_dropped_since_last` inside the `try`, same injection point:
  seqs on disk:   [0, 1]        <- the interrupted entry rolled off disk
  events on disk: ['first', 'third']
  verify():       valid=True  total_entries=2  error=None
  full suite:     1875 passed, 5 skipped, 3 subtests, exit 0 — IDENTICAL to HEAD.

⛔ AND THIS IS NOT THE RACE THE REPO ALREADY REFUSED, WHICH I CHECKED BEFORE FILING. store.py:4964 names a structurally identical commit/ack gap and refuses it with three reasons, the FIRST of which is: [quoted, store.py:4972] `            # 1. THE DIRECTION IS THE SAFE ONE. This counter answers "is the`. That reason does not transfer here. There the residual produces an OVER-COUNT on a degraded-audit gauge, which still answers its question. Here it produces `verify(): valid=False` — a FALSE TAMPERING VERDICT, which the same comment block calls out in its own words as "a durability hiccup read as tampering, on the record whose entire value is telling those apart". The safe-direction argument is inverted at this site.

⚠ AND THE SECOND ASYMMETRY IS THE RECORD ITSELF: store.py:4964 opens `⚖ A COMMIT/ACK RACE LIVES IN THIS GAP AND IS DELIBERATELY NOT CLOSED — REFUSED WITH REASONS`. audit.py names no residual window anywhere (`grep -n "bytecode\|residual\|not closed\|still open" anneal_memory/audit.py` returns one unrelated hit at :132). A reader of audit.py:251-257 has no way to learn the property is partial.

[prescription: run] — the fix was applied to a scratch tree, the signature inverted in both directions, and the full suite re-run under it.
  Tags: anneal-memory, code_quality, code_review
```

---

```
[laptop/flow]   ID: diogenes-20260907-050629-5494ca982055
  Time: 2026-09-07T05:06:29  Agent: diogenes  Type: observation
  Source: laptop_overnight_review
  Content: SELF-CORRECTION to diogenes-20260907-045711-21dbe392f261 (the tests/test_audit.py:2219 MEDIUM). ⚠ THIS IS AN AMENDMENT, NOT A RETRACTION — nothing in that finding is withdrawn, every arm of the seven-arm matrix stands, and it is NOT counted as a second finding. WHAT WAS INCOMPLETE: I cited ONE home for the false mutation recipe. It has TWO, and the second is the durable one.

THE SECOND SITE: [quoted, project_memory/next_steps.md:75-77]
   canonical pipeline. ⚠ **Run its mutation with that test selected ALONE** — under the mutant the
   SIBLING test aborts the pytest session first, so `-k interrupt` reports an abort and looks like
   this one cannot go red either. It can: selected alone the mutant gives `1 failed`.

Same claim, same window, same falsity: arm B of the matrix ran EXACTLY that instruction — the `_batch()` post-commit catch narrowed to `except Exception`, `-k cannot_destroy_a_committed_wrap` selected alone — and returned `1 passed`.

⛔ WHY THE SECOND SITE IS THE WORSE ONE, AND WHY I AM NAMING IT RATHER THAN LETTING THE FIRST STAND ALONE. The docstring is read by whoever is already editing that test. `next_steps.md` is the PICKUP — the file the next seat opens first, and the one whose own header block this window spent a commit fixing because a stale number there mis-set three consecutive mornings. A false re-derivation recipe in the pickup is the same class the repo has been paying down all window: the defect propagated to the durable surface while the working surface got the attention.

⚠ AND MARKDOWN IS NEVER `DRIFT:` BY RULE, so this site is a FINDING-tier claim in its own right; I am folding it into the existing MEDIUM rather than filing it separately because it is ONE false claim with two homes, and counting it twice would inflate the debt number with a single defect.

CORRECTED SUBJECT LINE for that finding: the mutation recipe is false at tests/test_audit.py:2219 AND at project_memory/next_steps.md:77; a fix that corrects only the docstring leaves the pickup wrong.
  Tags: anneal-memory, code_review, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260907-050905-8d663b5724b9
  Time: 2026-09-07T05:09:05  Agent: diogenes  Type: observation
  Source: laptop_overnight_review
  Content: SELF-CORRECTION to diogenes-20260907-050558-68e28fba08de (the anneal_memory/audit.py:282 MEDIUM). ⛔ ONE POSITIONAL CLAIM IS WRONG. Every coordinate, every quoted line, and every measurement in that finding STANDS — the batch check ran 32 asserted coordinates through `awk`-equivalent `NR==n` reads and all 32 came back exact.

WHAT WAS WRONG. I wrote: "THE STRUCTURE THAT DEFEATS IT — the guarded region ends four lines before the state it guards". **FOUR IS WRONG UNDER EITHER READING OF "GUARDED REGION", AND THE TWO READINGS ARE 3 AND 33.**

THE CORRECTED CLAIM, computed rather than eyeballed:
· the `try` BODY ends at audit.py:249 (`os.fsync(f.fileno())`)
· `except BaseException:` opens at :250 and the handler runs to `raise` at :279
· the chain-state advance is at :282
· **282 − 249 = 33** lines from the end of the guarded body, with an entire 30-line handler in between
· **282 − 279 = 3** lines from the `raise` that closes the whole `try/except` statement
Neither is four. The honest sentence is: *the try's body ends at :249 and the state it guards advances at :282 — thirty-three lines and one whole handler later; the statement itself closes at :279, three lines above the advance.*

▶ NOTHING ELSE MOVES. The defect, the reproduction (`[0, 1, 1]`, `verify(): valid=False, Hash mismatch at seq 1`), the executed fix (`[0, 1]`, `valid=True`), the suite result (1875 passed / 5 skipped / 0 failed, identical to HEAD), and the store.py:4964 asymmetry are all unaffected — the structural point is that the advance is OUTSIDE the try, and it is outside by 33 lines rather than by 4, which makes it *more* clearly outside, not less.

⚡ WHAT CAUGHT IT, AND IT IS THE 2026-09-07 wisp CLOSE FIRING ONE REPO LATER. That close reads: *"a filtered read may establish ORDER, never POSITION — and the batch `awk NR==n` check must run over positional CLAIMS, not only over cited coordinates."* Tonight's batch had a second section that did exactly that: it printed the arithmetic for every distance I had asserted in prose. The 32 coordinates were clean; the one number I had NOT derived was the one in a sentence, not in a `file:line`. **A coordinate gets checked because it looks like a coordinate. A distance stated in prose does not look like one, and that is the whole gap.**

▶ CLOSE, MECHANICAL AND NARROWER THAN "CHECK YOUR PROSE": **any INTERVAL between two coordinates must be computed by subtracting them in the same tool call that reads them, never written from the shape of the code on screen.** A distance is a derived quantity wearing the costume of an observation.
  Tags: anneal-memory, code_quality, code_review
```
