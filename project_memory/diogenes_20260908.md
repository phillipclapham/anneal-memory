# DIOGENES OVERNIGHT — anneal-memory, 2026-09-08

**Source selector:** `episodic.py read --since 24h --tags anneal-memory  (filtered to Agent: diogenes at 2026-09-08*)`
**Stated count: 9 episode(s).** Severity: HIGH 2 · MEDIUM 4 — 6 of 9 episode(s) carry a severity; the other 3 are COVERAGE 1 · COVERAGE-OPEN 1 · STILL OPEN 1
**STILL OPEN: 7** — **6 filed tonight** (measured at HEAD `5e4b834`) · **1 carried**. A carried item was NOT necessarily re-checked this window; only the slot block below says which ones were.

> ⚠ **UNTRIAGED AND VERBATIM.** Generated by `scripts/route_diogenes.py` from the episodic store without judgement. Nothing here has been checked against disk except where a line says so explicitly. Diogenes' own `[prescription: ...]` tags are his assessment, not a ruling.

---

```
[laptop/flow]   ID: diogenes-20260908-033217-2df1a48fca31
  Time: 2026-09-08T03:32:17  Agent: diogenes  Type: observation
  Source: laptop_overnight_review
  Content: COVERAGE-OPEN: anneal-memory 2026-09-08, HEAD 5e4b834b1f79fb18d4b456a5ead2229b1e8703c1 (== origin/main? YES — `git ls-remote origin HEAD` answered 5e4b834b at start; will re-check at finish). `git status --porcelain` EMPTY. `pwd -P` = /Users/phillipclapham/Briefcase/anneal-memory (NOT a symlink this time — the Documents path is, the Briefcase path is real). Window 0e6f1564..HEAD = 12 commits (dispatch said 11; a sorted-hash `diff` of the window set against the 24h slice returns IDENTICAL, so the 24h slice is the same 12).

BASELINE, AND IT IS NOT ONE POOL: my 2026-09-07 anneal-memory run DIED. The store holds its COVERAGE-OPEN stub (diogenes-20260907-044903-a77d85f68f80), THREE findings (2 MEDIUM / 1 LOW) and two SELF-CORRECTIONS, and NO `STILL OPEN` slot and NO `COVERAGE:` line. That is exactly the failure Step 0 was written to make legible, and it worked — the stub is why I know the 09-07 list is partial rather than clean. So the candidate pool is the 09-06 slot (6 at a12e66ab) PLUS the 09-07 orphans (3 at 0e6f1564), resolved against the TREE and the repo's own triage files, never summed.

REVIEW STARTED, NOT FINISHED — if no COVERAGE line for anneal-memory exists in this run, THIS RUN DIED and the finding list is PARTIAL.
  Tags: anneal-memory, code_review, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260908-033943-517b207ff53f
  Time: 2026-09-08T03:39:43  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: HIGH: anneal_memory/audit.py:1322 — THE TORN-TAIL RECOVERY THIS WINDOW BUILT IS UNREACHABLE FOR HALF OF THE TORN TAILS, AND THE OTHER HALF BRICKS THE TRAIL FOR GOOD. Making read errors propagate out of ``_read_last_valid_entry`` was ruled deliberately and is graded — I am not re-litigating it. The defect is that the propagation is scoped by EXCEPTION TYPE and not by WHAT FAILED: a ``UnicodeDecodeError`` raised by the line iterator is not a failed READ, it is a TORN LINE — the exact condition the loop's next four lines already handle for ``json.JSONDecodeError`` with ``pass  # Partial write — skip``. Because it escapes instead, ``_initialize`` raises, ``_initialized`` stays False, and EVERY subsequent ``log()`` re-enters the same scan and raises again. There is no recovery path: the condition is on disk, permanent, and re-derived from scratch each time.

[quoted, audit.py:1322-1332]
    with open(path, "r", encoding="utf-8") as f:
        for line in f:
            stripped = line.strip()
            if not stripped:
                continue
            try:
                json.loads(stripped)
                last_valid = stripped
            except json.JSONDecodeError:
                pass  # Partial write — skip

⛔ THE SHARPEST HALF IS THAT THIS WINDOW BUILT THE RECOVERY AND THEN PUT IT BEHIND THE RAISE. ``_seed_from_manifest()`` was added to ``_initialize``'s no-valid-entry branch (:905) precisely so a torn-only active file anchors on the manifest instead of restarting from genesis. RUN in the repo's own fixture shape from ``test_a_torn_only_active_file_still_anchors_on_the_manifest`` (test_audit.py:5049), three entries + a forced rotation + a torn fragment written over the active file, varying ONLY the byte offset of the tear:
  A. fragment = ``{"v":1,"seq":4,"ts":"2026-09-07T00:00:00.0000``  (the tested shape, valid UTF-8)
     -> ``_initialize`` OK, anchored=True, ``verify()`` valid=True
  B. fragment = the same JSON cut one byte inside a ``⛔`` (U+26D4)
     -> ``_initialize`` RAISED UnicodeDecodeError, ``_initialized``=False, the manifest anchor NEVER RAN
Same physical event, one byte of difference, and B has no path back. Independently, the general shape: an ``AuditTrail`` over a file with an undecodable tail RAISES on ``log()`` and RAISES on the retry (measured twice in a row), and ``Store.record()`` still succeeds — the post-commit swallow at store.py:5256 catches it — so the store goes on working while the audit trail records NOTHING, for ever, behind one dedup'd UserWarning. Verified as a REGRESSION, not a pre-existing state: the identical probe against the tree at 0e6f1564 prints ``log() SUCCEEDED`` twice and ``stats()`` returns entry_count 3.

⚠ AND THE REPO ALREADY AGREES THE SHAPE IS REAL. tests/test_audit.py:5140, written this window, says *"invalid UTF-8 after a valid prefix is a real torn-write shape on this file, not a hypothetical"*. Its gate injects the error from a monkeypatched iterator that goes away when the patch is undone, so it grades the TRANSIENT case only; nothing exercises the byte actually sitting on disk. This is an emoji-dense codebase writing JSON payloads — a tear inside a multibyte character is the ordinary case, not the exotic one.

The fix: decode PER LINE and treat an undecodable line as what it is. Open ``rb``, and inside the loop ``try: stripped = raw.decode("utf-8").strip() / except UnicodeDecodeError: continue``, leaving ``OSError`` to propagate exactly as ruled. EXECUTED in a copy of the tree: arm B above becomes ``_initialize`` OK, anchored=True. ⚠ COST, STATED: it retires the ``UnicodeDecodeError`` arm of ``test_a_read_failure_during_recovery_is_not_an_empty_file`` (test_audit.py:5110-5117), whose injection targets a text-mode ``open``; that arm must be re-pointed at the OSError contract or re-expressed against real bytes, and codex's reason for parameterising it (a guard removed over N exception types needs N arms) still holds. It also exposes the SAME defect one layer out in ``verify()`` — filed separately. [prescription: run]
  Tags: anneal-memory, code_quality, code_review
```

---

```
[laptop/flow]   ID: diogenes-20260908-034006-a0c6d3a6cbdf
  Time: 2026-09-08T03:40:06  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: HIGH: anneal_memory/audit.py:1341 — ``anneal-memory verify`` TRACEBACKS ON A TORN TAIL INSTEAD OF REPORTING ONE, AND ``verify()`` IS THE ONE SURFACE THAT CANNOT RECOVER ITSELF. ``_iter_lines`` opens in text mode and yields straight from the file handle, so an undecodable byte anywhere in an audit file raises ``UnicodeDecodeError`` out of the generator, out of ``verify()``'s scan loop at :771, and out of the CLI.

[quoted, audit.py:1335-1342]
def _iter_lines(path: Path):
    """Iterate lines from an audit file (handles .gz transparently)."""
    if path.name.endswith(".gz"):
        with gzip.open(path, "rt", encoding="utf-8") as f:
            yield from f
    else:
        with open(path, "r", encoding="utf-8") as f:
            yield from f

⛔ ``verify()`` ALREADY HAS THE CORRECT ANSWER FOR THIS AND NEVER GETS TO USE IT. ``AuditVerifyResult`` carries ``skipped_lines`` for exactly this condition, and a decodable torn tail returns it. RUN at HEAD, three entries plus a torn fragment appended to the active file, varying only where the tear lands:
  A. decodable fragment ``{"v":1,"seq":3,"ts":"2026-09``  -> ``verify valid=True skipped=1``
  B. the same JSON cut one byte inside a ``⛔``           -> ``verify RAISED UnicodeDecodeError``
RUN THROUGH THE REAL OPERATOR SURFACE, not the API: ``python3 -m anneal_memory.cli --db <store> verify`` on B prints a bare traceback ending ``File "anneal_memory/audit.py", line 1342, in _iter_lines / yield from f / UnicodeDecodeError``. ``cmd_verify`` (cli.py:887) and the server path (server.py:1247) both call it unwrapped, so neither the human nor the ``--json`` consumer gets a verdict.

⛔ AND IT IS WORSE HERE THAN IN THE SIBLING DEFECT, BECAUSE ``verify`` IS A CLASSMETHOD THAT NEVER CONSTRUCTS A TRAIL — this file records that property itself, at the ``_rotate_if_needed`` comment: *"``verify`` is a classmethod and never constructs a trail, so ``anneal-memory verify`` cannot trigger the recovery that would fix it"*. So no amount of fixing the recovery path reaches this one. It is also the surface a suspicious operator runs FIRST, and the answer it gives to "was my log tampered with" is a stack trace.

⚠ IN SCOPE THOUGH ``_iter_lines`` IS NOT IN THE DIFF: it was turned up while measuring the propagation change that IS in the diff, and it is the same conflation one layer out. It is arguably older than the window; what the window did was make the recovery path raise too, so both readers of a torn file now fail where one used to.

The fix: iterate bytes and decode per line, skipping what will not decode — ``verify`` counts it in ``skipped_lines``, which is the field's stated purpose. Applies to both branches; ``gzip.open(path, "rb")`` for the sealed half. NOT executed here — I patched the sibling site and this one surfaced as its next failure, which is how I found it, but I did not write or grade the ``_iter_lines`` change itself. [prescription: reasoned]
  Tags: anneal-memory, code_quality, code_review
```

---

```
[laptop/flow]   ID: diogenes-20260908-034058-31558c9e3281
  Time: 2026-09-08T03:40:58  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: MEDIUM: anneal_memory/audit.py:952 — TWO READERS OF ONE MANIFEST FILE NOW HOLD OPPOSITE POLICIES FOR A CORRUPT ONE, WHICH IS THE DEFECT THIS WINDOW'S OWN COMMENT SAYS IT JUST FINISHED CLOSING. The extracted ``_seed_from_manifest`` narrowed its catch to ``FileNotFoundError`` alone; ``json.JSONDecodeError`` now propagates out of ``_initialize`` and out of ``log()``, permanently, because the bad bytes are on disk and every retry re-reads them.

[quoted, audit.py:948-954]
        try:
            raw = self._manifest_path.read_text(encoding="utf-8")
        except FileNotFoundError:
            return
        manifest = json.loads(raw)
        self._prev_hash = manifest.get("active_last_hash", GENESIS_HASH)
        self._seq = manifest.get("active_last_seq", 0)

[quoted, audit.py:1231-1237, the other reader of the SAME path]
        if self._manifest_path.exists():
            try:
                return json.loads(
                    self._manifest_path.read_text(encoding="utf-8")
                )
            except (json.JSONDecodeError, OSError):
                pass

RUN, one entry + a manifest overwritten with ``{ not json`` + a zero-byte active file: ``_load_manifest()`` returns a dict and carries on; ``log()`` raises ``JSONDecodeError`` on attempt 1, again on attempt 2, again on attempt 3. So a single unparseable manifest makes the trail permanently un-appendable through one method and completely unremarkable through the other, 280 lines apart in one file.

⚡ THE INHERITED ARGUMENT DOES NOT COVER THIS CASE. The docstring's reason for propagating is transient: *"a TRANSIENT read error silently restarts the chain and ``verify()`` cries tampering once the disk recovers"* — correct, and it is an argument about ``OSError``. A manifest that does not PARSE is not a disk that will recover. Retrying it for ever is not fail-safe, it is a wedge, and it converts a degraded audit trail into a store that cannot log at all.

⛔ THE REPO HAS ALREADY RULED THIS EXACT QUESTION THE OTHER WAY, ONE MODULE OVER. ``continuity.py`` routes its crystal store through ``_crystal_active_safe`` for the documented reason that *"a crystal fault yields no-credit (the gate stays strict), never breaks the save"* (:325-328), and the corrupt-store case emits the warning at :560 — pinned by ``tests/test_crystal_migrate.py::TestCrystallizationCredit::test_corrupt_crystal_store_no_credit_no_raise``, whose name is the policy. Same shape (an unparseable JSON sidecar), same stakes, opposite answer, no test here for the corrupt-manifest case at all.

The fix, and it preserves the ruling rather than reversing it: split the two classes the way the rest of the module does. Let ``OSError`` propagate — that is the transient case the change was written for and it is graded. Catch ``json.JSONDecodeError`` and degrade the way ``_load_manifest`` already does, with the warning ``audit.py`` already emits for its analogous ``on_event`` failure so the operator is told the anchor was lost rather than the process being wedged. NOT executed: which side wins is a policy call about a tamper-evident record, and the two live answers are both defensible — the finding is that ONE FILE now holds BOTH and neither reader knows about the other. [prescription: reasoned]
  Tags: anneal-memory, code_quality, code_review
```

---

```
[laptop/flow]   ID: diogenes-20260908-034119-f817df96c38f
  Time: 2026-09-08T03:41:19  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: MEDIUM: anneal_memory/store.py:5645 — THE ONE CLAUSE THE ROSTER GATE DELIBERATELY EXCLUDED AS "PROSE ABOUT THE MECHANISM" IS THE CLAUSE THAT WENT FALSE, AND IT NAMES A HELPER ANOTHER TEST ASSERTS HAS NO CALLERS AT ALL. ``_batch()``'s docstring heads its twelve-name roster with:

[quoted, store.py:5644-5645]
        **Batch-aware methods** (check ``_defer_commit`` and route
        audit events through :meth:`_audit_log`):

RUN, an AST census over ``Store`` at HEAD for calls to ``_audit_log`` / ``_audit_log_after_commit`` inside each of the twelve named methods: ``record`` (:1980), ``delete`` (:2096), ``wrap_completed`` (:3096), ``record_associations`` (:3790), ``decay_associations`` (:3845), ``seed_pattern_co_graduation`` (:3945), ``drain_co_surface_events`` (:3971), ``gc_pattern_associations`` (:4022), ``rename_pattern_association`` (:4045), ``sever_pattern_concept`` (:4071) — all TEN emit via ``_audit_log_after_commit``; ``upsert_pattern_history`` (:3580) and ``seed_pattern_max_level`` (:3715) emit through neither directly. ZERO of the twelve route through ``_audit_log``, and ``tests/test_audit.py::TestNoBareAuditEmitSites::test_the_pre_commit_helper_has_no_production_callers`` (:2233) asserts mechanically that none can: *"``_audit_log`` now has ZERO production call sites, and this asserts it."*

⛔ AND THE MISS IS STRUCTURAL, NOT AN OVERSIGHT — IT WAS DECIDED. ``tests/test_store.py`` grades this roster against the code and explicitly carves this sentence out:

[quoted, tests/test_store.py:414-416]
        # BULLET LINES ONLY. The section HEADING also carries a ``:meth:``
        # reference — "route audit events through :meth:`_audit_log`" — which
        # is prose about the mechanism, not a roster entry. This test caught
        # that on its first run, which is the behaviour it exists for.

So the twelve NAMES are pinned and cannot drift, and the sentence saying what they DO is unpinned and has drifted. ⚡ The exclusion was right as scoping — a heading is not a roster entry — and the conclusion drawn from it was wrong: "not a roster entry" was treated as "not a claim". This is the same window that added ``_seed_from_manifest`` so two branches could not compute one thing differently; here one gate computes half a sentence and nothing computes the other half. Two more copies of the wording sit at :5598 (``audit events route through :meth:`_audit_log```) and in ``project_memory/diogenes_20260906.md:143``.

The fix is a word, not a mechanism: the heading should name ``_audit_log_after_commit``, since that is what every one of the ten actually calls and what the sibling gate at :2181 pins. If the roster gate is to cover it, the cheap form is asserting the heading's ``:meth:`` reference resolves to a helper with at least one production caller — which is the same query ``test_the_pre_commit_helper_has_no_production_callers`` already runs, inverted. NOT executed: I measured the disagreement, I did not write the assertion. [prescription: reasoned]
  Tags: anneal-memory, code_review, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260908-034326-56cd33de62d8
  Time: 2026-09-08T03:43:26  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: MEDIUM: tests/test_audit.py:5029 — TWO DOCSTRINGS IN ONE CLASS, FORTY-FIVE LINES APART, MADE BY ONE COMMIT, MAKE OPPOSITE MEASURED CLAIMS ABOUT THE IDENTICAL MUTANT — AND THE COMMIT'S OWN SUBJECT IS GRADING GATES FOR FALSE-GREEN.

[quoted, test_audit.py:5027-5029, inside ``test_a_torn_only_active_file_still_anchors_on_the_manifest``]
        under its OWN mutant too, so it discriminates nothing. MEASURED —
        delete ``or active.stat().st_size == 0`` from ``_initialize`` and the
        WHOLE audit suite still passes, 165 green.

[quoted, test_audit.py:5074-5077, inside ``test_the_zero_byte_fast_path_avoids_a_read_that_can_now_raise``]
        ⛔ MUTATION-CHECKED 2026-09-07, mutant re-read off disk: delete
        ``or active.stat().st_size == 0`` from ``_initialize`` and this
        fails with the injected OSError escaping. **This is the claim the
        sibling gate can no longer make**

RUN at HEAD, mutant applied in a copy of the tree and READ BACK OFF DISK (``sed -n '863p'`` returns ``        if not active.exists():``), full audit suite: **``1 failed, 167 passed``** — the failure being ``test_the_zero_byte_fast_path_avoids_a_read_that_can_now_raise``, with ``OSError: [Errno 5] Input/output error`` escaping exactly as its own docstring promises. Unmutated control at HEAD: ``168 passed``.

So the sentence at :5029 is false in BOTH halves. The suite does not still pass — it goes red. And the count is not 165 — it is 167 green under the mutant, 168 clean. ``git blame`` puts both docstrings in ``18fb7006`` (2026-09-07), whose message is *"tests: grade the gates -- 8 of 9 codex round-2 findings, all false-green"*.

⚡ THE PARAGRAPH CONTAINING THE FALSE CLAIM IS THE PARAGRAPH DESCRIBING THIS EXACT FAILURE MODE. Four lines below it: *"A gate written and mutation-verified that morning became decoration by lunchtime, without being edited, while staying green — the same shape as an answer invalidated by something that never touched it."* The claim above that sentence was invalidated by the test written beneath it, in the same commit, and it says so about a different gate while not noticing itself.

⛔ AND THE CONSEQUENCE IS NOT COSMETIC, BECAUSE THE FALSE HALF IS LOAD-BEARING FOR A DELETION ARGUMENT. :5022-5038 uses "the whole suite still passes" to establish that the ``st_size == 0`` clause is no longer load-bearing for correctness, and then keeps it only for a second property. A future reader running the recipe verbatim gets a RED suite where the docstring promises green, and the honest reading available to them is *"the recipe is dead"* — which is precisely the class this repo closed on 2026-09-07 for ``test_audit.py:2219`` and then reproduced here. The same "165 green" claim has a second home at ``project_memory/next_steps.md:1864``.

The fix: delete the count and the green claim, not correct them — the surviving true statement is "the sibling no longer discriminates, and this gate does", which is what :5074 already says and can be checked by running the two tests. A figure describing a suite that grows every session is a perishable; the re-derivation is the durable form. ⚠ Note ``next_steps.md:58``'s ``1900 passed`` is NOT part of this — I checked it and it is exactly right in the ``.venv`` the file's own re-derive block names (system python3 gives ``1897 passed, 3 skipped``, the three being ``test_packaging.py`` without hatchling). That candidate died on disk. [prescription: run]
  Tags: anneal-memory, code_review, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260908-034538-0d798c07c6b9
  Time: 2026-09-08T03:45:38  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: MEDIUM: anneal_memory/audit.py:946 — THE ONE ROUND-2 FIX THAT GOT NO GATE IS IN THE COMMIT WHOSE ENTIRE SUBJECT IS GRADING THE ROUND-2 FIXES. ``_seed_from_manifest``'s genesis reset closes a real, measured false-tampering verdict and NOTHING in the suite goes red when you remove it.

[quoted, audit.py:944-947]
        # Reset FIRST: this can run on an instance whose cached chain state
        # is stale, and genesis is the only defensible starting anchor.
        self._prev_hash = GENESIS_HASH
        self._seq = 0

RUN, both arms, scratch trees, mutant read back off disk. Scenario taken verbatim from the helper's own docstring (:925-929) — one entry, NO manifest, the active file emptied as a failed-fsync rollback leaves it, ``_initialized`` forced False so ``_initialize`` re-runs on a DIRTY instance, then one more append:
  HEAD                     -> ``verify valid=True``
  mutant (both reset lines deleted, i.e. the pre-extraction behaviour)
                           -> ``verify valid=False``, ``Hash mismatch at seq 1: expected sha256:GENESIS..., got sha256:efd52b5337557...``
So the fix is real and the docstring's reproduction is accurate to the error-string shape. Then the gate check: same mutant, full audit suite -> **``168 passed``**, zero red. Unmutated control -> ``168 passed``. Identical.

⛔ THE CONTEXT IS WHAT MAKES THIS A FINDING RATHER THAN A GAP. ``git blame`` puts this line in ``18fb7006``, *"tests: grade the gates -- 8 of 9 codex round-2 findings, all false-green"*, whose message says *"Every one was a test that could pass without grading its subject, which is the only class that matters when the gates are the artifact"* and then names, one by one, the arms it fixed: the guarded-region AST gate, the restore-order gate, the strict xfail, the read-failure gate's missing second exception arm, the ``note_write_failure`` fixture, the log-handler gate, a stale mutation recipe. **This fix is not in that list and is not the "1 of 9" it declines to claim** — the message's own untouched item is the ``_sick_disk`` scoping, which it names in full. The genesis reset simply never came up.

⚡ AND THE HELPER'S DOCSTRING ARGUES THE DANGER OUT LOUD: *"the inlined version it came from only ever ran on a FRESH instance… so 'leave the cached values alone when there is no manifest' was correct BY ACCIDENT OF ITS CALLER. The 2026-09-07 rollback change made ``_initialize`` re-runnable on a DIRTY instance."* A correctness property that used to be free from its caller and is now paid for by two explicit lines is exactly the property a refactor deletes as redundant — and 168 green tells the next refactorer it is.

The fix: the reproduction above is the test body, and it needs no injection machinery at all — no monkeypatch, no sick disk, four setup lines and a ``verify()``. That is cheaper than every gate ``18fb7006`` did write. ⚠ I ran the scenario and the mutant; I did NOT add the test to the suite or run it as pytest. [prescription: run]
  Tags: anneal-memory, code_quality, code_review
```

---

```
[laptop/flow]   ID: diogenes-20260908-034818-a6cfd3885c7c
  Time: 2026-09-08T03:48:18  Agent: diogenes  Type: observation
  Source: laptop_overnight_review
  Content: STILL OPEN (7), TOTAL AT HEAD 5e4b834b1f79fb18d4b456a5ead2229b1e8703c1 — anneal-memory, 2026-09-08. 6 filed tonight (2 HIGH / 4 MEDIUM / 0 LOW) + 1 carried. Filed separately: 0 DRIFT (not in this count).

THE BASELINE IS TWO POOLS AND THE SECOND ONE EXISTS ONLY BECAUSE STEP 0 WORKED. My 2026-09-07 anneal-memory run DIED: the store holds its COVERAGE-OPEN stub (diogenes-20260907-044903-a77d85f68f80), three findings and two self-corrections, and NO slot and NO coverage line. Without the stub that night reads as a quiet one. Candidates were therefore the 09-06 slot (6 at a12e66ab) PLUS the three 09-07 orphans — enumerated, NOT summed, and resolved against the TREE and the repo's own triage file rather than against my own silence.

RESOLVED, 8 of 9 CLOSED:
· All FIVE filed 09-06 are closed, and I spot-checked four of the five mechanisms on disk rather than taking the triage table's word: the wrap-destruction gate now names the artifact (``test_an_interrupt_in_the_replay_cannot_destroy_a_committed_wrap``, test_audit.py:2919, its docstring stating the sibling "never reads the continuity file") · ``close()`` sets ``_closed`` AFTER the underlying close with the retry documented (store.py:6472) · ``_parse_format_version`` exists with 4 references, the one-shared-parser fix · the batch roster is no longer hand-maintained (the AST partition test, test_store.py:395-435) · and the caller census now states the rule at CHANGELOG.md:333 + :643.
· TWO of the three 09-07 orphans are closed (``audit.py:282``'s advance moved inside the guarded region; ``test_audit.py:2219``'s dead recipe corrected in BOTH homes).
· ONE OF MY OWN IS REFUTED AND I RECORD IT AS A CLOSURE AGAINST MYSELF: ``store.py:1715``'s "~5s is the setting wearing the word measured, the real block is ~11.7s". The repo re-derived it five ways — four probe runs at 5.37/5.36/5.41/5.38 s, ``PRAGMA busy_timeout`` read back at 5000 ms, the open traced to exactly ONE ``BEGIN IMMEDIATE``, and a STANDING suite test timed by pytest at ~5.2s. A standing test is a better oracle than a probe written to settle the argument it is settling, and my 11.7s did not reproduce. The finding's load-bearing half (the contention is not NEW) was confirmed by both runs and is not what I withdraw.

THE ONE CARRIED IS RE-DERIVED, NOT REMEMBERED: ``graduation.py`` still reads ``_BARE_GRADUATION_RE = re.compile(r"\|\s*([23])x...")`` at :98, and the deliberate asymmetry is documented at :68-83 with its blast-radius argument and its own measured refutation of the "so the asymmetry is inert" clause. Held by spore-676, gated on spore-675. A ruled deferral with a live measured consequence — ten today-dated bare 4x+ lines silently skipped at flow's most recent consolidate — so it stays in the count.

⚠ TWO ITEMS ARE THE REPO'S OWN OPEN LIST AND ARE DELIBERATELY NOT IN MY 7, BECAUSE THEY ARE NOT MINE TO COUNT: "no directory fsync anywhere in audit.py" and "``verify()`` cannot see a duplicated entry whose chain is continuous", both at next_steps.md:33 and :43. Named here so a reader does not mistake my 7 for the repo's total debt.

Slot pre-flighted through the LIVE parser in real block form: ``extract_still_open`` -> ``'7'``, ``extract_measurement_split`` -> ``(6, 1)``, reconciling against the headline. Non-vacuity control (the same block with the count moved out of the parentheses) -> ``AMBIGUOUS``, correctly refused. ⚠ MY FIRST PRE-FLIGHT RETURNED ``None`` AND THE SLOT WAS NOT THE PROBLEM — I passed a bare string where the function takes a LIST of report blocks, so it iterated characters; and my first wording said "6 filed this window" where ``_FILED_RE`` reads ``\b(\d+)\s+(?:new\s+)?filed\s+tonight\b``, which is why the split half was silent while the count half was fine. Both caught before writing, both by reading the parser's source instead of trusting the probe. That is the 09-05 close ("a probe whose OUTPUT is the evidence must be validated before its output is") paying for itself twice in one command.
  Tags: anneal-memory, code_review, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260908-034918-35d7426d28b8
  Time: 2026-09-08T03:49:18  Agent: diogenes  Type: observation
  Source: laptop_overnight_review
  Content: COVERAGE: anneal-memory 2026-09-08, HEAD 5e4b834b1f79fb18d4b456a5ead2229b1e8703c1 **== origin/main VERIFIED AGAINST THE LIVE REMOTE** (`git ls-remote origin HEAD` answered 5e4b834b at start AND at finish, not a cached tracking ref). `git status --porcelain` EMPTY both ends, HEAD unchanged; everything read-only or under /tmp/dio_am8* (a `git archive HEAD` tree, a `git archive 0e6f1564` control tree, four mutant copies, one patched copy), all removed — `ls /tmp | grep -c dio_am8` = 0. `pwd -P` first: the Briefcase path is real, the Documents one is the symlink. Window `0e6f1564..HEAD` = 12 (dispatch said 11; a sorted-hash `diff` of the window set against the 24h slice returns IDENTICAL).

RE-DERIVED BY RUNNING, NOT CARRIED FORWARD:
· Full suite TWICE, on both interpreters, because the file's own numbers depend on which: system python3 -> **1897 passed / 3 skipped / 0 failed**; `.venv/bin/python3` -> **1900 passed / 0 failed**, which confirms `next_steps.md:58`'s `1900 passed` exactly and killed that candidate. The 3 skips are `test_packaging.py` without hatchling. `tests/test_audit.py` alone -> **168 passed**.
· ⛔ AND THE FIRST SUITE RUN WAS A FALSE GREEN I ALMOST BANKED: I typed `timeout 1800 python3 -m pytest`, macOS HAS NO `timeout`, and the harness reported **exit code 0** for a command that never ran. Caught by reading the output (`command not found`) instead of the exit code, and re-run under `perl -e 'alarm N; exec @ARGV'`. This is the exact trap `partnership.md` documents, hit anyway.
· `mypy anneal_memory` -> **Success: no issues found in 18 source files**. `ruff check .` -> **63 errors**, identical to the 63 I measured on 09-06, so the window added none.
· FOUR MUTANTS, each in its own copy, each **read back off disk** before running. Boundary newline deleted -> **3 failed / 165 passed** (the guard genuinely inverts) · `or active.stat().st_size == 0` deleted -> **1 failed / 167 passed**, which is how I falsified the "165 green" docstring · invalidate-first moved below the log call -> **1 failed / 167 passed** (the residual close is graded) · `_seed_from_manifest`'s genesis reset deleted -> **168 passed, ZERO RED**, which became a finding.
· EIGHT probes on disk, including a CONTROL TREE at `0e6f1564` to establish that the torn-tail brick is a regression this window introduced rather than a standing state, and the real operator surface driven end-to-end (`python3 -m anneal_memory.cli --db <store> verify` -> a bare traceback).
· 27 asserted coordinates batch-checked through `awk NR==n` before the slot: **all 27 exact, ZERO self-corrections this run.** The 09-06 close (derive and write must be SEPARATE tool calls) and the 09-05 close (never a coordinate off a diff hunk; every one via `grep -n`) both held.
· FIVE PRIOR FINDINGS SPOT-CHECKED ON DISK rather than accepted from the triage table, and one of my own accepted as REFUTED.

⚠ NOT CHECKED — STATED PLAINLY, NOT IMPLIED:
· **NO DOCKER, NO MCP TRANSPORT, NO SERVER STARTED.** `server.py:1247` is the second unwrapped `verify()` call site in my HIGH; I read it and did not drive it. The CLI half I ran.
· **PYTHON 3.13 ONLY — FIFTH NIGHT.** The 3.10 contention-fallback branch is unexercised again, and CI is where that matrix lives.
· **THE GZIP HALF OF `_iter_lines` WAS NEVER EXERCISED.** My HIGH about it measured only the plain-text branch; the `gzip.open(..., "rt")` branch carries the identical conflation by inspection and I did not plant an undecodable byte in a sealed `.gz`. The prescription covers both; the measurement covers one.
· **NO CONCURRENCY WORK AT ALL** — no two-process runs. The single-writer contract was assumed, as the module asks, not tested.
· **~200 of `next_steps.md`'s 805 new lines read**, the rest reached only by targeted grep; `CHANGELOG.md`'s +159 checked at three points, not audited. ⛔ **`project_memory/diogenes_20260907.md` (+187) is MY OWN routed report and I audited its PROVENANCE only** — I confirmed its coordinates are anchored, because its first block is the COVERAGE-OPEN stub carrying HEAD `0e6f1564`, so a reader resolving `audit.py:282` against today's tree has the anchor. I did NOT re-check its 30-odd coordinates against that commit. My own routed report has been the vector twice (nowhere's invented class name into shipped source; levain's two false carries into a triage file), and it is unaudited again.
· **NO ROTATION OR RETENTION PATH BEYOND A FORCED `_last_week` FLIP** in probes; `_adopt_orphaned_files` never exercised; `_cleanup` never run.
· ⛔ **A CANDIDATE I COULD NOT CLOSE AND DID NOT FILE, NAMED RATHER THAN BURIED:** `_load_manifest` returns whatever `json.loads` gives it with no dict check, so a manifest holding a JSON array would reach `manifest["files"].append(...)` in `_rotate_if_needed`. I could not establish a path that WRITES a non-dict manifest — `_save_manifest` is the only writer and always takes a dict — so I could not call it reachable, and I am not clearing it either.
· ⚠ **AND THE SHARPEST GAP IS IN MY OWN HIGH:** the fix I executed for `_read_last_valid_entry` retires the `UnicodeDecodeError` arm of an existing gate, and I say so in the finding. I ran the fix's effect on the defect; I did NOT run the suite against the fixed tree to enumerate what else it breaks. The diagnosis is `run`; the prescription is one measured step short of it.
[prescription: none]
  Tags: anneal-memory, code_review, semantic
```
