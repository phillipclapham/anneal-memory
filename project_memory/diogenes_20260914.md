# DIOGENES OVERNIGHT — anneal-memory, 2026-09-14

**Source selector:** `episodic.py read --since 24h --tags anneal-memory  (filtered to Agent: diogenes at 2026-09-14*)`
**Stated count: 4 episode(s).** Severity: MEDIUM 1 — 1 of 4 episode(s) carry a severity; the other 3 are COVERAGE 1 · COVERAGE-OPEN 1 · STILL OPEN 1
**STILL OPEN: 1**

> ⚠ **UNTRIAGED AND VERBATIM.** Generated by `scripts/route_diogenes.py` from the episodic store without judgement. Nothing here has been checked against disk except where a line says so explicitly. Diogenes' own `[prescription: ...]` tags are his assessment, not a ruling.

---

```
[laptop/flow]   ID: diogenes-20260914-020910-26e081db62a6
  Time: 2026-09-14T02:09:10  Agent: diogenes  Type: observation
  Source: laptop_overnight_review
  Content: COVERAGE-OPEN: anneal-memory 2026-09-14, HEAD 42ae8de71830 (== origin/main? yes — `git ls-remote origin HEAD` answered 42ae8de71830 live), `git status --porcelain` empty, window a498101..42ae8de = 20 commits on main (304f242..42ae8de; branches audit-r10/r10b/hybrid/r10b3-fix are OFF main and out of this window). REVIEW STARTED, NOT FINISHED — if no COVERAGE line for this repo exists in this run, THIS RUN DIED and the finding list is PARTIAL.
  Tags: anneal-memory, code_review, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260914-021257-19da2ecd6956
  Time: 2026-09-14T02:12:57  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: MEDIUM: anneal_memory/audit.py:1449-1456 (with :1351) — LIVE AT main HEAD 42ae8de, INTRODUCED IN-WINDOW by round 9 `4d00ca7`, ALREADY FIXED AND PINNED ON UNMERGED `origin/audit-r10b` (tests/test_audit.py:6726 `test_a_leftover_copy_of_a_manifested_week_is_set_aside_not_adopted`, carried onto `trial-hybrid-rebase-01b2ed8`). Filed because main is the tree flow's editable venv runs under the FF hold (spore-1019), so until the merge this is what the live store executes. Round 9 moved the duplicate-.jsonl delete to AFTER the manifest save, but a failed unlink only logs, and a leftover `.jsonl` beside its now-manifested `.gz` is a name absent from `known_files` that passes `_is_sealed_filename`. The next `_initialize` therefore adopts it as a SECOND record for the same week, and `verify()` returns a permanent false tampering verdict. A crash between save and unlink reaches the same state (reasoned, not run). [quoted: `self._save_manifest(manifest)` / `for dup in deferred_deletes:` / `logger.warning("Failed to remove duplicate orphan: %s", dup.name)` and `if path.name not in known_files and _is_sealed_filename(path.name, stem):`]
BEHAVIOURAL, so the signature and no harness. Setup: a trail with 3 entries, active bytes copied to `memory.audit.2026-W36.jsonl` plus an identical `.jsonl.gz`, active removed, no manifest. The first init's unlink of the `.jsonl` raises OSError; then a second init. Observed on main: manifest files after first init `['memory.audit.2026-W36.jsonl.gz']`; after second init `['memory.audit.2026-W36.jsonl.gz', 'memory.audit.2026-W36.jsonl']`; verify `valid=False total_entries=3 files_verified=1 error='Hash mismatch at seq 0: expected sha256:<W36 last hash>..., got sha256:GENESIS...'`. Control (unlink succeeds): one record, `valid=True total_entries=5 files_verified=2`. Same setup on both the r10b (01b2ed8) and trial-rebase (b009653) trees: one record, valid=True, 5/2. The live ~/.anneal-memory holds NO uncompressed sealed `.jsonl` tonight, so no store is in this state now.
Fix: none new; it is the merge the pickup already sequences. Do not back-port a main-only patch under the FF hold. [prescription: run]
  Tags: anneal-memory, code_quality, code_review
```

---

```
[laptop/flow]   ID: diogenes-20260914-021304-15773cb42719
  Time: 2026-09-14T02:13:04  Agent: diogenes  Type: observation
  Source: laptop_overnight_review
  Content: STILL OPEN (1), total at main HEAD 42ae8de: the one is tonight's MEDIUM (audit.py:1449 failed-unlink re-adoption, introduced by round 9 4d00ca7). It is ALREADY FIXED and test-pinned on unmerged origin/audit-r10b, and I measured the fix on both the r10b and trial-rebase trees, so it closes at the merge with no new work. The 09-09 slot's 9 were dispositioned by the seat in d37fcc4. I read the code half of that HIGH in this window's diff (all manifest readers now `read_bytes()` + the `_UNPARSEABLE_JSON` tuple, a superset of UnicodeDecodeError), but did NOT re-verify the record-only items, so those stay OUT of N as seat-closed, not re-checked by me. Also OUT of N and UNVERIFIED by me tonight: the seat's own tracked-at-HEAD items from next_steps.md (manifest wipe via `_load_manifest`'s fresh-manifest fallback on an unreadable manifest → the hybrid branch; rotation race B; retention M4; `verify --json` exit 0). Those are the seat's record, not my measurement. Filed separately: 0 DRIFT (not in this count).
  Tags: anneal-memory, code_review, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260914-021315-4d157f05e068
  Time: 2026-09-14T02:13:15  Agent: diogenes  Type: observation
  Source: laptop_overnight_review
  Content: COVERAGE: anneal-memory 2026-09-14, HEAD 42ae8de == origin/main (live `git ls-remote`), porcelain empty, window a498101..42ae8de = 20 commits; the last code commit is 4d00ca7 (round 9), and `git diff --quiet 2ed7579 HEAD -- anneal_memory` passed, confirming the pickup's "code-unchanged" claim. OPENED AND READ: the full audit.py window diff (_parse_manifest_bytes, _require_entry_dict, log() pre-validation, verify() unmanifested-file check + _guarded_lines, _seed_from_manifest, _adopt_orphaned_files dedup/deferred delete, _rotate_if_needed, _load_manifest, _iter_lines/_is_corrupt), HEAD audit.py 1171-1650 in full, the cli.py cmd_audit diff + its stem derivation, the CHANGELOG diff, the d37fcc4 disposition commit, and the next_steps.md pickup top. RAN: tests/test_audit.py + test_cli.py + test_store.py on main's tree, 701 passed (pytest from the repo root, cwd-first import; __file__ not separately asserted for that run). A scratch reproduction under /tmp with an asserted __file__, against main AND against git-archive extracts of origin/audit-r10b (01b2ed8) and origin/trial-hybrid-rebase-01b2ed8 (b009653), repo untouched, no worktree registered. A read-only `ls` of ~/.anneal-memory (16 sealed .gz W22-W37, no uncompressed sealed .jsonl). `git grep` of the branch tests for a failed-unlink pin (found, r10b :6726). NOT CHECKED: the full suite (only those 3 files); tests/test_audit.py's 1121 added lines read line by line (only grepped); the CHANGELOG prose checked claim by claim against the code (read, not walked); the crash-between-save-and-unlink variant (reasoned, only the failed-unlink variant run); `_load_manifest`'s fresh-manifest fallback, whose stricter round-6/7 parse (duplicate names, stem-bound filenames) now also triggers the wipe the hybrid exists for (read, not reproduced, left to the seat's hybrid track); the seat's untriaged re-pass c7c73130 and every unmerged branch beyond that one reproduction; server.py/store.py consumers of AuditTrail; next_steps.md's 534 added lines beyond the pickup top; Windows/fsync paths; a real second process. [prescription: none]
  Tags: anneal-memory, code_review, semantic
```
