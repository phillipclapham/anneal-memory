# DIOGENES OVERNIGHT — anneal-memory, 2026-09-15

**Source selector:** `episodic.py read --since 24h --tags anneal-memory  (filtered to Agent: diogenes at 2026-09-15*)`
**Stated count: 4 episode(s).** Severity: MEDIUM 1 — 1 of 4 episode(s) carry a severity; the other 3 are COVERAGE 1 · COVERAGE-OPEN 1 · STILL OPEN 1
**STILL OPEN: 1**

> ⚠ **UNTRIAGED AND VERBATIM.** Generated by `scripts/route_diogenes.py` from the episodic store without judgement. Nothing here has been checked against disk except where a line says so explicitly. Diogenes' own `[prescription: ...]` tags are his assessment, not a ruling.

---

```
[laptop/flow]   ID: diogenes-20260915-020106-54c087bb2679
  Time: 2026-09-15T02:01:06  Agent: diogenes  Type: observation
  Source: laptop_overnight_review
  Content: COVERAGE-OPEN: anneal-memory 2026-09-15, HEAD 4080eb8fc136bd25cf0b77a65735c26401919cc5 (== origin/main? yes, verified via `git ls-remote origin HEAD`), `git status --porcelain` empty, window f69578d..4080eb8 = 27 commits (28 since 09-14 02:00 counting the f69578d routing commit). REVIEW STARTED, NOT FINISHED — if no COVERAGE line for this repo exists in this run, THIS RUN DIED and the finding list is PARTIAL.
  Tags: anneal-memory, code_review, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260915-020532-1b81613b59d4
  Time: 2026-09-15T02:05:32  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: MEDIUM: anneal_memory/audit.py:2526 (`_open_regular`, introduced in-window by 61c3c3e, shipped in 0.9.10) — the single read path for every audit file calls `os.open` WITHOUT `O_BINARY`, so on Windows the descriptor is in CRT TEXT mode, and `os.fdopen(fd, "rb")` does not change that. `_iter_lines` (:2537ff) now wraps that descriptor in `gzip.GzipFile(fileobj=raw, mode="rb")`. v0.9.9 used `gzip.open(path, ...)`, which opens binary. STRUCTURAL, so the evidence is the documents, not a harness. Python's os docs: "on Windows adding O_BINARY is needed to open files in binary mode". Microsoft `_read`: "In text mode, each carriage return-line feed pair \r\n is replaced with a single line feed character \n" and "the read terminates when _read encounters a CTRL+Z character". Compressed bytes for any real week will contain 0x0D0A or 0x1A, so on Windows every sealed `.gz` read would surface as `_CorruptAuditFile`. That breaks `verify()`, `anneal-memory audit`, orphan adoption (`_scan_sealed`), `_seed_from_sealed_tail` and `repair_manifest`: `verify()` returns valid=False for every rotated trail. The manifest and .jsonl reads survive, since JSON lines hold no CR or 0x1A. Scope: nothing in CI runs Windows (`.github/workflows/test.yml` is ubuntu-latest only) and pyproject has no OS classifier. But the same window's CHANGELOG ships a Windows-specific rotation fsync fix, and this function's own docstring asserts Windows behaviour ("Where the platform has no O_NONBLOCK (Windows) there are no FIFOs to open, and the fstat check still applies"), so the module does claim Windows. NOT run on Windows. That `io.FileIO` reads through CRT `_read` on a fd it did not open is reasoned from CPython's fileutils `_Py_read`, not run. Fix: `fd = os.open(path, os.O_RDONLY | _O_NONBLOCK | getattr(os, "O_BINARY", 0))`, with a test asserting the flag is passed (monkeypatch `os.open`), since CI cannot exercise Windows. [quoted: `fd = os.open(path, os.O_RDONLY | _O_NONBLOCK)` / `return os.fdopen(fd, "rb")` / `with gzip.GzipFile(fileobj=raw, mode="rb") as f:`] [prescription: reasoned]
  Tags: anneal-memory, code_quality, code_review
```

---

```
[laptop/flow]   ID: diogenes-20260915-020532-4b888bc475d3
  Time: 2026-09-15T02:05:32  Agent: diogenes  Type: observation
  Source: laptop_overnight_review
  Content: STILL OPEN (1), total at main HEAD 4080eb8: the one is tonight's MEDIUM (audit.py:2526 `_open_regular` without O_BINARY, Windows text-mode reads of sealed .gz; reasoned from docs, not run on Windows). 09-14's MEDIUM (failed-unlink re-adoption) is CLOSED at HEAD, checked against the repo, not my store: round 10b merged to main in 052f735, its pin `test_a_leftover_copy_of_a_manifested_week_is_set_aside_not_adopted` is at tests/test_audit.py:6740, recovery now never deletes (`_set_aside`), and the suite passed at HEAD. OUT of N and UNVERIFIED by me: the seat's own CANDIDATE list in next_steps.md (the repair empty-string prev_hash fallback, repair's .gz-preferred pick before the chain check, `_verify_listed`'s empty-trail "appeared" filter ignoring marker names, 09-14's `_parse_manifest_bytes` last_ts/last_hash) and the ruled codex #1 lock gap (shipped with a release-notes line). Those are the seat's record, not my measurement. Filed separately: 0 DRIFT (not in this count).
  Tags: anneal-memory, code_review, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260915-020532-a64433cf65b5
  Time: 2026-09-15T02:05:32  Agent: diogenes  Type: observation
  Source: laptop_overnight_review
  Content: COVERAGE: anneal-memory 2026-09-15, HEAD 4080eb8 == origin/main (live `git ls-remote`), porcelain empty, window f69578d..4080eb8 = 27 commits. The code lands in 07d276a/052f735 (0.9.10); after that come a version bump and next_steps only. OPENED AND READ: the full window diff of anneal_memory/audit.py (quarantine markers, `_load_manifest`/`_quarantine_manifest`, `verify()` settle loop + `_verify_once`/`_verify_listed`, `repair_manifest`, `_seed_from_sealed_tail`, `_adopt_orphaned_files` + `_scan_sealed` + `_set_aside`, the reordered `_rotate_if_needed`, `_open_regular`/`_read_regular_bytes`/`_iter_lines`, `_rotation_in_flight`, `_unmanifested_sealed_names`, `_stat_signature`/`_signatures_match`, `_sealed_record`, `_first_prev_hash`); cli.py (cmd_verify anchor note, cmd_audit_repair, the bracketed `_read_audit_entries`/cmd_audit retry); server.py and __init__.py; the CHANGELOG 0.9.10 section; the README line; the next_steps.md pickup top. Also read at HEAD: `_initialize`, `_seed_from_manifest`, `_cleanup`, `_save_manifest`, the verify walk's per-file seq rule, rotation's seq reset, the rotation early-return branch, and test_audit.py:5674. KILLED ON DISK: a candidate that quarantine seeding or repair's `active_last_seq: 0` would false-flag seq across files. Rotation resets `_seq` to 0 (audit.py:2244) and verify checks seq within each file only. RAN: the full suite at HEAD, 2017 passed / 3 skipped. The import was proved to be the repo tree by a pytest-run probe asserting `anneal_memory.__file__` under /Users/phillipclapham/Briefcase/anneal-memory/ at 0.9.11.dev0 (pyproject `pythonpath = ["."]`). flow's venv separately holds the 0.9.10 wheel. WebFetched the Python os.open and MS `_read` docs for the finding. NOT CHECKED: anything on Windows (the finding is reasoned, not run); tests/test_audit.py's +1420 and test_cli.py's +435 lines read line by line (only grepped and run); no scratch reproduction of any concurrency path (rotation-vs-verify settle, cmd_audit's 3-attempt bracket, which retries with no delay between attempts, repair vs a concurrent quarantine); the CHANGELOG checked claim by claim against code (read, not walked); the seat's four CANDIDATE items (not re-derived); the real ~/.anneal-memory store (not touched tonight); store.py consumers of AuditTrail; next_steps.md beyond the pickup; the unmerged superseded branches. [prescription: none]
  Tags: anneal-memory, code_review, semantic
```

---
## SECOND LINEAGE (glm) CANDIDATES — appended by the 2026-09-15 morning ritual, UNVERIFIED
glm is a heuristic site match; the grounding floor quarantined 53-60% of this seat's output in the 08-17 bake. Verify against disk before acting.
- `anneal_memory/audit.py:1952` (`_adopt_orphaned_files`): with the manifest lost after a retention cleanup, the surviving sealed files may never be adopted because the first readable orphan's `first_prev_hash` is not GENESIS and no `chain_anchor` survives. Not checked.
- `anneal_memory/audit.py:1776` (`_seed_from_sealed_tail`): `audit_dir.iterdir()` has no `OSError` guard, so a directory that is writable but not searchable (0o300) raises `PermissionError` rather than `_ManifestQuarantined`. The ritual confirmed the call is unguarded at the site. Whether a caller catches it was NOT checked.
- `anneal_memory/audit.py:1262` (`verify()`): if the directory vanishes between the empty-trail first and second listings, the second listing's error path differs from the first listing's `valid=True`. Likely consistent with the first-listing design; not checked.
