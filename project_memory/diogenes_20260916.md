# DIOGENES OVERNIGHT — anneal-memory, 2026-09-16

**Source selector:** `episodic.py read --since 24h --tags anneal-memory  (filtered to Agent: diogenes at 2026-09-16*)`
**Stated count: 8 episode(s).** Severity: LOW 2 · MEDIUM 3 — 5 of 8 episode(s) carry a severity; the other 3 are COVERAGE 1 · COVERAGE-OPEN 1 · STILL OPEN 1
**STILL OPEN: 5**

> ⚠ **UNTRIAGED AND VERBATIM.** Generated by `scripts/route_diogenes.py` from the episodic store without judgement. Nothing here has been checked against disk except where a line says so explicitly. Diogenes' own `[prescription: ...]` tags are his assessment, not a ruling.

---

```
[laptop/flow]   ID: diogenes-20260916-021806-b51846f3745d
  Time: 2026-09-16T02:18:06  Agent: diogenes  Type: observation
  Source: laptop_overnight_review
  Content: COVERAGE-OPEN: anneal-memory 2026-09-16, HEAD baeaa1ef6a5d3d93b5f45eabb6e2f7602b48840a (== origin/main? yes — `git ls-remote origin HEAD` answered baeaa1ef at start), `git status --porcelain` <empty>, window b2b80ae..baeaa1e = 25 commits. REVIEW STARTED, NOT FINISHED — if no COVERAGE line for this repo exists in this run, THIS RUN DIED and the finding list is PARTIAL.
  Tags: anneal-memory, code_review, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260916-023039-44ba03edfb05
  Time: 2026-09-16T02:30:39  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: MEDIUM: anneal_memory/continuity.py:2633 — THE WINDOW'S OWN codex HIGH ("a committed save no longer reports failure under an error warnings-filter") WAS FIXED ON THE WARNING CHANNEL ONLY. The identical failure is still live one call lower, on the DB channel, at the one post-commit store call the pipeline leaves unguarded.

The window added `_warn_after_commit` and stated the invariant it protects as a ⛔ rule:

[quoted, continuity.py:1645-1646]
    ⛔ Once the batch has committed, the renames have run and the wrap token is
    cleared, nothing may make the caller believe the save failed.

The post-commit audit flush already honours it — `# an audit log failure must not cause the pipeline to report failure to the caller. L3 complement F4.` (:2535-2539) wrapping `if store._audit is not None:` / `try:` (:2540-2541). `prune()`'s own AUDIT half was guarded on 2026-09-04 for exactly this reason, and store.py:4165-4166 says why in its own words: "``validated_save_continuity`` calls prune AFTER the wrap commit, so it reached the canonical pipeline."

The DB half of the same call was never guarded:

[quoted, continuity.py:2633]
        pruned = store.prune()

`prune()` runs its SQL inside `with self._db_boundary("prune"):` (store.py:4133), so any `sqlite3.Error` — disk full, locked DB, I/O error — surfaces as `StoreDatabaseError` and propagates straight out of `validated_save_continuity`. By then `db_committed` is True, both renames have run (:2452, :2486) and `wrap_completed` has cleared the token inside the batch. A post-commit store census over :2528-:2900 returns exactly three store calls — the audit block, the retention check, and this one — so `prune()` is the only unguarded site, not a sample.

RUN (diagnosis), scratch store at /tmp/dio_am16/probe, HEAD baeaa1e, a `Store(..., retention_days=30)` with a single `sqlite3.OperationalError("database or disk is full")` injected at prune's own `SELECT ... FROM episodes WHERE timestamp <` and nothing else touched. FALSIFIABLE SIGNATURE, five observations that must all reproduce together on an independently-built harness:
  1. `validated_save_continuity` raises `StoreDatabaseError` whose `.operation` is the string `prune`;
  2. `store.load_continuity()` CONTAINS the new wrap's text (the save committed and renamed);
  3. `store.load_wrap_snapshot()` is `None`;
  4. `len(store.get_wrap_history())` is 1;
  5. the operator's immediate retry with the SAME wrap_token raises `ValueError` whose message begins "No wrap in progress — nothing to save."
That is the codex HIGH's symptom verbatim — save succeeded, caller told it failed, retry refused — on a channel its fix did not cover. Reachability is narrower than the warnings-filter case (retention_days must be configured AND SQLite must fail), which is why this is MEDIUM and not HIGH; the blast radius is identical.

Fix (NOT executed, NOT run): wrap the `store.prune()` call the way :2540 wraps the audit flush — catch, surface the prune failure through the result or a `_warn_after_commit` message, and let the committed save return success. Retention is a housekeeping step; it must not be able to fail a wrap that is already on disk.

[prescription: reasoned — the DIAGNOSIS is run and reproduced in a scratch dir; the FIX above is reasoned from the adjacent guarded site and was never executed or tested]
  Tags: anneal-memory, code_quality, code_review
```

---

```
[laptop/flow]   ID: diogenes-20260916-023105-65c2561f8526
  Time: 2026-09-16T02:31:05  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: MEDIUM: CHANGELOG.md:51 — THE WINDOW ADDED WINDOWS CI AND LEFT THREE RECORDS SAYING IT DOES NOT EXIST, INCLUDING ONE THAT SHIPS IN THE SDIST AND ONE THAT TELLS THE NEXT FIXER A RESIDUE IS UNDISCHARGED WHEN CI HAS SINCE DISCHARGED IT.

`7e28510` promoted a real `test-windows` job. At HEAD:

[quoted, .github/workflows/test.yml:72]
    runs-on: windows-latest

Three sites in the same window still deny it, all quoted verbatim from disk:

[quoted, CHANGELOG.md:50-51 — inside the `## [0.9.11] — 2026-09-15` O_BINARY entry]
every other write are unchanged. Reasoned from the Python and Microsoft documentation, not run on
Windows: nothing in this project's CI runs Windows.

[quoted, project_memory/next_steps.md:124]
- ⚠ RESIDUE, UNDISCHARGED: never run on Windows. No Windows host here and none in CI (`grep -n runs-on .github/workflows/test.yml`). The test proves the flag reaches `os.open`, not that a Windows read of a sealed `.gz` succeeds.

[quoted, tests/test_audit.py:8011-8013 — the docstring of the O_BINARY test added this window]
        is in CRT text mode and a sealed ``.gz`` reads as corrupt. CI has no
        Windows, so this asserts the flag reaches ``os.open``: the platform value

Each is grounded against HEAD, not inferred:
· CHANGELOG.md is in `EXPECTED_SDIST_TOP_LEVEL` (tests/test_packaging.py:95), so the false claim ships to PyPI. It is also the GitHub-visible file, two directories from the workflow that refutes it.
· next_steps.md:124 hands the reader a re-derive command that REFUTES its own sentence — `grep -n runs-on .github/workflows/test.yml` returns `windows-latest` at :72 — and it contradicts line 26 of the same file ("`.github/workflows/test.yml` now has a real `test-windows` job (windows-latest, Python 3.13, no `continue-on-error`)"). A pickup file that denies a fact its own top asserts is the shape a cold reader cannot resolve.
· And the RESIDUE ITSELF IS DISCHARGED, which is the part that makes this more than tidiness. RUN: `gh run view 34986410969` (the latest main run, headSha `baeaa1ef` == HEAD) reports `pytest (Windows, Python 3.13) -> success` alongside the four Ubuntu jobs and mypy. All five `skipif(sys.platform == "win32")` markers in tests/test_audit.py (:6835, :7012, :7082, :7577, :7697) are chmod-based; `TestMultiRotationIntegration::test_multi_rotation_verify_and_recovery` (tests/test_audit.py:1228) carries none and asserts `result.files_verified == 2  # W10.gz + active` — i.e. a real Windows read of a sealed `.gz` through `_open_regular` -> `gzip.GzipFile` runs green in CI at HEAD. The line that says "never run on Windows" is false in both of its clauses, and it is the line a fixer would read before deciding whether the O_BINARY work is finished.

⛔ THE DIRECTION OF THE ERROR IS WHAT COSTS: all three under-claim, so a reader distrusts a fix that CI has actually exercised, and the next seat re-opens closed work. An out-of-date instruction is worse than an out-of-date fact.

Fix: CHANGELOG — the 0.9.11 entry is a dated receipt, so amend rather than rewrite ("not run on Windows at release time; Windows CI landed the same day in the unreleased work below"), and note it in the [Unreleased] entry that this window also owes. next_steps.md:124 — replace with the discharge, naming run 34986410969 and the unskipped test. tests/test_audit.py:8011-8013 — drop "CI has no Windows" and say what the test actually buys (the flag reaches `os.open` on a platform that lacks the constant), which is still worth saying. STRUCTURAL: all three coordinates, quoted code and edits are here; nothing is withheld.

[prescription: reasoned — the CI-green and unskipped-test measurements are run; the three edits above are reasoned and were not applied]
  Tags: anneal-memory, code_review, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260916-023123-b3884edf0289
  Time: 2026-09-16T02:31:23  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: MEDIUM: CHANGELOG.md:5 — `[Unreleased]` IS EMPTY WHILE README:32 TELLS USERS TO EXPECT A BEHAVIOUR CHANGE "IN THE NEXT RELEASE". Whoever cuts 0.9.12 renames an empty heading and ships the window's two user-facing changes with no notes.

[quoted, CHANGELOG.md:4-7]

## [Unreleased]

## [0.9.11] — 2026-09-15

Nothing sits between them. Yet main at HEAD carries, after the `v0.9.11` tag (`153823d`):
· the CLI stdio UTF-8 fix — `57115f7` + `f0c765d`, `anneal_memory/cli.py:3627-3633`, which changes what `print()` does under a non-UTF-8 locale on every platform and what `record -` / `save-continuity -` do with piped non-ASCII stdin. `next_steps.md:106` calls it "a real, user-facing defect, not test-only";
· the Windows CI gate — `7e28510`, `.github/workflows/test.yml:62-90`.

README.md already promises the first of these to every PyPI reader:

[quoted, README.md:32]
- **CLI output is UTF-8 on every platform, including piped, redirected, and subprocess consumption** — unreleased as of `v0.9.11`, first shipping in the next release.

And next_steps.md:29 records the intent: "this Windows-CI work is unreleased code on main, shipping in whatever cuts next." So three artifacts agree there is unreleased user-facing work, and the file whose entire job is to record it is blank. The concrete failure: at cut time `[Unreleased]` becomes `[0.9.12]`, a user upgrading sees release notes that do not mention the encoding change, and the README bullet that promised it is the only trace — which is the same doc-vs-artifact mismatch this project already filed against itself once (CHANGELOG.md:2125, the v0.3.3 "`[Unreleased]` section cleared" entry).

This is a release-hygiene defect, not a code defect, and the reason it is MEDIUM rather than LOW is that the README makes the forward promise in the shipped package description: `readme = "README.md"` makes it the PyPI long_description, so the promise is public and the notes that would honour it do not exist.

Fix: write the [Unreleased] entries now, while the diff is still legible — a `### Fixed` for the CLI stdio UTF-8 reconfigure (naming the stdin half and the deliberate `backslashreplace` on stderr) and a `### Changed`/`### Added` for the Windows CI gate plus the disclosed limits the README now carries. STRUCTURAL: nothing withheld.

[prescription: reasoned]
  Tags: anneal-memory, code_review, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260916-023141-951c78b65799
  Time: 2026-09-16T02:31:41  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: LOW: README.md:32 — THE SECOND BULLET UNDER "three disclosed limits" STATES A CAPABILITY, NOT A LIMIT, AND NEVER SAYS WHAT THE RELEASED VERSION ACTUALLY DOES. A Windows user reading the disclosure section for the behaviour they will hit gets the behaviour they will NOT hit.

[quoted, README.md:29]
anneal-memory runs on Windows, with three disclosed limits:

[quoted, README.md:32]
- **CLI output is UTF-8 on every platform, including piped, redirected, and subprocess consumption** — unreleased as of `v0.9.11`, first shipping in the next release.

The bolded lede — the part a scanning reader takes — asserts the FIXED behaviour. The limit is only implied by a trailing clause, and the actual released behaviour is never stated anywhere in the section. next_steps.md:106 has the sentence that is missing: under a non-UTF-8 locale encoding (a Windows console codepage, or any piped/redirected/subprocess consumption on any platform) the CLI's own glyphs raise `UnicodeEncodeError`, and `record -` / `save-continuity -` mis-decode piped non-ASCII into corrupted store content. Neither consequence appears in the README.

Two second-order problems in the same line:
· `readme = "README.md"` in pyproject.toml makes this the PyPI long_description, and the section is not yet published (it landed in `3baf389`, after the `153823d` release commit) — so the first audience to see it will be 0.9.12 users, for whom "unreleased as of `v0.9.11`, first shipping in the next release" describes a release they are already past. The bullet is wrong for the 0.9.11 reader by omission and wrong for the 0.9.12 reader by tense.
· The heading promises three limits and four bullets follow. Bullets 1, 3 and 4 (advisory locking, the eager default-db-path crash, the reduced CI subset) are each defensible as a limit, so the count is only reconcilable by reading bullet 2 as something other than a limit — which is precisely the defect above, showing through the arithmetic.

Fix: state the limit in the lede and the fix in the clause, version-anchored rather than relative — e.g. "**In v0.9.11 and earlier, CLI output uses the locale encoding**, so the CLI's own status glyphs raise `UnicodeEncodeError` under a Windows console codepage or any piped/redirected consumption, and piped non-ASCII stdin can be silently mis-decoded into the store. Fixed in v0.9.12: stdin, stdout and stderr are UTF-8 on every platform." Then either say "four disclosed limits" or move the CI-coverage bullet out from under the count. STRUCTURAL: nothing withheld.

[prescription: reasoned]
  Tags: anneal-memory, code_review, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260916-023157-ad0d1b991c16
  Time: 2026-09-16T02:31:57  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: LOW: .github/workflows/test.yml:8 — THE FILE'S OWN HEADER MAKES AN EXPLICIT COMPLETENESS CLAIM THAT THE SAME WINDOW FALSIFIED SIXTY LINES BELOW IT.

[quoted, .github/workflows/test.yml:3-8]
# Minimal CI for anneal-memory — runs the full pytest suite on every
# push and PR across the supported Python versions, plus a mypy gate.
#
# anneal-memory is zero-dep (stdlib only), so no dependency install
# is needed beyond the package itself and the dev tooling. This
# workflow is deliberately small: test matrix, lint gate, done.

"test matrix, lint gate, done" is an enumeration that closes itself with the word "done". At HEAD the file defines three job kinds, not two:

[quoted, .github/workflows/test.yml:62 and :71-72]
  test-windows:
    name: pytest (Windows, Python 3.13)
    runs-on: windows-latest

`7e28510` added the job and did not touch the header. The header is the first thing a contributor reads to learn what this CI does and what it deliberately omits; it now under-reports a required gate that can fail a PR, and the omission reads as intentional because of that closing "done". Not a `DRIFT:` item — the file's code changed in the window (the job is new), so this is a FINDING at its real severity, and the severity is LOW because nothing executes the comment.

Fix: amend the second paragraph to name the Windows job and, more usefully, say WHY it exists in one clause (the failures a Windows user hits are platform-specific, which the job's own comment at :63-70 already says well) — then drop or re-word the closing "done" so the next added job does not silently falsify it again.

[prescription: reasoned]
  Tags: anneal-memory, code_review, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260916-023223-40b927a53544
  Time: 2026-09-16T02:32:23  Agent: diogenes  Type: observation
  Source: laptop_overnight_review
  Content: STILL OPEN (5), TOTAL AT HEAD baeaa1ef == origin/main, resolved against the TREE and the repo's own pickup file, not against my store. Filed separately: 0 DRIFT (not in this count).

The five, all filed tonight, all grounded at HEAD: MEDIUM continuity.py:2633 (post-commit `store.prune()` reports a committed save as failed; reproduced) · MEDIUM CHANGELOG.md:51 + next_steps.md:124 + tests/test_audit.py:8012 (three records deny the Windows CI this window added; counted ONCE, one class, one fix pass) · MEDIUM CHANGELOG.md:5 (empty `[Unreleased]` against README:32's public promise) · LOW README.md:32 (the "limit" bullet that states a capability) · LOW .github/workflows/test.yml:8 (header's closing "done" now false).

BASELINE RESOLVED TO ZERO CARRIED. My 2026-09-15 slot was STILL OPEN (1) at HEAD `4080eb8` — the `_open_regular` O_BINARY MEDIUM. CLOSED at HEAD, checked against the tree: `anneal_memory/audit.py:2523` now reads `fd = os.open(path, os.O_RDONLY | _O_NONBLOCK | getattr(os, "O_BINARY", 0))`, landed in `10679ab`, pinned by `tests/test_audit.py:8010 test_open_regular_passes_o_binary_to_os_open`, and the residue the seat recorded as undischarged is discharged by the Windows CI job it also merged (run 34986410969 at headSha baeaa1ef: `pytest (Windows, Python 3.13) -> success`, with the sealed-`.gz` verify test at tests/test_audit.py:1228 carrying no win32 skip). The finding is closed; the RECORD of it is not, which is why that record is one of tonight's five.

OUT of N and UNVERIFIED BY ME — the seat's and the fan-in's own records, not my measurement, and I did not re-derive them: the 0.9.12 override-tamper-evidence gap (next_steps.md:116 — an `allow_unlinked` or `allow_shrink` override leaves no audit trace; DESK-VERIFIED at `v0.9.11`, not by me) · the CONFIRMED-LOW stdin-reconfigure-after-partial-read gap (next_steps.md:107, cli.py:3627) · the eager default-db-path startup crash at cli.py:3073 + server.py:1206 (I confirmed both coordinates hold verbatim on disk; I did not establish reachability myself) · the three-site advisory-lock no-op on Windows (store.py:1002, crystal.py:461, spores.py:412 — all three confirmed on disk as `if fcntl is not None:` guards, disclosed in README, deliberately not fixed) · the AM-LINKGATE one-pair-capable-line gap named in the block's own docstring · the 103 + 73 encoding-less test I/O sites (next_steps.md:111-112 — I re-ran both greps at HEAD and got exactly 103 and 73, so the counts hold; the desk ruled them not swept).
  Tags: anneal-memory, code_review, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260916-023322-b7ed5c5af6ea
  Time: 2026-09-16T02:33:22  Agent: diogenes  Type: observation
  Source: laptop_overnight_review
  Content: COVERAGE: anneal-memory 2026-09-16, HEAD baeaa1ef6a5d3d93b5f45eabb6e2f7602b48840a **== origin/main VERIFIED AGAINST THE LIVE REMOTE** (`git ls-remote origin HEAD` answered baeaa1ef at start AND at finish, not a cached tracking ref). `git status --porcelain` EMPTY both ends, HEAD unchanged; everything read-only or under /tmp/dio_am16 (a `git archive b2b80ae` control tree + one probe store), removed — `ls /tmp | grep -c dio_am16` = 0. `pwd -P` = /Users/phillipclapham/Briefcase/anneal-memory; `~/Documents/anneal-memory` is the symlink, not the real path. Window `b2b80ae..baeaa1e` = 25 (dispatch said 25; the 24h slice is 26, the extra being `b2b80ae` itself, my own 2026-09-15 routing commit — the same shape for the fourth time on this repo).

RE-DERIVED BY RUNNING, NOT CARRIED FORWARD:
· Full suite at HEAD: **2032 passed / 3 skipped / 0 failed** (42-44s, run twice). The 3 skips are `tests/test_packaging.py` without hatchling, confirmed by `-rs`. Interpreter was flow's venv python3; `pyproject` `pythonpath = ["."]` puts the repo tree first, and the probe printed `anneal_memory.__file__` under /Users/phillipclapham/Briefcase/anneal-memory at 0.9.12.dev0.
· `mypy anneal_memory` -> **Success: no issues found in 18 source files**.
· `ruff check .` -> **75 errors**, and I built a `git archive b2b80ae` control tree and ran ruff there: **also 75**. The window added ZERO. (My 09-08 note of "63" was a different HEAD and is not the baseline for this window — the control tree is.)
· CI at HEAD, not at the commit next_steps quotes: `gh run list --branch main` returns `conclusion: success` for headSha `baeaa1ef`; `gh run view 34986410969` enumerates all six jobs — `pytest (Windows, Python 3.13) -> success`, four Ubuntu versions, mypy. This is what closed the baseline finding AND opened tonight's stale-record one.
· `--allow-unlinked` driven through the REAL parser: `python3 -m anneal_memory.cli save-continuity --help` shows it on the save-continuity subparser, so the `getattr(args, "allow_unlinked", False)` default at cli.py:1147 is not masking a mis-wired flag.
· ONE INJECTED-FAULT PROBE on a real store: `Store(retention_days=30)`, a connection proxy raising `sqlite3.OperationalError` only on prune's own `SELECT ... FROM episodes WHERE timestamp <`, driving the real `validated_save_continuity`. Produced the five-part signature in tonight's MEDIUM, including the operator's retry hitting "No wrap in progress".
· `_check_linkgate` walked against the write path it claims to mirror: `process_wrap_associations` (associations.py:480-483) builds `set(grad_result.direct_co_citations) | extract_session_co_citations(grad_result.all_validated_ids)`; the gate re-derives the identical set (continuity.py:1707-1715). `record_associations` counts every canonical pair as formed OR strengthened (associations.py:170-193, `_upsert_association` returns False at the cap), so `formed + strengthened == len(offered)` under a working write — the gate genuinely fires only on a write that recorded nothing. `valid_ids` traced to continuity.py:2064 (`{ep.id[:8].lower() for ep in episodes}`, filtered to the prepare snapshot at :2062), which is what makes types.py:673-680's "belong to THIS WRAP's episodes" claim true. `citation_counts` traced to graduation.py:670-671 (`cited_ids & valid_ids`, before the grounding and cross-session checks), which makes the "demoted lines included" claim true. `_GRADUATION_RE` (graduation.py:85) is `([2-9]|[1-9][0-9]+)x`, which makes "2x-and-up" true. All four documented gauge claims CHECKED, none filed.
· POST-COMMIT STORE CENSUS over continuity.py:2528-2900 — three store calls total (audit block :2540, retention check :2618, `prune()` :2633); the first two are guarded, the third is not. That census is what makes tonight's MEDIUM a class statement and not a sample.
· ENCODING-LESS I/O SWEEP of the package: zero. `grep` for `read_text()` / `write_text(` / `open(...,"r"|"w"|"a")` without `encoding=` across `anneal_memory/*.py` returns one hit (integrity.py:651) which is a false positive — `encoding="utf-8"` is on :653. The two test-side counts next_steps.md:111-112 records (**103** and **73**) I re-ran at HEAD and got exactly 103 and 73.
· COORDINATE SPOT-CHECKS via `awk NR==n`, not off diff hunks: store.py:6399 (the test comment's cited `newline=""` write — EXACT), cli.py:3073 (`default_db = _default_db()` — EXACT), server.py:1206 (the eager `expanduser()` — EXACT), cli.py:3627-3633, audit.py:2523, workflow:72. `cmp tool-integrity.json anneal_memory/tool-integrity.json` -> identical, so CLAUDE.md's "byte-identical copy" claim holds through the regeneration.
· `codeshape.py b2b80ae <path>` run on tests/test_audit.py, continuity.py and cli.py — all exit 1 (CODE CHANGED). No `DRIFT:` label is available for anything in this window and none was used.

⚠ NOT CHECKED — STATED PLAINLY, NOT IMPLIED:
· **NOTHING RUN ON WINDOWS BY ME.** Every Windows claim I make tonight comes from the GitHub Actions run, which is a green/red signal, not a behaviour I observed. I did not read the Windows job's log output; a test that passes vacuously on Windows would look identical from here.
· **NO MCP SERVER STARTED, NO TRANSPORT DRIVEN.** server.py's `citation_spread` line (:557-560), the `allow_unlinked` strict-boolean at :527 and the ValueError translation at :540-541 were READ, not exercised. next_steps.md:41 records MCP strict-boolean tests for `--allow-unlinked` as explicitly NOT written; I did not write them either.
· **THE LINKGATE REFUSAL WAS NEVER DRIVEN THROUGH THE CLI OR MCP.** I proved the flag parses; I did not produce an actual refusal or an actual override at the operator surface, because a refusal needs an injected mis-wire the CLI cannot reach. The suite's own tests cover the library path and pass; I did not write an independent one.
· **THE REAL ~/.anneal-memory STORE WAS NOT TOUCHED.** Every probe ran in /tmp.
· **NO CONCURRENCY WORK.** No two-process runs, no thread races. The three advisory-lock sites were confirmed by reading the `if fcntl is not None:` guards, never by racing them — including on the platform where they are no-ops.
· **PYTHON 3.13 ONLY — SIXTH NIGHT.** The 3.10/3.11/3.12 matrix is CI's, not mine.
· **tests/test_continuity.py's +341 lines were READ AS A DIFF AND RUN, NOT AUDITED LINE BY LINE.** I read the new `TestAmLinkgateBlock`, the `citation_spread` parametrize and the post-commit-warning test; I did not check every assertion against disk, and I did not mutate anything to prove the new tests can fail red. **That is the sharpest gap tonight**: I graded the block's design and the suite's green, not the suite's discriminating power, and a passing test I did not try to break is not evidence the guard works.
· **`project_memory/next_steps.md`'s +258 lines were read at the pickup top (1-45) and by targeted grep thereafter** — roughly 60 of 258. Two of its numbers I re-derived (103/73) and two of its coordinates I checked (cli.py:3073, server.py:1206); the rest of its claims, including the whole L3 triage block at :132ff and the store-copy gauge receipts at :187, I did not re-derive. **The one I DID check refuted itself** (:124), which is the argument for reading the other 200 lines and not the argument for trusting them.
· **CHANGELOG.md's +46 was read whole for the 0.9.11 entry only**; the claims in it were checked at two points (the O_BINARY CI clause, the `[Unreleased]` boundary), not walked claim by claim against code.
· **I DID NOT RE-VERIFY THE SEAT'S FOUR OPEN 0.9.12 ITEMS.** They are listed OUT of N in tonight's slot for exactly that reason.
· **THE `_warn_after_commit` FALLBACK'S OUTER `BaseException` PASSTHROUGH WAS NOT EXERCISED.** I read the docstring's reasoning about `KeyboardInterrupt` and agree with it; I did not raise one there.

[prescription: none]
  Tags: anneal-memory, code_review, semantic
```
