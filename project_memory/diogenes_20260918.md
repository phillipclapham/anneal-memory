# DIOGENES OVERNIGHT — anneal-memory, 2026-09-18

**Source selector:** `episodic.py read --since 24h --tags anneal-memory  (filtered to Agent: diogenes at 2026-09-18*)`
**Stated count: 5 episode(s).** Severity: LOW 2 — 2 of 5 episode(s) carry a severity; the other 3 are COVERAGE 1 · SELF 1 · STILL OPEN 1
**STILL OPEN: 2**

> ⚠ **UNTRIAGED AND VERBATIM.** Generated by `scripts/route_diogenes.py` from the episodic store without judgement. Nothing here has been checked against disk except where a line says so explicitly. Diogenes' own `[prescription: ...]` tags are his assessment, not a ruling.

---

```
[laptop/flow]   ID: diogenes-20260918-024239-ec92da7a8cd7
  Time: 2026-09-18T02:42:39  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: LOW: anneal_memory/server.py:869-875 (with store.py:4790 `_record_prune_failure`) — the new MCP status line keeps saying retention "may be behind" after retention has caught up. `_prune_failures` only ever increments; nothing resets it or records a later successful post-commit prune. It copies the `audit_write_failures` precedent, but that counter is monotonic on purpose (store.py ~1473: "a trail that lost an entry is permanently incomplete"). A prune failure is not permanent: CHANGELOG and both warnings say "Retention runs again on the next prune". So one disk-full blip in the first hour of a server that runs for weeks leaves a ⚠ in every status call for the process lifetime, and the operator cannot tell a failure that is still happening from one that recovered. BEHAVIOURAL, falsifiable signature: one Store with retention_days=7; one `wrap_completed(episodes_compressed=1, continuity_chars=10)` with `Store.prune` forced to raise sqlite3.OperationalError inside `_db_boundary`; the real prune restored; a second `wrap_completed` returns pruned_count 0 without raising, and a direct `prune()` returns 0. After that, `status().prune_failures == 1` and `Server(store)._tool_status({})` still contains the line beginning "⚠ 1 post-commit auto-prune failure(s) this session — retention may be behind". Reproduced in a scratch dir against the repo tree (anneal_memory.__file__ checked). Fix (a design call, several shapes work): stamp a `prune_last_success` from the post-commit success branches and render "may be behind" only while the last failure is newer, or drop the clause and print the count and timestamp alone. [quoted: `f"failure(s) this session — retention may be behind"`] [prescription: reasoned]
  Tags: anneal-memory, code_review, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260918-024239-984f36601760
  Time: 2026-09-18T02:42:39  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: LOW: tests/test_continuity.py:2740 — the new test's docstring cites a coordinate that was already stale in the commit that wrote it. At the parent 2e3ba86, store.py:3422 was `if self._retention_days is not None and not self._defer_commit:` (git show | grep -n). The same commit 48acdbe added 15 lines to `Store.__init__` and 2 to `status()` above that point, so at HEAD the guard is at store.py:3439 and the call `pruned = self.prune()` at 3447. The number was copied from last night's finding text, not grepped. STRUCTURAL. This is an L0 shape (a comment stating a number), and the next_steps closure block describes an L0 pass on this same diff. The file's code changed in the window (new tests), so this is a FINDING, not DRIFT. Fix: remove the line number and name the site, e.g. "The standalone ``Store.wrap_completed`` post-commit prune guard (outside ``_batch``)". [quoted: `"""The standalone ``Store.wrap_completed`` guard (store.py:3422,`] [prescription: reasoned]
  Tags: anneal-memory, code_review, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260918-024304-8cb0588e68d2
  Time: 2026-09-18T02:43:04  Agent: diogenes  Type: observation
  Source: laptop_overnight_review
  Content: STILL OPEN (2), at HEAD 48acdbe (== origin/main, live ls-remote). All four from 09-17's slot (STILL OPEN 4 @ 2e3ba86) are closed by 48acdbe. I checked each against the tree, not against next_steps. (1) The MEDIUM persistent-prune-silence is closed for the long-running case: `_record_prune_failure` runs at both catch sites (store.py:3460, continuity.py:2650) before the warn, and it reaches the MCP `status` tool (server.py:869). The CLI half is left unwired on purpose and routed to 0.9.12+. A fresh CLI process re-warns on every run, because the dedup is per process, so the CLI never had the silence. Not counted. (2) The standalone `wrap_completed` guard now has a test (test_continuity.py:2738, passes). (3) The stale "caller is told so" comment is gone. (4) CHANGELOG no longer says "required", and the workflow really has no `continue-on-error` (grep). The 2 open are both new tonight, both LOW: the MCP prune ⚠ never clears after retention recovers, and a test docstring cites store.py:3422, which the same commit moved to 3439. NOT in N: the older 0.9.12 residues next_steps records (two AM-LINKGATE residues, the stdin-reconfigure LOW, the eager default-db-path startup crash, and now the prune counter's durability for the CLI). I did not re-verify them tonight, so they are UNVERIFIED rather than counted. Filed separately: 0 DRIFT (not in this count).
  Tags: anneal-memory, code_review, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260918-024304-37d51a662824
  Time: 2026-09-18T02:43:04  Agent: diogenes  Type: observation
  Source: laptop_overnight_review
  Content: COVERAGE: anneal-memory 2026-09-18, HEAD 48acdbe == origin/main (live `git ls-remote`), porcelain empty, window 2e3ba86..48acdbe. That is 2 commits: 465094f is the route report plus the next_steps pointer only, and 48acdbe is the code commit (8 files). Read the full 48acdbe diff. Read both post-commit prune catch sites in full context (continuity.py Phase 5 at 2610-2660, store.py wrap_completed at 3410-3480), the server save path (server.py:530 passes the held `self._store`, so the counter lands on the instance status reads), the CLI `status --json` builder (cli.py:470-500: fields are listed explicitly, so the always-0 field cannot leak into the CLI via asdict), the audit counter's lifetime rationale (store.py ~1466-1485), the whole 09-17 routed report, and the new next_steps block. RAN: the full suite from the repo tree (.venv, anneal_memory.__file__ checked) → 2040 passed, 3 subtests passed; mypy on anneal_memory → clean, 18 files; `gh run list` → CI success on 48acdbe; a scratch repro of fail-then-recover prune → the ⚠ line persists (filed LOW); `git show 2e3ba86:store.py | grep -n` for the stale coordinate. NOT CHECKED: no revert-mutation of the new counter against its tests (the seat's next_steps claims pre-fix FAIL, and I did not reproduce that); no Windows execution; next_steps' "store.py ~4934" pointer to `_persist_audit_health` is actually 4967 (grep). That is a "~" approximation in a prose pickup file and was not filed; the right fix is subtraction, not re-numbering. Did not re-verify the older 0.9.12 residues. Did not read the rest of store.py or continuity.py outside the prune and status paths. [prescription: none]
  Tags: anneal-memory, code_review, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260918-024316-edce35d38d42
  Time: 2026-09-18T02:43:16  Agent: diogenes  Type: observation
  Source: laptop_overnight_review
  Content: SELF-CORRECTION: two coordinates I typed tonight were not grepped. (a) diogenes-20260918-024304-8cb0588e68d2 (the STILL OPEN slot) says the new wrap_completed test is at test_continuity.py:2738. `grep -n` puts `def test_wrap_completed_post_commit_prune_failure_does_not_fail_wrap(` at 2737; 2738 is its first parameter line. (b) diogenes-20260918-024239-ec92da7a8cd7 cites "store.py:4790 `_record_prune_failure`". The method is defined at store.py:4773, and 4790 is the `self._prune_failures += 1` inside it. The claims stand, the count stays (2), and every other coordinate in the slot was re-grepped (store.py:3460, continuity.py:2650, server.py:869). This is the same failure I filed against the seat's docstring an hour ago: a typed number sitting next to a correct claim.
  Tags: anneal-memory, code_review, semantic
```

---

## TRIAGE — 0919+21 maint-seat, 2026-09-19 (re-derived against disk; commits b66d5fb, 4504dea, 35553ed; NOT released, NOT pushed to PyPI)

- **LOW server.py:869 prune ⚠ never clears — CLOSED.** Reproduced by reading (`_prune_failures` monotonic, no recovery state). `StoreStatus.prune_behind` is set by `_record_prune_failure`, cleared by a `prune()` that completes AND covers the configured retention; the MCP line says "retention may be behind" only while set, else "a later prune completed, retention has caught up". Tests: `test_status_prune_line_clears_after_a_later_prune_completes`, `test_wider_override_prune_does_not_clear_the_behind_flag`, `test_override_prune_clears_flag_only_when_it_covers_retention` — each mutation-checked (clearing removed / ungated clear / `<=`→`<` each FAIL). Suite 2043 passed, mypy clean.
- **L3 (complement+codex+glm, round 1, bb63238..): all three converged on ONE MED in my first fix** (a wider `prune(older_than_days=365)` cleared the flag while 7-day retention was still violated) — fixed in 4504dea. Round 2 (complement+glm, fix diff): 2 LOW test/comment items fixed in 35553ed; glm's "retention None + explicit prune never clears" is **REFUTED as unreachable** — `_record_prune_failure` is only called from auto-prune sites that run only when `retention_days` is set. Round 2 had no codex (round 1 codex covered the class; the fix diff was 8 lines) [judged by 0919+21, 2026-09-19].
- **LOW tests/test_continuity.py:2740 stale store.py:3422 cite — CLOSED** (b66d5fb): line number removed, site named.
- ⚠ **RESIDUE, UNDISCHARGED:** flow's venv is pinned to 0.9.11; none of this reaches a real consolidate until the 0.9.12 cut + re-pin. CLI `status` still does not carry the prune counter (0.9.12+, per the 09-17 block).
