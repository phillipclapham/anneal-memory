# DIOGENES OVERNIGHT — anneal-memory, 2026-09-20

**Source selector:** `episodic.py read --since 24h --tags anneal-memory  (filtered to Agent: diogenes at 2026-09-20*)`
**Stated count: 5 episode(s).** Severity: LOW 2 — 2 of 5 episode(s) carry a severity; the other 3 are COVERAGE 1 · COVERAGE-OPEN 1 · STILL OPEN 1
**STILL OPEN: 2**

> ⚠ **UNTRIAGED AND VERBATIM.** Generated by `scripts/route_diogenes.py` from the episodic store without judgement. Nothing here has been checked against disk except where a line says so explicitly. Diogenes' own `[prescription: ...]` tags are his assessment, not a ruling.

---

```
[laptop/flow]   ID: diogenes-20260920-032352-8a73f9927780
  Time: 2026-09-20T03:23:52  Agent: diogenes  Type: observation
  Source: laptop_overnight_review
  Content: COVERAGE-OPEN: anneal-memory 2026-09-20, HEAD d70678b (== origin/main? yes), `git status --porcelain` <empty>, window bb63238..d70678b = 4 commits (the prompt said 3; all four landed 2026-09-19 16:35-16:47 EDT and all four are inside the 24h boundary, so I am reviewing four). REVIEW STARTED, NOT FINISHED — if no COVERAGE line for this repo exists in this run, THIS RUN DIED and the finding list is PARTIAL.
  Tags: anneal-memory, code_review, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260920-033258-2baa74b2a8f7
  Time: 2026-09-20T03:32:58  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: LOW: anneal_memory/types.py:169-170 (and the same claim at anneal_memory/store.py:1521-1523) — both comments say the flag is cleared by any `prune()` that COMPLETES. 4504dea narrowed that the same afternoon and updated neither comment, so the field's only documentation now states the exact behaviour the gate was added to prevent.

[quoted types.py:169-171:]
`    # True while the latest post-commit prune failure has not been followed by`
`    # a prune() that completed; False once retention has caught up.`
`    prune_behind: bool = False`

[quoted store.py:1521-1524:]
`        # True from a recorded post-commit prune failure until the next`
`        # prune() that completes; lets status() tell "still behind" from`
`        # "failed once, retention has since caught up" (Diogenes 2026-09-18).`
`        self._prune_behind: bool = False`

AGAINST DISK AT d70678b — the clear is gated, at store.py:4187-4191:
`        covers_retention = (`
`            older_than_days is None`
`            or (self._retention_days is not None and days <= self._retention_days)`
`        )`
and both clear sites (store.py:4200-4201, 4223-4224) are under `if covers_retention:`.

⛔ THE REPO'S OWN TEST REFUTES THE COMMENT, three lines of it, at tests/test_server.py:501-502:
`            assert s.prune(older_than_days=365) == 1`
`            assert s.status().prune_behind is True`
That is a `prune()` that completed — it deleted a row and returned 1 — with the flag still True. The comment's stated condition is satisfied and the field disagrees with it. This is not imprecision; it is false.

⚠ WHY IT IS NOT ZERO, AND ALSO WHY IT IS NOT MEDIUM. `StoreStatus` is public (`anneal_memory/__init__.py:30,139` — it is in `__all__`), and these two lines are the entire documentation of the field. next_steps.md already routes the next hand to this exact spot: "THEN wire it into the CLI `status` (`--json` and human)" for 0.9.12. That seat reads this comment. Against that: the VALUE is correct at every call site, so nothing downstream computes a wrong answer today — only a reader does. Hence LOW.

▶ NOT `DRIFT:` — checked mechanically, not by feel: `codeshape.py bb63238 anneal_memory/types.py` -> `CODE CHANGED — FINDING` (exit 1), same for store.py. Both fail the second half of the DRIFT test (the field itself was added in this window), so it resolves to a FINDING, which is the safe direction the rule specifies.

FIX: both comments need the coverage clause. types.py:169-170 -> "True while the latest post-commit prune failure has not been followed by a `prune()` that completed AND covered the configured `retention_days`; a wider override (`prune(older_than_days=365)` against `retention_days=7`) completes without clearing it, because it leaves 7..365-day-old episodes in place." store.py:1521-1523 -> the same qualifier on "until the next prune() that completes". Subtraction is not available here — the distinction is the whole content of 4504dea. [prescription: reasoned]
  Tags: anneal-memory, code_review, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260920-033314-047a2a06f903
  Time: 2026-09-20T03:33:14  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: LOW: CHANGELOG.md:17-22 — the `[Unreleased]` prune-health paragraph was written by 48acdbe (09-17) alongside its code and has not been extended for the 09-19 work, which added a PUBLIC field to `StoreStatus` and changed the MCP `status` tool's user-visible text. At the 0.9.12 cut the release notes will omit both.

[quoted CHANGELOG.md:17-22:]
`outcome unknown. Retention runs again on the next prune. A persistent failure warns only once under`
`Python's default `warnings` dedup, so `status()` now also reports `prune_failures` and`
`prune_last_failure`, and the MCP `status` tool surfaces them — this Store-instance only, unlike`
`audit_write_failures`: it does not survive a restart or converge across two instances on the same`
`database, so it is not yet in the CLI's `status`, which opens a fresh instance per invocation. Found`
`by review.`

AGAINST DISK AT d70678b. Nothing in that paragraph is FALSE — this is an omission, which is why it is LOW and not higher. What is missing:
 · `StoreStatus.prune_behind` (types.py:171) is a third public field on the same dataclass. `StoreStatus` is exported: `anneal_memory/__init__.py:30` imports it and `:139` lists it in `__all__`. A library consumer gets a new field with no changelog line.
 · the status line is no longer a fixed string. server.py:869-876 now prints "retention may be behind" only while the flag is set, else "a later prune completed, retention has caught up". The paragraph says only that the tool "surfaces them".

⛔ THE DISCIPLINE IS THIS REPO'S OWN, NOT MY PREFERENCE, and it is written down twice in its own memory. next_steps.md's 2026-09-16 block: "Prose (items 2–5): `[Unreleased]` now records the prune fix, the CLI stdio UTF-8 fix and Windows CI." Its 2026-09-17 block: "`CHANGELOG.md`'s `[Unreleased]` Windows-CI entry no longer calls the job 'required'". Both sessions treated the `[Unreleased]` edit as part of the fix. `git diff bb63238..HEAD --stat` for this window lists seven files and CHANGELOG.md is not among them. CHANGELOG.md:3 states the contract: "All notable changes to anneal-memory."

⚠ SCOPE HONESTLY: this is unreleased and 0.9.12 has not been cut (project_memory/diogenes_20260918.md records "NOT released, NOT pushed to PyPI"), so nothing has shipped wrong. The cost lands at the cut, when whoever writes the release notes reads this paragraph as the record of the prune work and it is one commit-generation stale.

FIX: extend the same paragraph rather than opening a new section — it is one arc. Two sentences: that a post-commit prune failure now also sets `StoreStatus.prune_behind`, cleared by a later `prune()` that covers the configured retention (a wider override does not clear it); and that the MCP `status` line consequently says "retention has caught up" instead of continuing to warn for the life of the process. [prescription: reasoned]
  Tags: anneal-memory, code_review, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260920-033328-2ad2240eba61
  Time: 2026-09-20T03:33:28  Agent: diogenes  Type: observation
  Source: laptop_overnight_review
  Content: STILL OPEN (2), TOTAL AT HEAD d70678b — 0 HIGH / 0 MEDIUM / 2 LOW, both NEW, both filed tonight. Filed separately: 0 DRIFT (not in this count).

NEW. LOW types.py:169-170 and store.py:1521-1523 both say `prune_behind` is cleared by any `prune()` that completes; 4504dea gated the clear on covering the configured retention the same afternoon and updated neither comment — the repo's own test at test_server.py:501-502 asserts a completed, row-deleting prune that leaves the flag True. LOW the `[Unreleased]` prune paragraph was not extended when the same work added a public `StoreStatus` field and made the MCP status line conditional; the two prior sessions in this arc both edited it as part of the fix.

CARRIED: none. BOTH 09-18 LOWs are CLOSED, resolved against the tree at HEAD and by running, not taken from the seat's word — and this time the repo's triage file agrees with the tree, which is worth saying after last night's opposite result elsewhere. (1) LOW "server.py:869 prune ⚠ never clears" — closed by b66d5fb+4504dea+35553ed; server.py:869-876 is now conditional on `status.prune_behind`, and I did not stop at reading it (see the mutation runs in COVERAGE). (2) LOW "tests/test_continuity.py stale store.py:3422 cite" — `grep -n "3422" tests/test_continuity.py` -> ZERO hits at HEAD; the docstring now names the site without a coordinate.

⛔ THE CODE IN THIS WINDOW IS SOUND AND I COULD NOT BREAK IT. The three-commit prune_behind arc is the good kind: a real defect, a fix, an L3 that found a real MED inside the fix, a second fix, and tests that genuinely discriminate. Both findings tonight are description-level, on a diff whose CODE I tried and failed to fault. That is the honest shape of this night — an empty code half is a result, not a gap.
  Tags: anneal-memory, code_review, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260920-033359-4d7a2a6ad782
  Time: 2026-09-20T03:33:59  Agent: diogenes  Type: observation
  Source: laptop_overnight_review
  Content: COVERAGE: anneal-memory 2026-09-20, HEAD d70678b == origin/main, `git status --porcelain` empty, window bb63238..d70678b = 4 commits (the prompt said 3; all four landed 2026-09-19 16:35-16:47 EDT, so all four are inside the boundary and all four were reviewed).

READ IN FULL, not diff-scoped: `Store.prune` (store.py:4160-4235, the whole method), the `_prune_*` declarations (store.py:1510-1525), `Store.status` (store.py:2366-2385), `_record_prune_failure` (store.py:4789-4813), both post-commit catch sites (store.py:3438-3472 `wrap_completed`, continuity.py:2610-2660 `validated_save_continuity` Phase 5), `_tool_status`'s prune block (server.py:855-882), `StoreStatus` in full (types.py:98-171), all three new tests plus the touched docstring, and both changed project_memory files against the 09-16/09-17/09-18 blocks above them.

RE-DERIVED BY RUNNING, not carried forward:
· Suite at HEAD: `2040 passed, 3 skipped, 19 warnings, 3 subtests passed` under the flow venv. `mypy anneal_memory/` -> `Success: no issues found in 18 source files`.
· ⚠ AND THE SKIPS ARE MINE, NOT THE REPO'S — I nearly filed a finding on this. project_memory/diogenes_20260918.md's triage claims "Suite 2043 passed"; my first run printed 2040+3, and 2040+3 is exactly the arithmetic a fabricated number would show. It is not fabricated. The 3 skips are `pytest.importorskip("hatchling...")` in tests/test_packaging.py, and the repo has its own `.venv` with hatchling installed: `.venv/bin/python -m pytest tests/ -q` prints `2043 passed`, reproducing the seat's number verbatim. The wrong interpreter, not the wrong claim. Dropped.
· ALL THREE MUTATIONS THE COMMIT MESSAGES CLAIM, run in a scratch clone at d70678b (`/tmp/dio_am`), each RED-and-GREEN with a restored control, never green-only. Control 3 passed -> (1) b66d5fb "clearing removed -> test FAILS": both clear sites disabled -> 3 FAILED. (2) 4504dea "ungated clear -> test FAILS": `covers_retention = True` -> 2 FAILED, 1 passed. (3) 35553ed "`<=` -> `<` FAILS": -> 1 FAILED, 2 passed. Restored -> 3 passed. Every claimed mutation reproduces and the tests discriminate; they are not vacuous.
· b66d5fb's `Sibling-grep receipt:` RE-RUN VERBATIM at its own commit AND at HEAD: `git grep -n "prune_failures\|prune_last_failure" -- anneal_memory/ tests/` -> consumers are server.py `_tool_status` only; cli.py has ZERO hits at both refs. The receipt reproduces exactly, number and conclusion. I also ran the same grep for the NEW field: `prune_behind` consumers are server.py:875, store.py, types.py and tests only — same shape, no CLI gap introduced.
· The refutation the seat recorded, checked rather than accepted: glm's "retention None + explicit prune never clears" is indeed unreachable — `_record_prune_failure` has exactly two non-test callers (store.py:3465, continuity.py:2650), both inside `if ... _retention_days is not None`, and `_retention_days` is assigned once at store.py:1414 with no setter. The seat's REFUTED verdict stands.
· Docs sweep for stale siblings of the changed status text: `grep -rn "prune_failures\|prune_behind\|retention may be behind\|auto-prune" README.md CHANGELOG.md docs/ skill/ examples/ scripts/` -> only CHANGELOG.md:7,18 (the finding) and an unrelated 2394 historical entry. No README or docs/ page describes the status line, so nothing else went stale.
· `codeshape.py bb63238` on store.py and types.py -> `CODE CHANGED — FINDING` (exit 1) on both; that is why tonight's comment item is a FINDING and not `DRIFT:`.
· The two prior-night LOWs re-verified against the tree, not the triage file: `grep -n "3422" tests/test_continuity.py` -> 0 hits; the status line is conditional at server.py:869-876.

NOT CHECKED — and some of this matters:
· NO CODE WAS RUN AGAINST A REAL ANNEAL STORE. Everything is tmp_path fixtures and a scratch clone. The residue both triage blocks flag is still undischarged: flow's venv is pinned to 0.9.11, so none of this arc has ever executed on a live consolidate, and I did not test that path.
· I did not exercise the MCP transport end-to-end. The recovery test drives `Store.wrap_completed` directly; nothing I ran proves a real MCP `save_continuity` call clears the flag, though it routes through the same `validated_save_continuity` prune.
· No multi-process or two-Store-on-one-DB behaviour tested — the docstrings disclose non-convergence and I took that disclosure as read.
· I did not review the ~2000 tests outside test_server.py/test_continuity.py beyond running them, and I opened no file in `anneal_memory/` outside store.py, server.py, types.py, continuity.py's Phase 5 and `__init__.py`. audit.py, cli.py and the spore/crystal surfaces were untouched by this window and I did not read them.
· `tool-integrity.json` carries a `status` hash (`a7840dd1…`); I did NOT trace what it hashes, so I cannot say whether the status TOOL DESCRIPTION is in scope of that digest or only the schema. If it covers output text, this window may owe it a regeneration. Unverified, named rather than guessed.

⚠ COMMITS THAT MOVED A NAMED FACT WITH NO `Sibling-grep:` TRAILER — reported here as the rule says, not as findings: **4504dea**, which moved the clear condition itself (the largest fact-mover in the window) and is the commit whose own two comments went stale as a result — a sibling grep for `prune_behind` would have printed exactly those two lines. **35553ed** likewise. b66d5fb is the only commit in the window carrying a receipt, and it is the one whose siblings are clean. [prescription: none]
  Tags: anneal-memory, code_review, semantic
```
