# DIOGENES OVERNIGHT — anneal-memory, 2026-09-25

**Source selector:** `episodic.py read --since 1d --tags anneal-memory  (filtered to Agent: diogenes at 2026-09-25*)`
**Stated count: 7 episode(s).** Severity: LOW 1 · MEDIUM 1 — 2 of 7 episode(s) carry a severity; the other 5 are COVERAGE 1 · COVERAGE-OPEN 1 · SELF 2 · STILL OPEN 1
**STILL OPEN: 4**

> ⚠ **UNTRIAGED AND VERBATIM.** Generated by `scripts/route_diogenes.py` from the episodic store without judgement. Nothing here has been checked against disk except where a line says so explicitly. Diogenes' own `[prescription: ...]` tags are his assessment, not a ruling.

---

```
[laptop/flow]   ID: diogenes-20260925-021121-eedffb0d1ade
  Time: 2026-09-25T02:11:21  Agent: diogenes  Type: observation
  Source: laptop_overnight_review
  Content: COVERAGE-OPEN: anneal-memory 2026-09-25, HEAD 395d420 (== origin/main? yes), `git status --porcelain` empty, window 3cccf4f..395d420 = 24 commits (0.9.12 marker-as-meaning + compost=, 0.9.13 baton gate). REVIEW STARTED, NOT FINISHED — if no COVERAGE line for this repo exists in this run, THIS RUN DIED and the finding list is PARTIAL.
  Tags: anneal-memory, code_review, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260925-021711-8bcf2b77589f
  Time: 2026-09-25T02:17:11  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: MEDIUM: anneal_memory/continuity.py:1736-1745 (_check_save_authority) — the save gate and the prepare gate disagree about the same session. `prepare_wrap(store, session_id="a", allow_sole_live=True)` authorizes a sole live session that holds NO baton (reason "sole-live-session", status "ready", wrap marked in progress). The CHANGELOG offers exactly this as the 0.9.13 migration (CHANGELOG.md:20-21: a single-session caller "calls claim_baton ... once, or passes allow_sole_live=True"). But `validated_save_continuity(..., session_id="a")` has no allow_sole_live and requires the baton unconditionally, so the save is refused and the wrap is left stranded in progress. The refusal also misstates its cause: it says the baton "may have been taken, or the baton file is unreadable". Neither is true, because it was never held, and the docstring at :1950-1952 ("proceeds only if that session STILL holds") presupposes a prior hold the sole-live path never had. The only workaround is to drop session_id from the save, which drops the save-time re-check entirely. No test covers the save leg of a sole-live wrap: every allow_sole_live test in tests/test_sessions.py stops at consolidate_authorized/prepare_wrap. Fix: either give validated_save_continuity the same allow_sole_live (re-run consolidate_authorized with it at the save, ignored on a policy store the way prepare ignores it), or document that a sole-live caller must not name itself at the save and correct the message for the never-held case. Pin it with a prepare→save test either way.
[quoted: `            held = sessions.holds_baton(store.continuity_path, session_id)` … `f"Session {session_id!r} does not hold the consolidate baton (it may have "` `f"been taken, or the baton file is unreadable), so it may not commit this "`]
BEHAVIOURAL, falsifiable signature, run 2026-09-25 against HEAD 395d420 on a fresh scratch Store holding one episode, with register_session(cp,"a") and no baton claimed: prepare_wrap(st, session_id="a", allow_sole_live=True) → status "ready". Then validated_save_continuity(st, <valid text>, wrap_token=<that token>, session_id="a") → ValueError "Session 'a' does not hold the consolidate baton (it may have been taken, or the baton file is unreadable), so it may not commit this wrap. Nothing was written.", after which st.status().wrap_in_progress is True. The identical save without session_id → succeeds. [prescription: reasoned]
  Tags: anneal-memory, code_quality, code_review
```

---

```
[laptop/flow]   ID: diogenes-20260925-021719-4c9ba0de584e
  Time: 2026-09-25T02:17:19  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: LOW: anneal_memory/types.py:543-544 and anneal_memory/continuity.py:1639-1643 both still describe PrepareWrapResult.status as TWO values, while the Literal at types.py:557 is `Literal["empty", "ready", "downgraded"]`. types.py says the Literal lets IDE autocomplete "offer the two valid status values". continuity.py (format_wrap_package_text) says `PrepareWrapResult.status is Literal["empty", "ready"]; on "empty" the package is None`. Both date from 934a3ce (2026-04-10) and went stale when db62917 added "downgraded" (2026-06-27), so they predate this window. They are filed tonight because the window touched both sites: 93577cd rewrote the "downgraded" sentence at types.py:536-540, two lines above the stale count, and left the count alone. fe05cb0 made "downgraded" reachable through format_wrap_package_text from the CLI and MCP transports for the first time (the require-baton policy downgrades a caller with no session_id), which is the branch whose comment names only "empty". The code is correct: `if result["status"] != "ready": return result["message"]` handles all three. Only the descriptions are wrong. Fix: say "three" and list "downgraded" in both comments. Not DRIFT, because both files' code changed in the window. [quoted: `offer the two valid status values; callers typoing` · `# PrepareWrapResult.status is Literal["empty", "ready"]; on` · `status: Literal["empty", "ready", "downgraded"]`] [prescription: reasoned]
  Tags: anneal-memory, code_review, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260925-021730-0ad1fc734efa
  Time: 2026-09-25T02:17:30  Agent: diogenes  Type: observation
  Source: laptop_overnight_review
  Content: SELF-CORRECTION: diogenes-20260925-021719-4c9ba0de584e says 93577cd "rewrote the 'downgraded' sentence at types.py:536-540". Too broad. git blame shows 93577cd rewrote lines 538-540 only. Lines 536-537 and 541 of that sentence are still db62917 (2026-06-27). The corrected claim: 93577cd rewrote three lines inside the sentence that ends five lines above the stale "two valid status values", and left the count alone. The finding stands.
  Tags: anneal-memory, code_review, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260925-021737-f455eb041973
  Time: 2026-09-25T02:17:37  Agent: diogenes  Type: observation
  Source: laptop_overnight_review
  Content: SELF-CORRECTION: diogenes-20260925-021730-0ad1fc734efa introduced a new wrong number while correcting an old one. It says the sentence "ends five lines above" the stale count, and I typed that without counting. Grepped: the sentence runs types.py:536-541, 93577cd rewrote 538-540, and the stale "offer the two valid status values" is at :544. That is three lines below the sentence's end and four below the last rewritten line. Neither correction changes the finding (diogenes-20260925-021719-4c9ba0de584e), which stands.
  Tags: anneal-memory, code_review, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260925-021748-a7076e93e691
  Time: 2026-09-25T02:17:48  Agent: diogenes  Type: observation
  Source: laptop_overnight_review
  Content: STILL OPEN (4), TOTAL AT HEAD 395d420 == origin/main, checked against the tree and not against my store: 0 HIGH / 1 MEDIUM / 3 LOW. Filed separately: 0 DRIFT (not in this count).

NEW tonight (2): MEDIUM continuity.py:1736-1745. A sole-live-authorized prepare (the documented 0.9.13 migration) is refused at save when it names itself, which strands the wrap in progress with a misleading reason (diogenes-20260925-021711-8bcf2b77589f, reproduced). LOW types.py:544 + continuity.py:1639 still say the prepare status has two values (diogenes-20260925-021719-4c9ba0de584e, with two self-corrections on my own line arithmetic, the finding unchanged).

CARRIED from 09-20 (2). The routed report diogenes_20260920.md was never triaged: next_steps.md:11 still reads "STILL OPEN: 2 @ d70678b". I re-verified both at HEAD. (1) LOW: types.py:169-171 still says prune_behind is True "while the latest post-commit prune failure has not been followed by a prune() that completed", and store.py:1525-1527 still says "until the next prune() that completes". The clear is gated on `if covers_retention:` at store.py:4275 and :4298. (2) LOW, and worse than on 09-20: the [Unreleased] prune paragraph it flagged has since SHIPPED as the published 0.9.12 entry (CHANGELOG.md:119-132) without mentioning the public StoreStatus field `prune_behind`. `grep -n prune_behind CHANGELOG.md` returns 0 hits, so a public field went to PyPI undocumented.
  Tags: anneal-memory, code_review, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260925-021800-2bcac4351f06
  Time: 2026-09-25T02:18:00  Agent: diogenes  Type: observation
  Source: laptop_overnight_review
  Content: COVERAGE: anneal-memory 2026-09-25, window 3cccf4f..395d420 (24 commits: 0.9.12 marker-as-meaning + compost=, 0.9.13 baton gate, releases/bumps/next_steps). I READ the full diffs of sessions.py, continuity.py, store.py, crystal.py, types.py, cli.py, server.py, audit.py, __init__.py and README, plus the 0.9.12 and 0.9.13 CHANGELOG entries, checking each claim against the code. RUN: the full suite at HEAD (2089 passed, 3 skipped; the in-repo package imported, 0.9.14.dev0). 15 edge-case lines through _extract_pattern_meta (markers-only → "", quoted-why fallback, unparsed evidence tag → "", bracketed prose kept, a stray "[" in a provenance tag). A scratch baton lifecycle: claim, same-holder no-op, refusal without take, non-holder release False, take, stale-holder reason, a `[]` baton → downgraded-registry-error, unreadable refusal, take-recovery. The sole-live prepare→save reproduction that grounds the MEDIUM. RE-DERIVED: the Sibling-grep receipts of 38aae99, 855e94d, 1cd60f5 and f909ac1 at their own commits, all holding. The v0.9.12 and v0.9.13 tags point at stamps 5e5b136 and f3ae58a, whose __init__/pyproject/server.json read the released number. The in-batch _check_save_authority really is the last statement before the _batch exit. sever_pattern_concept is batch-aware. Both carried 09-20 LOWs re-checked at HEAD. NOT CHECKED: concurrency of the flock/os.link claim paths under real parallel processes (single-process scratch only), and nothing at all on Windows or a no-hardlink filesystem. The PyPI artifact hashes in next_steps (not fetched). project_memory/flow_adaptation_0913.patch against flow's tree (grepped for the take= call shape only). tests/*.py diffs read by grep, not line by line. SIBLING-GREP GAP: the baton commits 93577cd, fe05cb0, 4691cd5, 328bbaf, cf400b9, b82b4ed, fa040ea and 3ee6ad6 retired two named facts (the sole-live grant, last-claim-wins) and carry NO Sibling-grep trailer. My own grep for "sole.live|last-claim-wins|last-wins|auto-authorized" outside tests found only the rewritten sites, plus two unrelated "last-wins" in graduation.py. docs/, skill/ and examples/ have no baton text at all. [prescription: none]
  Tags: anneal-memory, code_review, semantic
```
