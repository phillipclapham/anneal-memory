# DIOGENES OVERNIGHT — anneal-memory, 2026-09-26

**Source selector:** `episodic.py read --since 24h --tags anneal-memory  (filtered to Agent: diogenes at 2026-09-26*)`
**Stated count: 4 episode(s).** Severity: LOW 2 — 2 of 4 episode(s) carry a severity; the other 2 are COVERAGE 1 · STILL OPEN 1
**STILL OPEN: 2**

> ⚠ **UNTRIAGED AND VERBATIM.** Generated by `scripts/route_diogenes.py` from the episodic store without judgement. Nothing here has been checked against disk except where a line says so explicitly. Diogenes' own `[prescription: ...]` tags are his assessment, not a ruling.

---

```
[laptop/flow]   ID: diogenes-20260926-020547-5a9dab9124e2
  Time: 2026-09-26T02:05:47  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: LOW: anneal_memory/continuity.py:1515-1519 with :1551-1553 and :1569-1570 (prepare_wrap, 0.9.15 empty-window path) — a TRANSIENT database error is classified as a CORRUPT lifecycle, and the corrupt-lifecycle branch is the one that cancels unconditionally and switches off the gated-wrap refusal. `except StoreError:` also catches StoreDatabaseError (store.py:573 `class StoreDatabaseError(StoreError)`, which _db_boundary raises for ANY sqlite3.DatabaseError, e.g. "database is locked" / disk I/O on the one SELECT inside load_wrap_snapshot). That sets observed_partial=True, which (a) forces gated_by to None, so the 0.9.15 guarantee "a caller that names no session_id ... is likewise refused the empty-path cancel of a wrap that was prepared under the gate" is skipped, and (b) takes the `elif observed_partial: store.wrap_cancelled()` branch, a clear with no expect_token. Net: a sessionless caller (the CLI/MCP default) on a store without the require-baton policy whose one snapshot read hits a DB error destroys the baton holder's healthy in-flight wrap, the exact outcome the release's headline fix exists to prevent. Needs an emptied window plus a failing read, so it is rare, hence LOW. Fix: only the integrity StoreErrors that load_wrap_snapshot raises for partial state mean "partial"; catch StoreDatabaseError first and let it propagate (or return downgraded-wrap-replaced, retry) instead of treating it as corruption. Pin it with a test that faults the snapshot read once.
[quoted: `    except StoreError:` · `        observed, observed_partial = None, True` · `            observed_gated_by if session_id is None and not observed_partial else None` · `            elif observed_partial:` · `                store.wrap_cancelled()`]
BEHAVIOURAL, falsifiable signature, run 2026-09-26 at HEAD 30cf008 (in-repo 0.9.16.dev0) on a fresh scratch Store: record one episode, claim_baton("holder"), prepare_wrap(session_id="holder") → "ready", delete the episode. Control: prepare_wrap(store) → status "downgraded" (message "downgraded-gated-wrap-open"), status().wrap_in_progress True, wrap_gated_session() "holder". Same setup with store.load_wrap_snapshot made to raise StoreDatabaseError("database is locked") on its FIRST call only: prepare_wrap(store) → status "empty" ("No episodes since last wrap. Nothing to compress."), wrap_in_progress False, wrap_gated_session() None. The fault was injected by replacing the bound method, not produced by a real contended SQLite. [prescription: reasoned]
  Tags: anneal-memory, code_quality, code_review
```

---

```
[laptop/flow]   ID: diogenes-20260926-020559-72e6ff88d6a9
  Time: 2026-09-26T02:05:59  Agent: diogenes  Type: finding
  Source: laptop_overnight_review
  Content: LOW: docs/library-quickstart.md:218 (Error Handling) — the public error doc never names SaveAuthorityError, the class 0.9.15 published for exactly the refusal this paragraph describes. The paragraph still files the consolidate-gate refusal under "**Plain `ValueError`**", and the hierarchy block below it (AnnealMemoryError / StoreError / StoreDatabaseError / WrapInProgressError) has no entry for it either, while CHANGELOG.md:48 tells callers to "Test with `isinstance`, not the message text" — there is no class name in the docs to test against. `grep -rn SaveAuthorityError docs README.md skill` = 0 hits. Sequencing: the quickstart was last edited in 34b67d7; the class arrived two commits later in 54e3b5e, whose Sibling-grep trailer (`git grep -n 'SaveAuthorityError'`) searched for the NEW name and so structurally could not find prose describing the refusal under its old one. The paragraph's list also omits the 0.9.15 refusal of a gated wrap's save without its wrap_token. Code is correct (SaveAuthorityError subclasses ValueError, so nothing breaks); only the published description lags. Fix: name `SaveAuthorityError` (a ValueError subclass, top-level export) in that sentence, add the missing-token case, and note it beside the hierarchy as outside AnnealMemoryError. [quoted: `**Plain \`ValueError\`** is raised by \`validated_save_continuity\` for caller-contract violations` · `and the consolidate-gate refusal (a \`session_id\` that no longer holds the baton` · CHANGELOG.md:48 `Test with \`isinstance\`,`] [prescription: reasoned]
  Tags: anneal-memory, code_review, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260926-020616-7c36d0739972
  Time: 2026-09-26T02:06:16  Agent: diogenes  Type: observation
  Source: laptop_overnight_review
  Content: STILL OPEN (2), TOTAL AT HEAD 30cf008 == origin/main, checked against the tree and not my store: 0 HIGH / 0 MEDIUM / 2 LOW. Filed separately: 0 DRIFT (not in this count). NEW tonight (2): LOW continuity.py:1515-1519 — a transient StoreDatabaseError on the empty-window snapshot read is treated as a corrupt lifecycle, so a sessionless prepare cancels a healthy gated wrap unconditionally (diogenes-20260926-020547-5a9dab9124e2, reproduced with an injected fault). LOW docs/library-quickstart.md:218 — the public error doc never names SaveAuthorityError (diogenes-20260926-020559-72e6ff88d6a9). CARRIED from 09-25: none. All four of diogenes_20260925.md are closed at HEAD, re-verified in the window diff rather than taken from next_steps: the sole-live save leg (c4e3f79/8ae5b8b, _check_save_authority now takes allow_sole_live), the status-count comments (types.py "offer the valid status values", continuity.py "is a Literal (see types.py)"), the prune_behind comments (types.py:170-173, store.py:1545-1546), and the CHANGELOG [0.9.14] prune_behind paragraph. Not counted: the residuals CHANGELOG [0.9.15] states as bounded (baton take between final check and commit / wrap_started; policy flip before a sessionless wrap_started; corrupt-lifecycle clear racing a repairer; reverse-order policy audits) and next_steps' own "gated-wrap check runs before the token-mismatch check".
  Tags: anneal-memory, code_review, semantic
```

---

```
[laptop/flow]   ID: diogenes-20260926-020616-e22330422d6f
  Time: 2026-09-26T02:06:16  Agent: diogenes  Type: observation
  Source: laptop_overnight_review
  Content: COVERAGE: anneal-memory 2026-09-26, window 89b56a7..30cf008 (18 commits: 0.9.14 sole-live save fix, 0.9.15 gated-wrap/strict-match/empty-path CAS/SaveAuthorityError, stamps, bumps, next_steps). READ the full window diffs of continuity.py, store.py, types.py, cli.py, __init__.py, CHANGELOG [0.9.14]+[0.9.15], README gate paragraph, docs/library-quickstart.md, skill/SKILL.md, server.json/pyproject; read load_wrap_snapshot, _db_boundary, set_consolidate_requires_baton and wrap_completed's token CAS in full on disk. RUN: full suite at HEAD, 2125 passed (in-repo 0.9.16.dev0 via .venv). A scratch reproduction of the empty-path fault (control + injected StoreDatabaseError) grounding the LOW. A two-connection policy toggle (set True/True/False/True across two Store objects): stored value and audit `was` sequence False,True,True,False correct. RE-DERIVED: 54e3b5e's `git grep -n 'SaveAuthorityError'` trailer at that commit over docs/README/skill → 0 hits (the basis of the docs LOW); the other seven Sibling-grep trailers in the window were NOT re-run individually. Confirmed the strict-match gated_by read before the batch is safe because wrap_completed re-verifies the token by CAS inside the write transaction. NOT CHECKED: real multi-process concurrency (the empty-path CAS, BEGIN IMMEDIATE under an actual writer holding the lock, baton sidecar races) — single-process scratch only; the fault was injected, not a real SQLITE_BUSY. tests/test_sessions.py's +372 lines read by grep and the targeted cases only, not line by line. PyPI artifacts, the v0.9.14/v0.9.15 tags and the GitHub releases not fetched or compared. flow's anneal_dualwrite.py 0.9.15 fix (f0361e64) not opened — out of this repo. Nothing on Windows / no-flock filesystems. [prescription: none]
  Tags: anneal-memory, code_review, semantic
```
